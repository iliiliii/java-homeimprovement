# API 500错误排查指南

## 错误信息

```
code: 500
msg: "No static resource app/guest/demo-projects."
```

## 问题分析

这个错误通常表示：
1. Controller没有被正确注册
2. 后端代码没有部署
3. 路由配置有问题
4. Spring扫描包路径配置错误

---

## 排查步骤

### 步骤1：验证Controller是否存在

**测试健康检查接口：**
```bash
curl https://hsdlp.gzcelestial.com/prod-api/app/guest/health
```

**预期结果：**
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "status": "ok",
    "timestamp": 1234567890,
    "message": "游客配置服务正常运行"
  }
}
```

**如果返回404或500：** Controller没有被正确注册

### 步骤2：检查后端是否部署了新代码

**检查方法：**
1. 查看后端日志，搜索 `[游客配置API]`
2. 如果没有日志，说明代码没有部署
3. 需要重新编译和部署

**部署步骤：**
```bash
cd sb3
mvn clean package
# 重启服务
systemctl restart evs-home
```

### 步骤3：检查Spring扫描包路径

**查看启动类：**
```java
@SpringBootApplication
@ComponentScan(basePackages = {"com.ruoyi"})
public class RuoYiApplication {
    // ...
}
```

**确认：**
- `AppGuestConfigController` 在 `com.ruoyi.app.controller` 包下
- Spring能够扫描到这个包

### 步骤4：检查字典配置

即使Controller正常工作，如果字典配置不存在，也可能返回空数据。

**检查字典：**
1. 登录后台管理系统
2. 进入 系统管理 > 字典管理
3. 查找字典类型 `guest_demo`
4. 确认有字典数据

**如果没有字典：**
- 添加字典类型：`guest_demo`
- 添加字典数据：
  - 字典标签：`projects_01`
  - 字典键值：`实际的项目ID`

---

## 临时解决方案

如果后端暂时无法部署，可以在前端使用硬编码的项目ID：

### 修改 guest.js

```javascript
/**
 * 从服务端加载游客演示项目配置
 * @returns {Promise<void>}
 */
const loadGuestConfig = async () => {
  // 如果已经加载过，直接返回
  if (CONFIG_LOADED) {
    return Promise.resolve()
  }
  
  // 如果正在加载，等待加载完成
  if (CONFIG_LOADING) {
    return new Promise((resolve) => {
      LOAD_CALLBACKS.push(resolve)
    })
  }
  
  CONFIG_LOADING = true
  
  try {
    console.log('[Guest Config] 开始加载游客演示项目配置')
    
    const response = await uni.request({
      url: 'https://hsdlp.gzcelestial.com/prod-api/app/guest/demo-projects',
      method: 'GET',
      timeout: 10000
    })
    
    if (response.statusCode === 200 && response.data.code === 200) {
      const data = response.data.data
      GUEST_DEMO_PROJECT_IDS = data.projectIds || []
      DEFAULT_GUEST_PROJECT_ID = data.defaultProjectId || ''
      CONFIG_LOADED = true
      
      console.log('[Guest Config] 配置加载成功:', {
        projectIds: GUEST_DEMO_PROJECT_IDS,
        defaultProjectId: DEFAULT_GUEST_PROJECT_ID,
        count: data.count
      })
      
      // 通知所有等待的回调
      LOAD_CALLBACKS.forEach(callback => callback())
      LOAD_CALLBACKS = []
    } else {
      console.error('[Guest Config] 配置加载失败:', response.data)
      throw new Error('加载配置失败')
    }
  } catch (error) {
    console.error('[Guest Config] 配置加载异常:', error)
    
    // ===== 临时方案：使用硬编码的项目ID =====
    console.warn('[Guest Config] 使用硬编码的演示项目ID')
    GUEST_DEMO_PROJECT_IDS = ['9fa800b545b445e4b699b1598bec4619'] // 替换为实际的项目ID
    DEFAULT_GUEST_PROJECT_ID = '9fa800b545b445e4b699b1598bec4619' // 替换为实际的项目ID
    CONFIG_LOADED = true
    // ===== 临时方案结束 =====
    
    // 通知所有等待的回调
    LOAD_CALLBACKS.forEach(callback => callback())
    LOAD_CALLBACKS = []
  } finally {
    CONFIG_LOADING = false
  }
}
```

**注意：** 这只是临时方案，正式环境应该使用后端API。

---

## 完整的后端文件清单

确保以下文件都已创建并部署：

### 1. Controller
**文件：** `sb3/evs-home/src/main/java/com/ruoyi/app/controller/AppGuestConfigController.java`

**关键注解：**
```java
@RestController
@RequestMapping("/app/guest")
public class AppGuestConfigController {
    @GetMapping("/demo-projects")
    public AjaxResult getDemoProjects() { ... }
    
    @GetMapping("/health")
    public AjaxResult health() { ... }
}
```

### 2. Service接口
**文件：** `sb3/evs-home/src/main/java/com/ruoyi/app/service/IGuestConfigService.java`

### 3. Service实现
**文件：** `sb3/evs-home/src/main/java/com/ruoyi/app/service/impl/GuestConfigServiceImpl.java`

**关键注解：**
```java
@Service
public class GuestConfigServiceImpl implements IGuestConfigService {
    // ...
}
```

### 4. 枚举修改
**文件：** `sb3/evs-home/src/main/java/com/ruoyi/app/enums/UserTypeEnum.java`

**添加：**
```java
GUEST("guest", "游客")
```

### 5. Context修改
**文件：** `sb3/evs-home/src/main/java/com/ruoyi/app/security/AppContext.java`

**添加：**
```java
public static boolean isGuest() {
    return UserTypeEnum.GUEST.equals(getUserType());
}
```

---

## 验证清单

部署后，按顺序验证：

- [ ] 健康检查接口正常：`GET /app/guest/health`
- [ ] 演示项目配置接口正常：`GET /app/guest/demo-projects`
- [ ] 字典配置已添加：`guest_demo` 字典类型
- [ ] 字典数据已添加：`projects_01` 对应项目ID
- [ ] 前端能正常获取配置
- [ ] 未登录用户能访问设计页面
- [ ] 未登录用户能访问日志页面

---

## 常见问题

### Q1: 健康检查接口返回404

**原因：** Controller没有被Spring扫描到

**解决：**
1. 检查包路径是否正确
2. 检查 `@RestController` 注解是否存在
3. 检查启动类的 `@ComponentScan` 配置

### Q2: 健康检查接口正常，但demo-projects返回500

**原因：** Service层有异常

**解决：**
1. 查看后端日志
2. 检查 `GuestConfigServiceImpl` 是否有 `@Service` 注解
3. 检查字典配置是否存在

### Q3: 接口返回空数据

**原因：** 字典配置不存在或配置错误

**解决：**
1. 检查字典类型 `guest_demo` 是否存在
2. 检查字典数据是否正确
3. 检查字典标签是否为 `projects_01`

### Q4: 前端仍然报错

**原因：** 缓存问题或前端代码未更新

**解决：**
1. 清除小程序缓存
2. 重新编译前端代码
3. 重新上传小程序

---

## 调试技巧

### 1. 查看后端日志

```bash
# 实时查看日志
tail -f /path/to/logs/evs-home.log

# 搜索游客配置相关日志
grep "游客配置" /path/to/logs/evs-home.log
```

### 2. 使用curl测试

```bash
# 测试健康检查
curl -X GET "https://hsdlp.gzcelestial.com/prod-api/app/guest/health"

# 测试演示项目配置
curl -X GET "https://hsdlp.gzcelestial.com/prod-api/app/guest/demo-projects"

# 测试检查演示项目
curl -X GET "https://hsdlp.gzcelestial.com/prod-api/app/guest/check-demo-project/项目ID"
```

### 3. 使用Postman测试

1. 创建新请求
2. 方法：GET
3. URL：`https://hsdlp.gzcelestial.com/prod-api/app/guest/demo-projects`
4. 发送请求
5. 查看响应

---

## 联系支持

如果以上步骤都无法解决问题，请提供以下信息：

1. 后端日志（最近100行）
2. curl测试结果
3. 字典配置截图
4. 前端错误信息

---

**文档版本：** 1.0  
**创建时间：** 2026-02-13  
**维护人员：** Kiro AI Assistant
