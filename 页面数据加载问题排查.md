# 页面数据加载问题排查

## 问题现象
施工日志页面没有显示远程数据或模拟数据。

## 可能原因分析

### 1. 环境变量问题
- **问题**: `process.env.NODE_ENV` 可能不是 'development'
- **影响**: 导致 `USE_MOCK` 为 false，尝试调用真实API
- **解决方案**: 强制使用模拟数据进行测试

### 2. 组件渲染问题
- **问题**: `TimelineNode` 组件可能有渲染错误
- **影响**: 即使数据加载成功，也不显示
- **解决方案**: 使用简化的测试组件验证数据

### 3. 异步加载问题
- **问题**: 数据加载但页面没有响应式更新
- **影响**: 数据存在但界面不刷新
- **解决方案**: 添加调试信息确认数据状态

### 4. 用户状态问题
- **问题**: 用户未登录或项目信息缺失
- **影响**: 可能影响API调用或数据显示
- **解决方案**: 检查用户状态和项目信息

## 已实施的修复措施

### 1. 添加调试日志
```javascript
// 在页面加载时添加日志
console.log('Log page mounted')
console.log('Current project:', currentProject.value)
console.log('Environment:', process.env.NODE_ENV)

// 在API调用时添加日志
console.log('getProjectScheduleList called, USE_MOCK:', USE_MOCK)
console.log('Received data:', data)
```

### 2. 添加调试界面
```vue
<!-- 调试信息显示 -->
<view v-if="process.env.NODE_ENV === 'development'" class="debug-info">
  <text>Loading: {{ loading }}</text><br>
  <text>Schedules count: {{ schedules.length }}</text><br>
  <text>Current project: {{ currentProject?.name || 'None' }}</text>
</view>
```

### 3. 简化组件测试
```vue
<!-- 使用简单的文本显示替代复杂组件 -->
<view v-for="(schedule, index) in schedules" :key="schedule.id" class="test-item">
  <text>{{ schedule.stageName }} - {{ schedule.status }}</text>
</view>
```

### 4. 强制使用模拟数据
```javascript
// 临时强制使用模拟数据
export const getProjectScheduleList = () => {
  console.log('Force using mock data for testing')
  return getMockProjectScheduleList()
}
```

## 排查步骤

### 步骤1: 检查控制台日志
1. 打开浏览器开发者工具
2. 查看 Console 标签
3. 确认是否有以下日志：
   - "Log page mounted"
   - "getProjectScheduleList called"
   - "getMockProjectScheduleList called"
   - "Received data: [...]"

### 步骤2: 检查调试信息
1. 查看页面右上角的调试信息框
2. 确认以下状态：
   - Loading 状态是否正确变化
   - Schedules count 是否大于0
   - Current project 是否有值

### 步骤3: 检查网络请求
1. 打开 Network 标签
2. 刷新页面
3. 查看是否有API请求
4. 检查请求状态和响应

### 步骤4: 检查数据结构
1. 在控制台中输入 `schedules.value`
2. 检查数据结构是否正确
3. 确认必要字段是否存在

## 预期结果

修复后应该看到：
1. 控制台有完整的调试日志
2. 页面显示5个施工阶段的简化卡片
3. 调试信息显示正确的数据计数
4. 没有JavaScript错误

## 后续优化

数据显示正常后：
1. 恢复使用 `TimelineNode` 组件
2. 移除调试日志和调试界面
3. 恢复正常的环境判断逻辑
4. 测试真实API连接

## 常见问题

### Q: 页面显示"暂无施工进度"
**A**: 检查 `schedules.length` 是否为0，确认数据是否正确加载

### Q: 控制台没有日志输出
**A**: 检查浏览器控制台是否正确打开，或者日志级别设置

### Q: 调试信息不显示
**A**: 确认 `process.env.NODE_ENV` 是否为 'development'

### Q: 数据加载但不显示
**A**: 检查Vue响应式系统，确认数据绑定是否正确