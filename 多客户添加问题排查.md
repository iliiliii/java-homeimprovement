# 多客户添加问题排查

**问题**: 新增项目时选择3个客户，但编辑时只显示1个客户

---

## 🔍 问题分析

### 数据流程

**1. 前端提交**:
```json
{
  "customerIds": ["客户1", "客户2", "客户3"],
  "customerId": "客户1",  // 主客户
  "primaryCustomerId": "客户1"
}
```

**2. 后端 `insertProjects`**:
```java
// 创建项目
projectsMapper.insertProjects(projects)

// 同步创建主客户关联
if (projects.getCustomerId() != null) {
    syncProjectCustomer(projects.getId(), projects.getCustomerId(), "insert")
}
// ✅ 结果：创建了1个客户关联（主客户）
```

**3. 前端 `addCustomersToProject`**:
```javascript
addCustomersToProject({
  projectId: projectId,
  customerIds: ["客户1", "客户2", "客户3"],
  primaryCustomerId: "客户1"
})
// ❓ 这一步是否成功？
```

---

## 🎯 可能的原因

### 原因 1: `addCustomersToProject` 调用失败

**检查点**:
1. 前端是否正确调用了接口？
2. 后端接口是否返回错误？
3. 是否有权限问题？

### 原因 2: 后端逻辑问题

**检查 `addProjectCustomers` 方法**:
```java
public int addProjectCustomers(String projectId, List<String> customerIds, String primaryCustomerId) {
    // 检查是否已存在
    boolean exists = projectCustomersMapper.checkCustomerInProject(projectId, customerId);
    if (exists) {
        log.warn("客户{}已关联到项目{}，跳过", customerId, projectId);
        continue;  // ← 问题：主客户已存在，会跳过
    }
}
```

**问题**：
- 后端 `insertProjects` 已经创建了主客户关联
- 前端调用 `addCustomersToProject` 时，主客户已存在
- 后端会跳过主客户，只添加其他客户
- 但是如果所有客户都包含主客户，可能导致问题

### 原因 3: 前端获取 projectId 失败

**检查返回数据**:
```json
{
  "code": 200,
  "data": {
    "id": "eaa5a57433964f3ea308624e0ef857f8",
    ...
  }
}
```

**前端代码**:
```javascript
const projectId = response.data?.id || response.data
```

应该能正确获取到 `id`。

---

## ✅ 解决方案

### 方案 1: 修改后端逻辑（推荐）

**问题**: `insertProjects` 和 `addCustomersToProject` 都会创建主客户，导致重复

**解决**: 修改 `insertProjects`，不要自动创建客户关联，让前端统一调用 `addCustomersToProject`

**修改 `ProjectsServiceImpl.java`**:
```java
@Override
@Transactional(rollbackFor = Exception.class)
public int insertProjects(Projects projects) {
    if (projects.getId() == null || projects.getId().isEmpty()) {
        projects.setId(IdUtils.fastSimpleUUID());
    }
    projects.setCreatedAt(DateUtils.getNowDate());
    projects.setCreatedBy(SecurityUtils.getUsername());
    
    // 插入项目
    int result = projectsMapper.insertProjects(projects);
    
    // ❌ 注释掉自动同步，让前端统一处理
    // if (result > 0 && projects.getCustomerId() != null) {
    //     syncProjectCustomer(projects.getId(), projects.getCustomerId(), "insert");
    // }
    
    return result;
}
```

**优点**:
- 统一由前端控制客户关联
- 避免重复创建
- 逻辑更清晰

**缺点**:
- 如果前端调用失败，项目没有客户关联
- 需要确保前端一定会调用 `addCustomersToProject`

---

### 方案 2: 修改 `addProjectCustomers` 逻辑

**问题**: 主客户已存在时会跳过

**解决**: 如果客户已存在且是主客户，更新 `is_primary` 标记

**修改 `ProjectCustomersServiceImpl.java`**:
```java
for (String customerId : customerIds) {
    // 检查是否已存在
    boolean exists = projectCustomersMapper.checkCustomerInProject(projectId, customerId);
    if (exists) {
        // 如果是主客户，更新 is_primary 标记
        if (customerId.equals(primaryCustomerId)) {
            projectCustomersMapper.setPrimaryCustomer(projectId, customerId);
            log.info("客户{}已存在，更新为主客户", customerId);
        } else {
            log.warn("客户{}已关联到项目{}，跳过", customerId, projectId);
        }
        continue;
    }
    
    // 创建新关联
    // ...
}
```

---

### 方案 3: 前端添加错误处理

**当前代码**:
```javascript
addCustomersToProject(customerData).then(() => {
  proxy.$modal.msgSuccess("新增成功")
}).catch(err => {
  proxy.$modal.msgWarning("项目创建成功，但客户关联失败：" + err.message)
})
```

**问题**: 如果失败，用户看到警告但不知道具体原因

**改进**: 添加详细日志
```javascript
addCustomersToProject(customerData).then(res => {
  console.log('客户关联添加成功，返回:', res)
  proxy.$modal.msgSuccess("新增成功")
}).catch(err => {
  console.error('客户关联添加失败，错误:', err)
  console.error('请求数据:', customerData)
  proxy.$modal.msgWarning("项目创建成功，但客户关联失败：" + err.message)
})
```

---

## 🧪 测试步骤

### 1. 打开浏览器控制台

按 F12 打开开发者工具，切换到 Console 标签

### 2. 新增项目

1. 填写项目信息
2. 选择 3 个客户
3. 点击"确定"

### 3. 查看控制台日志

应该看到以下日志：
```
项目创建成功，返回数据: {...}
提取的项目ID: eaa5a57433964f3ea308624e0ef857f8
客户ID列表: ["客户1", "客户2", "客户3"]
主客户ID: 客户1
准备添加客户到项目，请求数据: {...}
客户关联添加成功
```

### 4. 检查是否有错误

如果看到错误日志：
```
客户关联添加失败: xxx
```

记录错误信息，用于排查问题。

### 5. 查询数据库

```sql
-- 查询项目
SELECT * FROM projects WHERE id = 'eaa5a57433964f3ea308624e0ef857f8';

-- 查询客户关联
SELECT * FROM project_customers WHERE project_id = 'eaa5a57433964f3ea308624e0ef857f8' AND deleted_at IS NULL;
```

应该看到 3 条记录。

### 6. 编辑项目

1. 点击"编辑"按钮
2. 查看控制台日志
3. 确认客户选择框显示 3 个客户

---

## 📊 预期结果

### 数据库

```sql
SELECT * FROM project_customers WHERE project_id = 'xxx';
```

| id | project_id | customer_id | is_primary |
|----|------------|-------------|------------|
| 1  | xxx        | 客户1       | 1          |
| 2  | xxx        | 客户2       | 0          |
| 3  | xxx        | 客户3       | 0          |

### 前端显示

编辑时客户选择框应该显示：
- ☑ 客户1
- ☑ 客户2
- ☑ 客户3

主客户下拉框应该选中：客户1

---

## 🎯 下一步

1. 先添加详细日志，确认问题出在哪里
2. 根据日志选择合适的解决方案
3. 修复后重新测试

---

**创建时间**: 2026-03-01  
**状态**: 待测试
