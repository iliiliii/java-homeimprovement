# 数据迁移执行指南

**创建时间**: 2026-03-01  
**目的**: 将 projects.customer_id 的历史数据迁移到 project_customers 表

---

## 📋 执行前检查清单

### 1. 环境准备
- [ ] 确认在测试环境执行（不要直接在生产环境）
- [ ] 确认数据库连接信息
- [ ] 确认有足够的磁盘空间（用于备份）
- [ ] 确认有数据库操作权限

### 2. 代码部署
- [ ] 确认已部署包含同步逻辑的代码
  - ProjectsServiceImpl.java
  - ProjectCustomersServiceImpl.java
- [ ] 确认应用已重启
- [ ] 确认应用运行正常

---

## 🚀 执行步骤

### 步骤 1: 备份数据库

```bash
# 备份整个数据库
mysqldump -u root -p your_database > backup_$(date +%Y%m%d_%H%M%S).sql

# 或者只备份相关表
mysqldump -u root -p your_database projects project_customers > backup_tables_$(date +%Y%m%d_%H%M%S).sql
```

**验证备份**:
```bash
# 检查备份文件大小
ls -lh backup_*.sql

# 查看备份文件前几行
head -n 20 backup_*.sql
```

---

### 步骤 2: 执行迁移 SQL

```bash
# 执行迁移脚本
mysql -u root -p your_database < 数据迁移-简化版.sql
```

**注意**:
- 执行过程中会输出统计信息
- 请仔细查看输出结果
- 如果有错误，立即停止并检查

---

### 步骤 3: 验证迁移结果

#### 3.1 查看迁移统计

SQL 会输出以下统计信息：

```
+--------------------------------------------------+-------+
| description                                       | count |
+--------------------------------------------------+-------+
| 迁移前 projects 表中有 customer_id 的记录数      | XXX   |
| 迁移后 project_customers 表中的记录数            | XXX   |
| 未迁移的记录数（应该为0）                        | 0     |
+--------------------------------------------------+-------+
```

**检查点**:
- [ ] "未迁移的记录数" 应该为 0
- [ ] 前两个数字应该相等

---

#### 3.2 数据一致性检查

SQL 会自动运行 4 个一致性检查：

**检查 1: 缺少 project_customers 记录的项目**
```
应该返回 0 行
```

**检查 2: 主客户不一致**
```
应该返回 0 行
```

**检查 3: 没有主客户的项目**
```
应该返回 0 行
```

**检查 4: 有多个主客户的项目**
```
应该返回 0 行
```

**如果有问题**:
- 记录问题项目的 ID
- 手动修复数据
- 重新运行检查

---

#### 3.3 手动验证（可选）

```sql
-- 1. 检查迁移数量
SELECT COUNT(*) FROM projects WHERE customer_id IS NOT NULL AND deleted_at IS NULL;
SELECT COUNT(*) FROM project_customers WHERE deleted_at IS NULL;

-- 2. 随机抽查几个项目
SELECT 
    p.id,
    p.name,
    p.customer_id AS projects_customer_id,
    pc.customer_id AS pc_customer_id,
    pc.is_primary
FROM projects p
LEFT JOIN project_customers pc ON p.id = pc.project_id AND pc.deleted_at IS NULL
WHERE p.deleted_at IS NULL
LIMIT 10;

-- 3. 检查是否有项目没有主客户
SELECT 
    p.id,
    p.name,
    p.customer_id
FROM projects p
WHERE 
    p.deleted_at IS NULL
    AND p.customer_id IS NOT NULL
    AND NOT EXISTS (
        SELECT 1 
        FROM project_customers pc 
        WHERE pc.project_id = p.id 
          AND pc.is_primary = 1
          AND pc.deleted_at IS NULL
    );
```

---

### 步骤 4: 测试应用功能

#### 4.1 测试客户登录
```
1. 使用客户账号登录
2. 检查是否能看到关联的项目
3. 检查项目列表是否正确
```

#### 4.2 测试项目访问
```
1. 访问项目详情
2. 检查权限是否正常
3. 检查数据是否完整
```

#### 4.3 测试创建项目
```
1. 创建新项目（设置 customer_id）
2. 检查 project_customers 表是否自动创建记录
3. 检查 is_primary 是否为 1
```

#### 4.4 测试修改项目
```
1. 修改项目的 customer_id
2. 检查旧的 project_customers 记录是否软删除
3. 检查新的 project_customers 记录是否创建
```

---

### 步骤 5: 监控应用日志

**查看同步日志**:
```bash
# 查看应用日志
tail -f /path/to/application.log | grep "同步"
```

**关键日志**:
```
同步创建 project_customers 记录：projectId=xxx, customerId=xxx
同步软删除旧的 project_customers 记录：id=xxx
同步更新 projects.customer_id 成功
```

**错误日志**:
```
同步失败：projectId=xxx, error=xxx
```

**如果看到错误日志**:
- 记录错误信息
- 检查数据库连接
- 检查数据完整性
- 运行一致性检查

---

## ⚠️ 常见问题

### 问题 1: 迁移后发现未迁移的记录

**原因**:
- 数据已存在（重复执行）
- 唯一约束冲突

**解决**:
```sql
-- 查看具体是哪些记录
SELECT 
    p.id,
    p.name,
    p.customer_id
FROM projects p
WHERE 
    p.customer_id IS NOT NULL 
    AND p.deleted_at IS NULL
    AND NOT EXISTS (
        SELECT 1 
        FROM project_customers pc 
        WHERE pc.project_id = p.id 
          AND pc.customer_id = p.customer_id
          AND pc.deleted_at IS NULL
    );

-- 手动迁移
INSERT INTO project_customers (id, project_id, customer_id, role, is_primary, created_at)
VALUES (REPLACE(UUID(), '-', ''), 'project_id', 'customer_id', 'OWNER', 1, NOW());
```

---

### 问题 2: 主客户不一致

**原因**:
- 数据在迁移后被修改
- 同步逻辑未生效

**解决**:
```sql
-- 查看不一致的记录
SELECT 
    p.id,
    p.customer_id AS projects_customer_id,
    pc.customer_id AS pc_customer_id
FROM projects p
INNER JOIN project_customers pc ON p.id = pc.project_id
WHERE 
    p.deleted_at IS NULL
    AND pc.deleted_at IS NULL
    AND pc.is_primary = 1
    AND p.customer_id != pc.customer_id;

-- 手动修复（以 projects 为准）
UPDATE project_customers pc
INNER JOIN projects p ON pc.project_id = p.id
SET pc.customer_id = p.customer_id
WHERE 
    pc.is_primary = 1
    AND pc.deleted_at IS NULL
    AND p.deleted_at IS NULL
    AND pc.customer_id != p.customer_id;
```

---

### 问题 3: 有多个主客户

**原因**:
- 数据异常
- 并发操作

**解决**:
```sql
-- 查看有多个主客户的项目
SELECT 
    project_id,
    COUNT(*) AS primary_count
FROM project_customers
WHERE 
    is_primary = 1
    AND deleted_at IS NULL
GROUP BY project_id
HAVING COUNT(*) > 1;

-- 手动修复（保留第一个，其他设为非主客户）
-- 需要根据具体情况处理
```

---

## 🔄 回滚方案

### 如果迁移失败需要回滚

```bash
# 1. 停止应用
systemctl stop your-application

# 2. 恢复数据库
mysql -u root -p your_database < backup_YYYYMMDD_HHMMSS.sql

# 3. 验证恢复
mysql -u root -p your_database -e "SELECT COUNT(*) FROM projects; SELECT COUNT(*) FROM project_customers;"

# 4. 重新部署旧版本代码（如果需要）

# 5. 启动应用
systemctl start your-application
```

---

## ✅ 迁移完成检查清单

### 数据检查
- [ ] 迁移统计信息正确
- [ ] 未迁移记录数为 0
- [ ] 数据一致性检查全部通过
- [ ] 手动抽查数据正确

### 功能测试
- [ ] 客户登录正常
- [ ] 项目访问正常
- [ ] 创建项目正常
- [ ] 修改项目正常
- [ ] 权限检查正常

### 日志监控
- [ ] 应用日志无错误
- [ ] 同步日志正常
- [ ] 无"同步失败"日志

### 文档更新
- [ ] 记录迁移时间
- [ ] 记录迁移结果
- [ ] 更新运维文档

---

## 📞 遇到问题怎么办

### 1. 立即停止
- 停止应用
- 停止数据库操作

### 2. 保留现场
- 导出错误日志
- 导出数据库状态
- 记录错误信息

### 3. 分析问题
- 查看应用日志
- 查看数据库日志
- 运行一致性检查

### 4. 决定方案
- 如果可以修复：手动修复数据
- 如果无法修复：执行回滚方案

---

## 📋 迁移记录模板

```
迁移时间: YYYY-MM-DD HH:MM:SS
执行人: [姓名]
环境: [测试/生产]

迁移前统计:
- projects 表记录数: XXX
- project_customers 表记录数: XXX

迁移结果:
- 迁移记录数: XXX
- 未迁移记录数: XXX
- 数据一致性检查: [通过/失败]

功能测试:
- 客户登录: [通过/失败]
- 项目访问: [通过/失败]
- 创建项目: [通过/失败]
- 修改项目: [通过/失败]

问题记录:
[如果有问题，记录在这里]

结论:
[迁移成功/失败]
```

---

**创建时间**: 2026-03-01  
**最后更新**: 2026-03-01  
**状态**: ✅ 准备就绪
