# 品牌页面设计团队排序优化说明

## 优化内容

1. 优化了小程序品牌页面（`pages/brand/index`）的设计团队成员排序逻辑，现在按照岗位的 `post_sort` 字段进行排序
2. 修复了团队成员头像无法显示的问题，添加了完整URL路径处理

## 修改文件

### 后端修改

**文件**: `sb3/evs-home/src/main/java/com/ruoyi/app/mapper/AppUserMapper.java`

**修改内容**:
- 在 `selectTeamMembersWithPost()` 方法的 SQL 查询中添加了 `MIN(p.post_sort) AS minPostSort` 字段
- 修改排序规则从 `ORDER BY u.create_time ASC` 改为 `ORDER BY minPostSort ASC, u.create_time ASC`

**优化逻辑**:
1. 使用 `MIN(p.post_sort)` 获取每个用户的最小岗位排序值
2. 优先按照岗位排序值（`minPostSort`）升序排列
3. 当岗位排序值相同时，按照用户创建时间升序排列

### 前端修改

**文件**: `uni3/src/pages/brand/index.vue`

**修改内容**:
1. 导入 `BASE_URL` 从 `@/utils/request`
2. 添加 `getFullUrl()` 函数处理图片路径
3. 修改头像显示为 `:src="getFullUrl(member.avatar)"`

**getFullUrl 函数逻辑**:
```javascript
const getFullUrl = (path) => {
  if (!path) return ''
  if (path.startsWith('http://') || path.startsWith('https://')) {
    return path
  }
  // 拼接基础URL
  if (path.startsWith('/')) {
    return BASE_URL + path
  }
  return BASE_URL + '/' + path
}
```

## 排序规则说明

根据数据库中的岗位配置（`sys_post` 表）：

| 岗位名称 | post_sort | 显示顺序 |
|---------|-----------|---------|
| 设计总监 | 1 | 第1位 |
| 设计助理 | 2 | 第2位 |
| 项目总监 | 3 | 第3位 |
| 项目助理 | 4 | 第4位 |
| 工程监理 | 5 | 第5位 |

## API 接口

**接口地址**: `GET /app/team/members`

**返回数据格式**:
```json
[
  {
    "name": "张三",
    "post": "设计总监",
    "avatar": "/profile/avatar/2024/12/18/xxx.jpg"
  },
  {
    "name": "李四",
    "post": "设计助理",
    "avatar": "/profile/avatar/2024/12/18/yyy.jpg"
  }
]
```

## 头像路径处理

- 后端返回的头像路径为相对路径（如：`/profile/avatar/2024/12/18/xxx.jpg`）
- 前端使用 `getFullUrl()` 函数自动拼接完整URL
- 支持相对路径和绝对路径（http/https）的自动识别

## 测试建议

1. 确保数据库中 `sys_post` 表的 `post_sort` 字段值正确设置
2. 验证团队成员在品牌页面按照岗位顺序正确显示
3. 测试一个用户拥有多个岗位时，按照最小的 `post_sort` 值排序
4. 验证团队成员头像能够正常显示

## 注意事项

- 如果用户没有关联岗位，`minPostSort` 将为 NULL，这些用户会排在最后
- 如果需要调整岗位显示顺序，只需在后台管理系统中修改 `sys_post` 表的 `post_sort` 字段值即可
- 头像路径处理参照了设计页面（`pages/design/index`）的实现方式
