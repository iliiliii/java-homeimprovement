# 客户显示优化说明

## 修改内容

### 1. 标签名称修改
- **修改前**: "关联客户"
- **修改后**: "客户"

### 2. 显示方式修改
- **修改前**: 显示为可点击的超链接，点击跳转到客户管理页面
- **修改后**: 只显示客户名称文本，不可点击

### 3. 空值显示
- **修改前**: 显示"未关联客户"
- **修改后**: 显示"-"

### 4. 客户ID显示
- **修改前**: 如果没有客户名称，显示"客户ID: xxx"
- **修改后**: 只显示客户名称，没有名称时显示"-"

## 代码修改

### 模板部分
```vue
<!-- 修改前 -->
<el-descriptions-item label="关联客户">
  <el-link
    v-if="getCustomerName(currentProject.customerId)"
    type="primary"
    :underline="false"
    @click="goToCustomer(currentProject.customerId)"
  >
    {{ getCustomerName(currentProject.customerId) }}
  </el-link>
  <span v-else style="color: #999;">未关联客户</span>
</el-descriptions-item>

<!-- 修改后 -->
<el-descriptions-item label="客户">
  <span v-if="getCustomerName(currentProject.customerId)">
    {{ getCustomerName(currentProject.customerId) }}
  </span>
  <span v-else style="color: #999;">-</span>
</el-descriptions-item>
```

### 脚本部分
```javascript
// 修改前
import { useRouter } from 'vue-router'
const router = useRouter()

function getCustomerName(customerId) {
  if (currentProject.value.customer && currentProject.value.customer.name) {
    return currentProject.value.customer.name
  }
  if (customerId) {
    return `客户ID: ${customerId}`
  }
  return '未关联客户'
}

function goToCustomer(customerId) {
  if (customerId) {
    router.push({
      path: '/evs/customers',
      query: { id: customerId }
    })
  }
}

// 修改后
// 移除 useRouter 导入和 router 变量

function getCustomerName(customerId) {
  if (currentProject.value.customer && currentProject.value.customer.name) {
    return currentProject.value.customer.name
  }
  return ''
}

// 移除 goToCustomer 函数
```

## 修改文件

- ✅ `vue3/src/views/evs/projects/components/ProjectDetail.vue`

## 效果

### 有客户信息时
```
客户: 张三
```

### 无客户信息时
```
客户: -
```

## 优点

1. **简洁明了** - 直接显示客户名称，不需要额外的交互
2. **代码简化** - 移除了不必要的路由跳转逻辑
3. **一致性** - 与其他字段的显示方式保持一致
4. **性能优化** - 减少了不必要的组件和事件处理

## 修改日期

2025年12月5日
