# 字段名修复完成说明

## 最新修复内容

### 问题分析
API调用失败的原因是字段名不匹配：
- **错误**: SQL中使用了 `psr.create_time`
- **正确**: 实际数据库字段名是 `psr.created_at`

### 已修复的字段名
1. **时间字段**: `create_time` → `created_at` (使用别名映射)
2. **创建人字段**: `create_by` → `created_by` (使用别名映射)
3. **用户信息**: 简化了用户信息查询，使用 `acceptor` 字段作为创建人姓名

### 修复的SQL查询
- `selectLatestRecordsByScheduleId` - 查询最新验收记录
- `selectProjectScheduleRecordList` - 查询验收记录列表
- `selectProjectScheduleRecordById` - 查询验收记录详情

### 字段映射关系
```sql
-- 实际数据库字段 → VO对象属性
psr.created_at as create_time     -- 创建时间
psr.created_by as create_by       -- 创建人ID
psr.acceptor as create_by_name    -- 创建人姓名
'staff' as create_by_role         -- 创建人角色（固定值）
```

## 测试验证

修复完成后，请：
1. 重新编译项目
2. 重启后端服务
3. 执行 `insert_test_data_correct_tables.sql` 插入测试数据
4. 测试API调用：`/app/projectSchedules/list`

## 预期结果
- API能正常返回项目进度列表
- 前端页面能正确显示施工进度时间轴
- 验收记录能正确显示标题、内容和时间信息

## 注意事项
- 用户信息查询已简化，避免复杂的表关联
- 使用 `acceptor` 字段作为验收人姓名显示
- 创建人角色统一显示为 'staff'