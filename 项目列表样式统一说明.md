# 项目列表样式统一说明

## 调整目标

将质量检测功能中左侧"进行中的项目"列表的样式，调整为与进度跟踪功能保持一致。

---

## 样式对比

### 调整前（质量检测）

```scss
.project-list {
  flex: 1;
  min-height: 0;
  position: relative;
  padding: 16px;  // ❌ 多余的外层padding
}

.project-item {
  padding: 16px;
  margin-bottom: 12px;
  // ...
}

.project-address {
  margin-bottom: 8px;  // ❌ 间距较小
}
```

**问题**:
1. `.project-list` 有16px的padding，导致项目卡片与容器边缘有额外间距
2. `.project-address` 的 `margin-bottom` 只有8px，视觉上较紧凑

### 调整后（质量检测）

```scss
.project-list {
  flex: 1;
  min-height: 0;
  position: relative;
  // ✅ 移除padding
}

.project-item {
  padding: 16px;
  margin-bottom: 12px;
  // ...
}

.project-address {
  margin-bottom: 12px;  // ✅ 增加间距
}
```

### 参考标准（进度跟踪）

```scss
.project-list {
  flex: 1;
  min-height: 0;
  position: relative;
  // 无padding
}

.project-item {
  padding: 16px;
  margin-bottom: 12px;
  // ...
}

.project-address {
  margin-bottom: 12px;
}
```

---

## 具体修改

### 1. 移除 `.project-list` 的 padding

**修改前**:
```scss
.project-list {
  padding: 16px;
}
```

**修改后**:
```scss
.project-list {
  // 无padding
}
```

**效果**: 项目卡片直接贴近容器边缘，与进度跟踪一致。

### 2. 调整 `.project-address` 的 margin-bottom

**修改前**:
```scss
.project-address {
  margin-bottom: 8px;
}
```

**修改后**:
```scss
.project-address {
  margin-bottom: 12px;
}
```

**效果**: 地址信息与下方内容的间距增加，视觉上更舒适。

### 3. 移除不必要的伪类选择器

**修改前**:
```scss
.project-item {
  &:first-child {
    margin-top: 0;
  }

  &:last-child {
    margin-bottom: 0;
  }
}
```

**修改后**:
```scss
.project-item {
  // 移除伪类选择器
}
```

**原因**: 进度跟踪没有这些选择器，保持一致性。

---

## 统一后的样式规范

### 项目列表容器
```scss
.project-list-card {
  flex: 1;
  display: flex;
  flex-direction: column;
  min-height: 0;
  height: 100%;
  overflow: hidden;

  :deep(.el-card__header) {
    flex-shrink: 0;
    padding: 16px;
  }

  :deep(.el-card__body) {
    flex: 1;
    display: flex;
    flex-direction: column;
    min-height: 0;
    padding: 0;  // 无padding
    overflow-y: auto;
    overflow-x: hidden;
  }
}
```

### 项目列表
```scss
.project-list {
  flex: 1;
  min-height: 0;
  position: relative;
  // 无padding
}
```

### 项目卡片
```scss
.project-item {
  padding: 16px;           // 内边距
  margin-bottom: 12px;     // 卡片间距
  border: 1px solid #e8e8e8;
  border-radius: 8px;
  cursor: pointer;
  transition: all 0.3s;
  background: #fff;

  &:hover {
    border-color: #1677ff;
    box-shadow: 0 2px 8px rgba(22, 119, 255, 0.1);
  }

  &.active {
    border-color: #1677ff;
    background: #f0f5ff;
    box-shadow: 0 2px 8px rgba(22, 119, 255, 0.2);
  }
}
```

### 项目信息
```scss
.project-item-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 8px;      // 标题与地址间距

  .project-name {
    font-size: 16px;
    font-weight: 600;
    color: #303133;
    flex: 1;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
}

.project-address {
  display: flex;
  align-items: center;
  gap: 4px;
  font-size: 13px;
  color: #666;
  margin-bottom: 12px;     // 地址与底部内容间距
}
```

---

## 视觉效果对比

### 调整前
```
┌─────────────────────────────────┐
│ 进行中的项目 [5]                 │
├─────────────────────────────────┤
│ ┌─────────────────────────────┐ │  ← 16px padding
│ │ 项目A                        │ │
│ │ 📍 地址A                     │ │  ← 8px margin-bottom
│ │ [点击查看详情]               │ │
│ └─────────────────────────────┘ │
│                                  │
│ ┌─────────────────────────────┐ │
│ │ 项目B                        │ │
│ └─────────────────────────────┘ │
└─────────────────────────────────┘
```

### 调整后
```
┌─────────────────────────────────┐
│ 进行中的项目 [5]                 │
├─────────────────────────────────┤
│┌─────────────────────────────┐  │  ← 无padding，贴边
││ 项目A                        │  │
││ 📍 地址A                     │  │  ← 12px margin-bottom
││ [点击查看详情]               │  │
│└─────────────────────────────┘  │
│                                  │
│┌─────────────────────────────┐  │
││ 项目B                        │  │
│└─────────────────────────────┘  │
└─────────────────────────────────┘
```

---

## 间距规范总结

| 元素 | 属性 | 值 | 说明 |
|-----|------|-----|------|
| `.project-list` | padding | 0 | 无外边距 |
| `.project-item` | padding | 16px | 卡片内边距 |
| `.project-item` | margin-bottom | 12px | 卡片间距 |
| `.project-item-header` | margin-bottom | 8px | 标题与地址间距 |
| `.project-address` | margin-bottom | 12px | 地址与底部内容间距 |

---

## 文件清单

### 修改文件
- ✅ `vue3/src/views/evs/qualityInspections/card.vue` - 调整项目列表样式

### 参考文件
- 📖 `vue3/src/views/evs/projectScheduleRecords/components/ProjectScheduleList.vue` - 进度跟踪项目列表（参考标准）

---

## 测试要点

### 视觉测试
1. ✅ 项目卡片与容器边缘无额外间距
2. ✅ 项目卡片之间的间距为12px
3. ✅ 地址信息与下方内容的间距为12px
4. ✅ 与进度跟踪的项目列表视觉效果一致

### 交互测试
1. ✅ 鼠标悬停效果正常
2. ✅ 选中状态样式正常
3. ✅ 滚动条样式正常
4. ✅ 响应式布局正常

---

## 总结

通过移除 `.project-list` 的padding和调整 `.project-address` 的margin-bottom，质量检测功能的项目列表样式现在与进度跟踪功能完全一致，提供了统一的用户体验。

**核心改进**:
1. ✅ 移除了多余的16px外层padding
2. ✅ 统一了地址信息的间距为12px
3. ✅ 保持了与进度跟踪一致的视觉效果
4. ✅ 提升了界面的整体一致性
