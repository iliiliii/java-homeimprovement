# 未登录访问演示项目实施说明

## 一、修改概述

在原有游客登录访问演示项目的基础上，进一步支持未登录用户直接访问演示项目的设计和日志页面。

### 核心改动

1. **后端API** - 移除设计和日志相关API的token必需验证
2. **后端Service** - 支持无token时访问演示项目
3. **前端配置** - 未登录用户也被视为游客
4. **前端请求** - 未登录时不添加token但仍然发送请求

---

## 二、后端修改详情

### 2.1 Controller层修改

#### AppDashboardController.java

**修改方法：** `getProjectRooms`

**修改前：**
```java
if (token == null || token.isEmpty()) {
    return AjaxResult.error(401, "未提供认证Token");
}
```

**修改后：**
```java
// 移除token必需验证，允许未登录访问
// token会传递给Service层进行权限验证
```

#### AppProjectScheduleController.java

**修改方法：** 
- `getProjectScheduleList` - 获取项目进度列表
- `getProjectScheduleRecordDetail` - 获取验收记录详情

**修改前：**
```java
if (token == null || token.isEmpty()) {
    log.warn("未提供认证Token");
    return AjaxResult.error(401, "未提供认证Token");
}
```

**修改后：**
```java
// 移除token必需验证
// 只验证projectId是否提供
if (projectId == null || projectId.isEmpty()) {
    log.warn("未提供项目ID");
    return AjaxResult.error(400, "未提供项目ID");
}
```

### 2.2 Service层修改

#### AppDashboardServiceImpl.java

**修改方法：** `validateTokenAndAccess`

**新增逻辑：**
```java
// 如果没有token，检查是否为演示项目（允许未登录访问演示项目）
if (token == null || token.isEmpty() || "null".equals(token)) {
    boolean isDemo = guestConfigService.isGuestDemoProject(projectId);
    if (!isDemo) {
        log.warn("[权限验证] 未登录用户尝试访问非演示项目: {}", projectId);
        throw new ServiceException("未登录用户只能访问演示项目");
    }
    log.info("[权限验证] 未登录用户访问演示项目: {}", projectId);
    return;
}
```

**验证流程：**
1. 检查token是否为空
2. 如果为空，调用 `guestConfigService.isGuestDemoProject(projectId)`
3. 检查projectId是否在演示项目列表中
4. 如果是演示项目，允许访问
5. 如果不是演示项目，抛出异常

#### AppProjectScheduleServiceImpl.java

与 `AppDashboardServiceImpl` 相同的修改逻辑。

### 2.3 权限验证层级

```
未登录用户访问
  ↓
Controller: 不验证token，直接传递给Service
  ↓
Service: validateTokenAndAccess()
  ↓
检查token是否为空
  ↓
如果为空，检查是否为演示项目
  ↓
是演示项目 → 允许访问
不是演示项目 → 拒绝访问
```

---

## 三、前端修改详情

### 3.1 配置模块修改 (guest.js)

#### isGuestUser() 函数

**修改前：**
```javascript
export const isGuestUser = () => {
  const guestMode = uni.getStorageSync('guestMode') === true
  const userType = uni.getStorageSync('userType')
  const isGuestType = userType === 'guest'
  return guestMode || isGuestType
}
```

**修改后：**
```javascript
export const isGuestUser = () => {
  // 检查是否已登录
  const token = uni.getStorageSync('token')
  const isLoggedIn = !!(token && token !== '' && token !== 'null')
  
  // 未登录用户视为游客
  if (!isLoggedIn) {
    return true
  }
  
  // 检查是否是游客模式或游客用户
  const guestMode = uni.getStorageSync('guestMode') === true
  const userType = uni.getStorageSync('userType')
  const isGuestType = userType === 'guest'
  
  return guestMode || isGuestType
}
```

**关键改动：** 未登录用户（无token或token为空）直接返回 `true`，视为游客。

### 3.2 请求拦截器修改 (request.js)

#### requestInterceptor() 函数

**修改前：**
```javascript
const token = uni.getStorageSync('token')
if (token) {
  config.header = {
    ...config.header,
    'Authorization': `Bearer ${token}`
  }
}
```

**修改后：**
```javascript
const token = uni.getStorageSync('token')
if (token && token !== '' && token !== 'null') {
  config.header = {
    ...config.header,
    'Authorization': `Bearer ${token}`
  }
} else {
  // 未登录时，不添加Authorization头，但仍然允许请求
  console.log('[Request] 未登录用户，不添加Authorization头')
}
```

**关键改动：** 未登录时不添加 `Authorization` 头，但仍然发送请求。

#### redirectToLogin() 函数

**修改前：**
```javascript
const isGuest = uni.getStorageSync('guestMode') === true

if (isGuest) {
  console.log('[Request] 游客模式，API调用失败但不跳转登录页')
  return
}
```

**修改后：**
```javascript
const isGuest = uni.getStorageSync('guestMode') === true
const token = uni.getStorageSync('token')
const isLoggedIn = !!(token && token !== '' && token !== 'null')

if (isGuest || !isLoggedIn) {
  console.log('[Request] 游客/未登录模式，API调用失败但不跳转登录页')
  return
}
```

**关键改动：** 未登录用户也不会被重定向到登录页。

### 3.3 页面加载逻辑修改

#### design/index.vue 和 log/index.vue

**修改：** 在 `onLoad` 生命周期中添加 `initProjectId()` 调用

```javascript
onLoad((options) => {
  if (options.projectId) {
    urlProjectId.value = options.projectId
    if (userStore.isStaff) {
      userStore.switchProject(options.projectId)
    }
  }
  // 未登录用户也需要初始化项目ID
  initProjectId()
})
```

**关键改动：** 确保未登录用户在页面加载时也能初始化项目ID。

---

## 四、数据流程

### 4.1 未登录用户访问流程

```
未登录用户打开小程序
  ↓
访问设计/日志页面
  ↓
isGuestUser() 返回 true（未登录）
  ↓
调用 getCurrentProjectId()
  ↓
从服务端获取演示项目配置
  ↓
返回默认演示项目ID
  ↓
发起API请求（不带Authorization头）
  ↓
后端Controller接收请求（token为null）
  ↓
后端Service验证：token为空 + 是否为演示项目
  ↓
是演示项目 → 返回数据
不是演示项目 → 返回错误
  ↓
前端展示数据
```

### 4.2 已登录游客用户访问流程

```
游客用户登录
  ↓
访问设计/日志页面
  ↓
isGuestUser() 返回 true（userType='guest'）
  ↓
调用 getCurrentProjectId()
  ↓
返回演示项目ID
  ↓
发起API请求（带游客token）
  ↓
后端Controller接收请求
  ↓
后端Service验证：userType='guest' + 是否为演示项目
  ↓
是演示项目 → 返回数据
不是演示项目 → 返回错误
  ↓
前端展示数据
```

### 4.3 正常用户访问流程

```
正常用户登录
  ↓
访问设计/日志页面
  ↓
isGuestUser() 返回 false
  ↓
使用用户自己的项目ID
  ↓
发起API请求（带用户token）
  ↓
后端Controller接收请求
  ↓
后端Service验证：用户权限 + 项目权限
  ↓
有权限 → 返回数据
无权限 → 返回错误
  ↓
前端展示数据
```

---

## 五、安全考虑

### 5.1 数据隔离

✅ **未登录用户只能访问演示项目**
- 后端严格验证projectId是否在演示项目列表中
- 尝试访问非演示项目会被拒绝

✅ **未登录用户只能读取数据**
- 写操作（验收上报、问题上报等）仍然需要token
- Controller层对写操作保持token必需验证

✅ **正常用户不受影响**
- 已登录用户仍然使用原有的权限验证逻辑
- 不会因为未登录访问功能而降低安全性

### 5.2 日志记录

**未登录访问日志：**
```
[权限验证] 未登录用户访问演示项目: {projectId}
[权限验证] 未登录用户尝试访问非演示项目: {projectId}
```

**监控要点：**
- 未登录用户访问频率
- 未登录用户尝试访问非演示项目的次数
- 演示项目API的调用量

### 5.3 防护措施

1. **后端验证**
   - 即使前端绕过，后端仍然验证projectId
   - 只有演示项目才允许未登录访问

2. **写操作保护**
   - 所有写操作仍然需要token
   - 未登录用户无法进行任何修改

3. **限流保护**
   - 建议对演示项目API添加限流
   - 防止恶意刷接口

---

## 六、测试清单

### 6.1 未登录访问测试

- [ ] 未登录用户能访问设计页面
- [ ] 未登录用户能看到演示项目的设计图
- [ ] 未登录用户能访问日志页面
- [ ] 未登录用户能看到演示项目的施工进度
- [ ] 未登录用户能查看设计图大图
- [ ] 未登录用户能查看施工进度详情
- [ ] 未登录用户不能进行编辑操作
- [ ] 未登录用户尝试访问非演示项目被拒绝

### 6.2 已登录用户测试

- [ ] 游客用户能访问演示项目
- [ ] 正常用户能访问自己的项目
- [ ] 员工用户能访问负责的项目
- [ ] 各类用户的权限验证正常

### 6.3 安全测试

- [ ] 未登录用户无法通过修改请求访问非演示项目
- [ ] 未登录用户无法进行写操作
- [ ] 后端日志正确记录未登录访问行为
- [ ] 限流机制正常工作（如果配置）

---

## 七、部署步骤

### 7.1 后端部署

1. **编译代码**
   ```bash
   cd sb3
   mvn clean package
   ```

2. **重启服务**
   ```bash
   systemctl restart evs-home
   ```

3. **验证API**
   ```bash
   # 测试未登录访问演示项目
   curl -X GET "https://hsdlp.gzcelestial.com/prod-api/app/dashboard/project/{演示项目ID}/rooms" \
     -H "X-Project-Id: {演示项目ID}"
   ```

### 7.2 前端部署

1. **编译代码**
   ```bash
   cd uni3
   npm run build:mp-weixin
   ```

2. **上传小程序**
   - 使用微信开发者工具打开 `uni3/dist/build/mp-weixin`
   - 上传代码到微信平台
   - 提交审核

3. **测试验证**
   - 清除小程序缓存
   - 不登录直接访问设计和日志页面
   - 确认能看到演示项目数据

---

## 八、常见问题

### 8.1 未登录用户看不到数据

**可能原因：**
1. 演示项目配置未添加
2. 前端未正确获取演示项目ID
3. 后端权限验证失败

**排查步骤：**
1. 检查字典配置是否正确
2. 查看浏览器控制台，确认API请求是否发送
3. 查看后端日志，确认权限验证逻辑
4. 确认projectId是否正确传递

### 8.2 未登录用户被重定向到登录页

**可能原因：**
1. `redirectToLogin()` 函数未正确修改
2. 其他地方有登录检查逻辑

**排查步骤：**
1. 检查 `request.js` 的 `redirectToLogin()` 函数
2. 检查页面是否有其他登录检查
3. 查看浏览器控制台的日志

### 8.3 API返回401错误

**可能原因：**
1. Controller层仍然要求token
2. 中间件或拦截器拦截了请求

**排查步骤：**
1. 确认Controller方法已移除token必需验证
2. 检查是否有全局拦截器
3. 查看后端日志

---

## 九、优化建议

### 9.1 性能优化

1. **添加缓存**
   - 缓存演示项目数据
   - 减少API调用次数

2. **CDN加速**
   - 演示项目的图片使用CDN
   - 提升加载速度

3. **懒加载**
   - 图片懒加载
   - 分页加载数据

### 9.2 用户体验优化

1. **添加引导**
   - 首次访问时显示引导页
   - 说明这是演示数据

2. **添加注册入口**
   - 在演示页面添加"立即注册"按钮
   - 引导用户注册成为正式用户

3. **添加分享功能**
   - 允许分享演示项目链接
   - 提高传播效率

### 9.3 监控优化

1. **访问统计**
   - 统计未登录用户访问量
   - 分析用户行为

2. **转化率分析**
   - 统计未登录用户的注册转化率
   - 优化引导策略

3. **性能监控**
   - 监控演示项目API性能
   - 及时发现和解决问题

---

## 十、总结

### 10.1 核心改动

✅ **后端** - 移除设计和日志API的token必需验证，支持未登录访问演示项目
✅ **前端** - 未登录用户视为游客，自动使用演示项目ID
✅ **安全** - 后端严格验证，只允许访问演示项目，写操作仍需登录

### 10.2 优势

1. **降低使用门槛** - 用户无需登录即可体验
2. **提高转化率** - 先体验后注册，提高用户转化
3. **保持安全性** - 只开放演示项目，不影响其他数据安全
4. **不影响现有功能** - 已登录用户完全不受影响

### 10.3 注意事项

⚠️ **配置演示项目** - 确保字典中配置了合适的演示项目
⚠️ **监控API调用** - 防止演示项目API被过度调用
⚠️ **定期更新演示数据** - 保持演示内容的新鲜度
⚠️ **添加限流保护** - 防止恶意刷接口

---

**文档版本：** 1.0  
**创建时间：** 2026-02-13  
**基于版本：** 游客演示项目完整实施方案 v2.0  
**维护人员：** Kiro AI Assistant
