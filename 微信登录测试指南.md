# 微信登录测试指南

## 当前状态
✅ 后端服务已启动在 http://192.168.5.102:8080  
✅ 数据库表 `app_wechat_bindings` 已创建  
✅ 后端代码已更新为使用数据库存储绑定关系  
✅ 前端代码已优化，增加详细日志  
✅ 创建了专门的测试页面  

## 测试页面
1. **网络测试页面**: `/pages/test/network-test`
   - 基础网络连接测试
   - API健康检查
   - 检查绑定接口测试

2. **微信绑定测试页面**: `/pages/test/wechat-binding`
   - 完整的微信登录流程测试
   - 分步骤测试每个环节
   - 实时状态显示和日志

## 测试步骤

### 方法一：使用专门测试页面（推荐）
1. 在微信开发者工具中打开小程序项目 `uni3`
2. 确保勾选了"不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书"
3. 在地址栏输入：`/pages/test/wechat-binding`
4. 按顺序点击测试按钮：
   - **获取微信Code** → 获取微信登录凭证
   - **检查绑定状态** → 查看当前openid是否已绑定
   - **测试绑定手机号** → 绑定测试手机号（如果未绑定）
   - **测试直接登录** → 使用openid直接登录（如果已绑定）

### 方法二：使用正常登录页面
1. 导航到 `/pages/login/index-new` 页面
2. 点击"微信登录"按钮
3. 观察控制台日志和页面反应

## 测试用户数据
系统中已有测试客户：
- **张先生**: 13812345678
- **李女士**: 13987654321  
- **王总**: 13611112222
- **测试客户**: 13800138000

可以使用这些手机号测试绑定流程。

## 预期行为

### 首次使用（未绑定）
1. 获取微信Code → 成功获取登录凭证
2. 检查绑定状态 → 返回 `isBound: false`
3. 输入已注册手机号 → 验证通过
4. 创建绑定关系 → 数据库插入记录
5. 登录成功 → 返回用户信息和Token

### 已绑定用户
1. 获取微信Code → 成功获取登录凭证
2. 检查绑定状态 → 返回 `isBound: true` 及用户信息
3. 直接登录 → 使用绑定的用户信息登录成功

## 错误处理测试
- **网络连接失败**：检查IP地址和端口
- **微信code获取失败**：检查微信开发者工具设置
- **手机号未注册**：输入未注册手机号，应提示联系管理员
- **重复绑定**：尝试绑定已绑定的手机号，应提示错误

## 调试技巧
1. **查看实时日志**：测试页面提供详细的操作日志
2. **检查网络连接**：先使用网络测试页面验证连接
3. **查看控制台**：微信开发者工具控制台有详细的API调用日志
4. **检查后端日志**：后端控制台会显示接口调用情况
5. **查看数据库**：检查 `app_wechat_bindings` 表的记录变化

## 数据库查询
```sql
-- 查看所有绑定记录
SELECT * FROM app_wechat_bindings ORDER BY create_time DESC;

-- 查看特定openid的绑定
SELECT * FROM app_wechat_bindings WHERE open_id = 'your_openid';

-- 查看特定手机号的绑定
SELECT * FROM app_wechat_bindings WHERE phone = '13812345678';

-- 清空测试数据（如需要）
DELETE FROM app_wechat_bindings WHERE phone IN ('13812345678', '13987654321');
```

## 快速访问链接
- 网络测试: `/pages/test/network-test`
- 微信绑定测试: `/pages/test/wechat-binding`
- 正常登录页面: `/pages/login/index-new`