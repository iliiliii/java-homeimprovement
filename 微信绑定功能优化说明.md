# 微信绑定功能优化说明

## 概述
为客户管理和用户管理页面添加了微信绑定状态显示和解绑功能，支持显示微信头像并可以解除绑定。

## 修改内容

### 1. 新增API文件
**文件**: `vue3/src/api/evs/wechatBindings.js`

新增了微信绑定相关的API接口：
- `listWechatBindings(query)` - 查询微信绑定列表
- `getWechatBinding(id)` - 查询微信绑定详细信息
- `unbindWechat(data)` - 解除微信绑定

### 2. 客户管理页面优化
**文件**: `vue3/src/views/evs/customers/card.vue`

#### 功能增强
1. **微信绑定状态显示**
   - 卡片头部显示微信头像（如果已绑定）
   - 卡片详情中显示微信绑定状态标签
   - 已绑定显示绿色"已绑定"标签，未绑定显示灰色"未绑定"标签

2. **微信头像集成**
   - 优先使用微信绑定的头像
   - 如果未绑定微信，显示客户姓名首字母

3. **解绑功能**
   - 卡片底部添加"解绑"按钮（仅已绑定时显示）
   - 详情对话框中添加"解除微信绑定"按钮
   - 解绑前需要确认操作

4. **微信绑定信息展示**
   - 详情对话框中新增"微信绑定信息"卡片
   - 显示微信昵称、头像、绑定手机、绑定时间、最后登录时间

#### 权限控制
- 解绑功能需要 `evs:appWechatBindings:remove` 权限

### 3. 用户管理页面优化
**文件**: `vue3/src/views/system/user/card.vue`

#### 功能增强
1. **微信绑定状态列**
   - 表格中新增"微信绑定"列
   - 显示绑定状态标签（已绑定/未绑定）
   - 使用微信图标标识

2. **微信头像集成**
   - 头像列优先显示微信绑定的头像
   - 如果未绑定微信，显示系统头像或用户名首字母

3. **解绑功能**
   - 操作列添加"解绑"按钮（仅已绑定时显示）
   - 解绑前需要确认操作

#### 权限控制
- 解绑功能需要 `evs:appWechatBindings:remove` 权限

## 数据流程

### 1. 获取微信绑定信息
```javascript
// 客户管理页面
listWechatBindings({ userType: 'customer' })

// 用户管理页面
listWechatBindings({ userType: 'staff' })
```

### 2. 解除微信绑定
```javascript
unbindWechat({
  openid: binding.openId,
  deviceId: 'admin-web'
})
```

## 后端API说明

### 查询微信绑定列表
- **接口**: `GET /evs/appWechatBindings/list`
- **参数**: 
  - `userType`: 用户类型（customer/staff）
  - 其他分页参数
- **权限**: `evs:appWechatBindings:list`

### 解除微信绑定
- **接口**: `POST /app/auth/unbind-wechat`
- **参数**:
  ```json
  {
    "openid": "微信openId",
    "deviceId": "设备ID"
  }
  ```
- **权限**: 无需特殊权限（通过openId验证）

## 数据结构

### AppWechatBindings 实体
```java
{
  "id": "主键ID",
  "openId": "微信openId",
  "unionId": "微信unionId",
  "userType": "用户类型（customer/staff）",
  "userId": "用户ID",
  "phone": "绑定手机号",
  "nickname": "微信昵称",
  "avatar": "微信头像",
  "sessionKey": "会话密钥",
  "bindTime": "绑定时间",
  "lastLoginTime": "最后登录时间"
}
```

## 样式优化

### 客户管理页面
1. 新增 `.customer-avatar-section` 样式，支持头像和名称并排显示
2. 新增 `.wechat-binding-item` 样式，显示微信绑定状态
3. 新增 `.wechat-binding-info` 样式，详情对话框中的微信信息展示

### 用户管理页面
1. 表格列宽度调整，为微信绑定列预留空间
2. 操作列宽度增加，容纳解绑按钮

## 使用说明

### 客户管理页面
1. 进入客户管理页面，卡片会自动显示微信绑定状态
2. 已绑定微信的客户会显示微信头像
3. 点击"查看"按钮，可以在详情中看到完整的微信绑定信息
4. 点击"解绑"按钮可以解除微信绑定

### 用户管理页面
1. 进入用户管理页面，表格会显示微信绑定状态列
2. 已绑定微信的用户会显示微信头像
3. 点击"解绑"按钮可以解除微信绑定

## 注意事项

1. **权限配置**: 确保相关角色拥有 `evs:appWechatBindings:list` 和 `evs:appWechatBindings:remove` 权限
2. **数据同步**: 解绑后会自动刷新绑定信息，无需手动刷新页面
3. **头像优先级**: 微信头像优先于系统头像显示
4. **用户类型**: 客户和员工的微信绑定通过 `userType` 字段区分
5. **安全性**: 解绑操作需要用户确认，防止误操作

## 测试建议

1. **功能测试**
   - 测试已绑定和未绑定用户的显示效果
   - 测试解绑功能是否正常工作
   - 测试微信头像是否正确显示

2. **权限测试**
   - 测试无权限用户是否能看到解绑按钮
   - 测试无权限用户点击解绑是否被拦截

3. **边界测试**
   - 测试微信头像加载失败的情况
   - 测试网络异常时的错误处理
   - 测试大量数据时的性能表现

## 后续优化建议

1. 添加批量解绑功能
2. 添加微信绑定历史记录
3. 支持管理员主动绑定用户微信
4. 添加微信绑定统计报表
