# 下一步补充清单

**创建时间**: 2026-03-01  
**当前进度**: 10.3%

---

## 📋 需要补充的内容总览

### 1️⃣ ProjectCustomersMapper.xml（最高优先级）
- **文件**: `sb3/evs-home/src/main/resources/mapper/evs/ProjectCustomersMapper.xml`
- **状态**: ⚠️ 已创建但不完整
- **需要**: 补充 9 个业务 SQL + 1 个结果映射 + 修改 3 个现有方法
- **预计时间**: 1.5小时

### 2️⃣ ProjectCustomersMapper.java
- **文件**: `sb3/evs-home/src/main/java/com/ruoyi/web/mapper/ProjectCustomersMapper.java`
- **状态**: ⚠️ 已创建但不完整
- **需要**: 补充 8 个业务方法
- **预计时间**: 30分钟

### 3️⃣ ProjectCustomers.java
- **文件**: `sb3/evs-home/src/main/java/com/ruoyi/web/domain/ProjectCustomers.java`
- **状态**: ⚠️ 已创建但有问题
- **需要**: 修改 isPrimary 类型 + 添加 customer 字段
- **预计时间**: 10分钟

### 4️⃣ IProjectCustomersService.java
- **文件**: `sb3/evs-home/src/main/java/com/ruoyi/web/service/IProjectCustomersService.java`
- **状态**: ⚠️ 已创建但不完整
- **需要**: 补充 7 个业务方法
- **预计时间**: 20分钟

### 5️⃣ ProjectCustomersServiceImpl.java
- **文件**: `sb3/evs-home/src/main/java/com/ruoyi/web/service/impl/ProjectCustomersServiceImpl.java`
- **状态**: ⚠️ 已创建但不完整
- **需要**: 实现 7 个业务方法（含事务处理、业务验证）
- **预计时间**: 2小时

### 6️⃣ ProjectAccessChecker.java（新建）
- **文件**: `sb3/evs-home/src/main/java/com/ruoyi/web/util/ProjectAccessChecker.java`
- **状态**: ❌ 未创建
- **需要**: 创建权限检查工具类
- **预计时间**: 1小时

---

## 🔴 P0 - 立即执行（今天必须完成）

### 任务 1: 补充 ProjectCustomersMapper.xml

#### 1.1 添加结果映射（5分钟）

在 `<mapper>` 标签内，`ProjectCustomersResult` 后面添加：

```xml
<!-- 包含客户信息的结果映射 -->
<resultMap type="ProjectCustomers" id="ProjectCustomersWithCustomerResult" 
           extends="ProjectCustomersResult">
    <association property="customer" javaType="Customers">
        <id property="id" column="customer_id"/>
        <result property="name" column="customer_name"/>
        <result property="phone" column="customer_phone"/>
        <result property="email" column="customer_email"/>
        <result property="avatar" column="customer_avatar"/>
    </association>
</resultMap>
```

#### 1.2 补充 9 个业务 SQL（1小时）

在 XML 文件末尾 `</mapper>` 前添加：

```xml
<!-- 查询项目的所有客户 -->
<select id="selectByProjectId" resultMap="ProjectCustomersWithCustomerResult">
    SELECT 
        pc.id, pc.project_id, pc.customer_id, pc.role, pc.is_primary,
        pc.deleted_at, pc.created_at, pc.updated_at, pc.created_by, pc.updated_by,
        c.name as customer_name, c.phone as customer_phone,
        c.email as customer_email, c.avatar as customer_avatar
    FROM project_customers pc
    LEFT JOIN customers c ON pc.customer_id = c.id
    WHERE pc.project_id = #{projectId} AND pc.deleted_at IS NULL
    ORDER BY pc.is_primary DESC, pc.created_at ASC
</select>

<!-- 查询客户的所有项目 -->
<select id="selectByCustomerId" resultMap="ProjectCustomersResult">
    SELECT *
    FROM project_customers
    WHERE customer_id = #{customerId} AND pc.deleted_at IS NULL
    ORDER BY is_primary DESC, created_at DESC
</select>

<!-- 查询项目的主客户 -->
<select id="selectPrimaryByProjectId" resultMap="ProjectCustomersWithCustomerResult">
    SELECT 
        pc.id, pc.project_id, pc.customer_id, pc.role, pc.is_primary,
        pc.deleted_at, pc.created_at, pc.updated_at,
        c.name as customer_name, c.phone as customer_phone,
        c.email as customer_email, c.avatar as customer_avatar
    FROM project_customers pc
    LEFT JOIN customers c ON pc.customer_id = c.id
    WHERE pc.project_id = #{projectId} 
      AND pc.is_primary = 1 
      AND pc.deleted_at IS NULL
    LIMIT 1
</select>

<!-- 检查客户是否关联到项目 -->
<select id="checkCustomerInProject" resultType="boolean">
    SELECT COUNT(*) > 0
    FROM project_customers
    WHERE project_id = #{projectId} 
      AND customer_id = #{customerId}
      AND deleted_at IS NULL
</select>

<!-- 批量插入 -->
<insert id="batchInsert">
    INSERT INTO project_customers (
        id, project_id, customer_id, role, is_primary,
        created_at, created_by
    ) VALUES
    <foreach collection="list" item="item" separator=",">
        (
            #{item.id}, #{item.projectId}, #{item.customerId}, 
            #{item.role}, #{item.isPrimary},
            #{item.createdAt}, #{item.createdBy}
        )
    </foreach>
</insert>

<!-- 软删除 -->
<update id="softDelete">
    UPDATE project_customers
    SET deleted_at = NOW(), updated_at = NOW()
    WHERE id = #{id}
</update>

<!-- 设置主客户 -->
<update id="setPrimaryCustomer">
    UPDATE project_customers 
    SET is_primary = 0, updated_at = NOW()
    WHERE project_id = #{projectId} AND deleted_at IS NULL;
    
    UPDATE project_customers
    SET is_primary = 1, updated_at = NOW()
    WHERE project_id = #{projectId} AND customer_id = #{customerId} AND deleted_at IS NULL;
</update>

<!-- 统计项目客户数量 -->
<select id="countByProjectId" resultType="int">
    SELECT COUNT(*)
    FROM project_customers
    WHERE project_id = #{projectId} AND deleted_at IS NULL
</select>

<!-- 批量统计 -->
<select id="countByProjectIds" resultType="java.util.Map">
    SELECT 
        project_id as projectId,
        COUNT(*) as customerCount
    FROM project_customers
    WHERE project_id IN
    <foreach collection="projectIds" item="id" open="(" separator="," close=")">
        #{id}
    </foreach>
    AND deleted_at IS NULL
    GROUP BY project_id
</select>
```

#### 1.3 修改现有方法（15分钟）

**修改 selectProjectCustomersList**:
```xml
<!-- 在 <where> 标签内第一行添加 -->
deleted_at IS NULL
```

**修改 deleteProjectCustomersById**:
```xml
<!-- 将 <delete> 改为 <update> -->
<update id="deleteProjectCustomersById" parameterType="String">
    UPDATE project_customers
    SET deleted_at = NOW(), updated_at = NOW()
    WHERE id = #{id}
</update>
```

**修改 deleteProjectCustomersByIds**:
```xml
<!-- 将 <delete> 改为 <update> -->
<update id="deleteProjectCustomersByIds" parameterType="String">
    UPDATE project_customers
    SET deleted_at = NOW(), updated_at = NOW()
    WHERE id IN
    <foreach item="id" collection="array" open="(" separator="," close=")">
        #{id}
    </foreach>
</update>
```

---

### 任务 2: 补充 ProjectCustomersMapper.java（30分钟）

在接口中添加以下方法：

```java
/**
 * 查询项目的所有客户
 */
List<ProjectCustomers> selectByProjectId(@Param("projectId") String projectId);

/**
 * 查询客户的所有项目
 */
List<ProjectCustomers> selectByCustomerId(@Param("customerId") String customerId);

/**
 * 查询项目的主客户
 */
ProjectCustomers selectPrimaryByProjectId(@Param("projectId") String projectId);

/**
 * 检查客户是否关联到项目
 */
boolean checkCustomerInProject(@Param("projectId") String projectId, 
                               @Param("customerId") String customerId);

/**
 * 批量插入
 */
int batchInsert(@Param("list") List<ProjectCustomers> list);

/**
 * 软删除
 */
int softDelete(@Param("id") String id);

/**
 * 设置主客户
 */
int setPrimaryCustomer(@Param("projectId") String projectId, 
                      @Param("customerId") String customerId);

/**
 * 统计项目客户数量
 */
int countByProjectId(@Param("projectId") String projectId);

/**
 * 批量统计
 */
List<Map<String, Object>> countByProjectIds(@Param("projectIds") List<String> projectIds);
```

---

### 任务 3: 修复 ProjectCustomers.java（10分钟）

#### 3.1 修改 isPrimary 类型

**查找**:
```java
private Integer isPrimary;
```

**替换为**:
```java
private Boolean isPrimary;
```

**同时修改 getter/setter**:
```java
public void setIsPrimary(Boolean isPrimary) {
    this.isPrimary = isPrimary;
}

public Boolean getIsPrimary() {
    return isPrimary;
}
```

#### 3.2 添加 customer 字段

在类中添加：

```java
/** 关联的客户信息 */
private Customers customer;

public void setCustomer(Customers customer) {
    this.customer = customer;
}

public Customers getCustomer() {
    return customer;
}
```

---

## 🟡 P1 - 今天或明天完成

### 任务 4: 补充 IProjectCustomersService.java（20分钟）

添加业务方法：

```java
/**
 * 查询项目的所有客户
 */
List<ProjectCustomers> selectByProjectId(String projectId);

/**
 * 查询客户的所有项目
 */
List<ProjectCustomers> selectByCustomerId(String customerId);

/**
 * 查询项目的主客户
 */
ProjectCustomers selectPrimaryByProjectId(String projectId);

/**
 * 添加客户到项目（支持多个）
 */
int addProjectCustomers(String projectId, List<String> customerIds, String primaryCustomerId);

/**
 * 移除客户
 */
int removeProjectCustomer(String projectId, String customerId);

/**
 * 设置主客户
 */
int setPrimaryCustomer(String projectId, String customerId);

/**
 * 检查权限
 */
boolean checkCustomerAccess(String projectId, String customerId);
```

---

### 任务 5: 实现 ProjectCustomersServiceImpl.java（2小时）

实现上述 7 个业务方法，包含：
- 事务处理（@Transactional）
- 业务验证（参数检查、数据验证）
- 错误处理
- 日志记录

参考模板见 `阶段一-基础代码模板.md` 中的 Service 层实现部分。

---

### 任务 6: 创建 ProjectAccessChecker.java（1小时）

创建统一的权限检查工具类：

**文件路径**: `sb3/evs-home/src/main/java/com/ruoyi/web/util/ProjectAccessChecker.java`

```java
package com.ruoyi.web.util;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;
import com.ruoyi.web.service.IProjectCustomersService;

/**
 * 项目访问权限检查工具类
 */
@Component
public class ProjectAccessChecker {
    
    @Autowired
    private IProjectCustomersService projectCustomersService;
    
    /**
     * 检查客户是否有权访问项目
     */
    public boolean checkAccess(String projectId, String customerId) {
        return projectCustomersService.checkCustomerAccess(projectId, customerId);
    }
    
    /**
     * 检查并抛出异常
     */
    public void checkAccessOrThrow(String projectId, String customerId) {
        if (!checkAccess(projectId, customerId)) {
            throw new RuntimeException("无权访问该项目");
        }
    }
}
```

---

## 📊 完成后的进度预估

| 任务 | 当前进度 | 完成后进度 | 提升 |
|------|---------|-----------|------|
| 任务1: XML补充 | 35% | 100% | +65% |
| 任务2: Mapper接口 | 30% | 100% | +70% |
| 任务3: 实体类修复 | 80% | 100% | +20% |
| 任务4: Service接口 | 30% | 100% | +70% |
| 任务5: Service实现 | 20% | 80% | +60% |
| 任务6: 权限工具类 | 0% | 100% | +100% |
| **阶段一总进度** | **36.25%** | **100%** | **+63.75%** |
| **阶段二总进度** | **12.5%** | **90%** | **+77.5%** |
| **总体进度** | **10.3%** | **32.5%** | **+22.2%** |

---

## ⏱️ 时间预估

### 今天（P0任务）
- 任务1: 1.5小时
- 任务2: 0.5小时
- 任务3: 0.2小时
- **小计**: 2.2小时

### 明天（P1任务）
- 任务4: 0.3小时
- 任务5: 2小时
- 任务6: 1小时
- **小计**: 3.3小时

### 总计
**5.5小时**（约1天工作量）

---

## ✅ 完成检查清单

### 今天必须完成
- [ ] ProjectCustomersMapper.xml 补充完成
- [ ] ProjectCustomersMapper.java 补充完成
- [ ] ProjectCustomers.java 修复完成
- [ ] 编译通过，无语法错误

### 明天完成
- [ ] IProjectCustomersService.java 补充完成
- [ ] ProjectCustomersServiceImpl.java 实现完成
- [ ] ProjectAccessChecker.java 创建完成
- [ ] 单元测试通过

---

## 📝 注意事项

1. **按顺序执行**: 必须先完成 XML 和 Mapper 接口，再实现 Service 层
2. **测试验证**: 每完成一个任务，立即编译测试
3. **软删除**: 所有删除操作都使用软删除（设置 deleted_at）
4. **事务处理**: Service 层的批量操作必须加 @Transactional
5. **错误处理**: 所有方法都要有适当的异常处理

---

**创建时间**: 2026-03-01  
**预计完成**: 2026-03-02
