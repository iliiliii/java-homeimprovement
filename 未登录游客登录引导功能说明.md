# 未登录游客登录引导功能说明

## 功能概述

为未登录的游客添加登录引导功能，当他们尝试查看大图时，提示需要登录。登录后的游客则可以直接查看大图。

---

## 用户体验流程

### 未登录的游客

```
访问设计/日志页面
    ↓
可以看到列表数据 ✅
    ↓
点击查看大图
    ↓
弹出提示："请登录后查看大图"
    ├─ 点击"去登录" → 跳转到登录页
    └─ 点击"取消" → 关闭提示
```

### 登录后的游客

```
登录（游客账号）
    ↓
访问设计/日志页面
    ↓
可以看到列表数据 ✅
    ↓
点击查看大图
    ↓
直接打开大图查看器 ✅
```

### 正常用户（客户/员工）

```
登录（正常账号）
    ↓
访问自己的项目
    ↓
可以看到列表数据 ✅
    ↓
点击查看大图
    ↓
直接打开大图查看器 ✅
```

---

## 技术实现

### 1. 新增辅助函数

#### 文件：`uni3/src/config/guest.js`

**新增函数：`isUnloggedGuest()`**

```javascript
/**
 * 判断是否为未登录的游客（没有token）
 * @returns {boolean}
 */
export const isUnloggedGuest = () => {
  try {
    const token = uni.getStorageSync('token')
    const isLoggedIn = !!(token && token !== '' && token !== 'null')
    return !isLoggedIn
  } catch (error) {
    console.error('[Guest Config] 判断未登录状态失败:', error)
    return true
  }
}
```

**区别说明：**
- `isGuestUser()` - 判断是否为游客（包括未登录和登录的游客）
- `isUnloggedGuest()` - 判断是否为未登录的游客（只有未登录）

---

### 2. 设计页面修改

#### 文件：`uni3/src/pages/design/index.vue`

**修改点1：导入新函数**
```javascript
import { getCurrentProjectId, isGuestUser, isUnloggedGuest } from '@/config/guest'
```

**修改点2：修改 `openRoom()` 函数**
```javascript
// 打开房间查看设计图
const openRoom = (room) => {
  if (!room.images || room.images.length === 0) {
    uni.showToast({
      title: '该房间暂无设计图',
      icon: 'none'
    })
    return
  }
  
  // 检查是否为未登录的游客
  if (isUnloggedGuest()) {
    uni.showModal({
      title: '登录提示',
      content: '请登录后查看大图',
      confirmText: '去登录',
      cancelText: '取消',
      success: (res) => {
        if (res.confirm) {
          // 跳转到登录页
          uni.navigateTo({
            url: '/pages/login/index-new'
          })
        }
      }
    })
    return
  }
  
  // 登录的游客或正常用户可以查看大图
  currentViewRoom.value = room
  viewerImages.value = room.images.map((url, index) => ({
    url: getFullUrl(url),
    name: `${room.roomName} - 设计图${index + 1}`
  }))
  viewerIndex.value = 0
  viewerVisible.value = true
}
```

**逻辑说明：**
1. 检查房间是否有图片
2. 检查是否为未登录的游客
   - 是 → 显示登录提示
   - 否 → 直接打开大图查看器

---

### 3. 日志页面修改

#### 文件：`uni3/src/pages/log/index.vue`

**修改点1：导入新函数**
```javascript
import { getCurrentProjectId, isGuestUser, isUnloggedGuest } from '@/config/guest'
```

**修改点2：修改 `handleRecordClick()` 函数**
```javascript
// 处理验收记录点击
const handleRecordClick = async (record) => {
  // 检查是否为未登录的游客
  if (isUnloggedGuest()) {
    uni.showModal({
      title: '登录提示',
      content: '请登录后查看大图',
      confirmText: '去登录',
      cancelText: '取消',
      success: (res) => {
        if (res.confirm) {
          // 跳转到登录页
          uni.navigateTo({
            url: '/pages/login/index-new'
          })
        }
      }
    })
    return
  }
  
  // 登录的游客或正常用户可以查看详情
  try {
    const detail = await getProjectScheduleRecordDetail(record.id)
    
    if (detail.images && detail.images.length > 0) {
      viewerImages.value = detail.images
      viewerIndex.value = 0
      viewerVisible.value = true
    } else {
      uni.showModal({
        title: detail.title,
        content: detail.description || '暂无详细描述',
        showCancel: false,
        confirmText: '知道了'
      })
    }
  } catch (error) {
    console.error('获取记录详情失败:', error)
    uni.showToast({
      title: '获取详情失败',
      icon: 'none'
    })
  }
}
```

**修改点3：修改 `handlePreviewImages()` 函数**
```javascript
// 处理图片预览
const handlePreviewImages = ({ images, index }) => {
  // 检查是否为未登录的游客
  if (isUnloggedGuest()) {
    uni.showModal({
      title: '登录提示',
      content: '请登录后查看大图',
      confirmText: '去登录',
      cancelText: '取消',
      success: (res) => {
        if (res.confirm) {
          // 跳转到登录页
          uni.navigateTo({
            url: '/pages/login/index-new'
          })
        }
      }
    })
    return
  }
  
  // 登录的游客或正常用户可以查看大图
  viewerImages.value = images
  viewerIndex.value = index
  viewerVisible.value = true
}
```

**逻辑说明：**
1. 检查是否为未登录的游客
2. 是 → 显示登录提示
3. 否 → 执行原有逻辑（获取详情或打开大图）

---

## 修改文件清单

### 修改的文件（3个）

1. ✅ `uni3/src/config/guest.js`
   - 新增 `isUnloggedGuest()` 函数

2. ✅ `uni3/src/pages/design/index.vue`
   - 导入 `isUnloggedGuest`
   - 修改 `openRoom()` 函数

3. ✅ `uni3/src/pages/log/index.vue`
   - 导入 `isUnloggedGuest`
   - 修改 `handleRecordClick()` 函数
   - 修改 `handlePreviewImages()` 函数

---

## 用户类型对比

| 用户类型 | 可以看列表 | 可以看大图 | 点击大图时的行为 |
|---------|-----------|-----------|----------------|
| **未登录的游客** | ✅ 是 | ❌ 否 | 显示登录提示 |
| **登录的游客** | ✅ 是 | ✅ 是 | 直接打开大图 |
| **客户** | ✅ 是 | ✅ 是 | 直接打开大图 |
| **员工** | ✅ 是 | ✅ 是 | 直接打开大图 |

---

## 交互细节

### 登录提示弹窗

**样式：**
- 标题：`登录提示`
- 内容：`请登录后查看大图`
- 确认按钮：`去登录`
- 取消按钮：`取消`

**行为：**
- 点击"去登录" → 跳转到 `/pages/login/index-new`
- 点击"取消" → 关闭弹窗，停留在当前页面

**代码：**
```javascript
uni.showModal({
  title: '登录提示',
  content: '请登录后查看大图',
  confirmText: '去登录',
  cancelText: '取消',
  success: (res) => {
    if (res.confirm) {
      uni.navigateTo({
        url: '/pages/login/index-new'
      })
    }
  }
})
```

---

## 测试场景

### 场景1：未登录用户访问设计页面

**步骤：**
1. 清除小程序缓存
2. 不登录，直接访问应用
3. 进入设计页面
4. 点击任意房间卡片

**预期结果：**
- ✅ 可以看到房间列表
- ✅ 点击房间后显示"请登录后查看大图"
- ✅ 点击"去登录"跳转到登录页
- ✅ 点击"取消"关闭弹窗

### 场景2：未登录用户访问日志页面

**步骤：**
1. 清除小程序缓存
2. 不登录，直接访问应用
3. 进入日志页面
4. 点击任意验收记录

**预期结果：**
- ✅ 可以看到进度列表
- ✅ 点击记录后显示"请登录后查看大图"
- ✅ 点击"去登录"跳转到登录页
- ✅ 点击"取消"关闭弹窗

### 场景3：登录后的游客访问设计页面

**步骤：**
1. 使用游客账号登录
2. 进入设计页面
3. 点击任意房间卡片

**预期结果：**
- ✅ 可以看到房间列表
- ✅ 点击房间后直接打开大图查看器
- ✅ 不显示登录提示

### 场景4：登录后的游客访问日志页面

**步骤：**
1. 使用游客账号登录
2. 进入日志页面
3. 点击任意验收记录

**预期结果：**
- ✅ 可以看到进度列表
- ✅ 点击记录后直接打开大图查看器
- ✅ 不显示登录提示

### 场景5：正常用户访问

**步骤：**
1. 使用客户或员工账号登录
2. 进入设计/日志页面
3. 点击查看大图

**预期结果：**
- ✅ 所有功能正常
- ✅ 不显示登录提示
- ✅ 直接打开大图查看器

---

## 代码优化说明

### 1. 复用登录提示逻辑

如果将来有更多地方需要登录提示，可以抽取为公共函数：

```javascript
// uni3/src/utils/auth.js
export const showLoginPrompt = () => {
  return new Promise((resolve, reject) => {
    uni.showModal({
      title: '登录提示',
      content: '请登录后查看大图',
      confirmText: '去登录',
      cancelText: '取消',
      success: (res) => {
        if (res.confirm) {
          uni.navigateTo({
            url: '/pages/login/index-new'
          })
          resolve(true)
        } else {
          reject(false)
        }
      }
    })
  })
}

// 使用
if (isUnloggedGuest()) {
  await showLoginPrompt()
  return
}
```

### 2. 自定义提示文案

如果不同场景需要不同的提示文案：

```javascript
const showLoginPrompt = (message = '请登录后查看大图') => {
  uni.showModal({
    title: '登录提示',
    content: message,
    confirmText: '去登录',
    cancelText: '取消',
    success: (res) => {
      if (res.confirm) {
        uni.navigateTo({
          url: '/pages/login/index-new'
        })
      }
    }
  })
}

// 使用
if (isUnloggedGuest()) {
  showLoginPrompt('请登录后查看设计图详情')
  return
}
```

---

## 注意事项

### 1. 登录后的状态同步

用户登录后，需要确保：
- ✅ token已保存到storage
- ✅ userType已保存到storage
- ✅ 页面状态已更新

### 2. 登录页返回逻辑

登录成功后，建议返回到之前的页面：

```javascript
// 登录页
onLogin() {
  // 登录成功后
  const pages = getCurrentPages()
  if (pages.length > 1) {
    // 返回上一页
    uni.navigateBack()
  } else {
    // 跳转到首页
    uni.reLaunch({ url: '/pages/dashboard/index' })
  }
}
```

### 3. 游客账号的创建

确保系统中有游客账号可以登录：
- userType: 'guest'
- 没有关联项目
- 可以访问演示项目

---

## 扩展功能建议

### 1. 记住登录前的位置

```javascript
// 跳转登录时记住当前位置
if (isUnloggedGuest()) {
  // 保存当前页面路径
  uni.setStorageSync('beforeLoginPath', getCurrentPages()[0].route)
  
  uni.showModal({
    title: '登录提示',
    content: '请登录后查看大图',
    confirmText: '去登录',
    cancelText: '取消',
    success: (res) => {
      if (res.confirm) {
        uni.navigateTo({
          url: '/pages/login/index-new'
        })
      }
    }
  })
  return
}

// 登录成功后返回
const beforeLoginPath = uni.getStorageSync('beforeLoginPath')
if (beforeLoginPath) {
  uni.redirectTo({ url: `/${beforeLoginPath}` })
  uni.removeStorageSync('beforeLoginPath')
}
```

### 2. 添加"稍后再说"选项

```javascript
uni.showModal({
  title: '登录提示',
  content: '登录后可以查看大图，是否现在登录？',
  confirmText: '立即登录',
  cancelText: '稍后再说',
  success: (res) => {
    if (res.confirm) {
      uni.navigateTo({
        url: '/pages/login/index-new'
      })
    }
  }
})
```

### 3. 添加快速注册入口

```javascript
uni.showModal({
  title: '登录提示',
  content: '请登录后查看大图',
  confirmText: '去登录',
  cancelText: '快速注册',
  success: (res) => {
    if (res.confirm) {
      uni.navigateTo({
        url: '/pages/login/index-new'
      })
    } else {
      uni.navigateTo({
        url: '/pages/register/index'
      })
    }
  }
})
```

---

## 总结

本次修改实现了：
1. ✅ 未登录游客点击大图时显示登录提示
2. ✅ 登录后的游客可以直接查看大图
3. ✅ 正常用户不受影响
4. ✅ 提供友好的登录引导

**修改文件：** 3个
**新增函数：** 1个（`isUnloggedGuest`）
**修改函数：** 3个（`openRoom`、`handleRecordClick`、`handlePreviewImages`）

**用户体验：** 清晰的登录引导，不影响浏览体验

---

**文档版本：** 1.0  
**创建时间：** 2026-02-13  
**维护人员：** Kiro AI Assistant
