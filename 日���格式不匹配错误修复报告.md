# 日期格式不匹配错误修复报告

## 🐛 错误信息

```
JSON parse error: Cannot deserialize value of type `java.util.Date` from String "2025-11-29 01:39:24":
not a valid representation (error: Failed to parse Date value '2025-11-29 01:39:24':
Cannot parse date "2025-11-29 01:39:24": while it seems to fit format 'yyyy-MM-dd'T'HH:mm:ss.SSSX', parsing fails (leniency? null))
```

## 🔍 问题分析

### 根本原因
**前端和后端日期格式不匹配**

| 位置 | 格式 | 示例 | 问题 |
|------|------|------|------|
| **前端发送** | `yyyy-MM-dd HH:mm:ss` | `2025-11-29 01:39:24` | ❌ 中间是**空格** |
| **后端期望** | `yyyy-MM-dd'T'HH:mm:ss` | `2025-11-29T01:39:24` | ✅ 中间是**T** |

Spring Boot默认使用ISO 8601标准日期格式，但前端Element Plus组件默认使用空格分隔的格式，导致JSON序列化/反序列化失败。

### 错误路径
1. 用户选择验收时间
2. Element Plus DatePicker生成字符串：`"2025-11-29 01:39:24"`
3. 前端发送JSON请求给后端
4. Spring Boot尝试解析日期，匹配失败
5. 抛出 `JsonParseException` 异常

## ✅ 修复方案

### 修复1：前端日期格式 (AcceptanceReportDialog.vue)

**第65行**：修改Element Plus DatePicker的value-format

```vue
<!-- 修改前 -->
value-format="YYYY-MM-DD HH:mm:ss"

<!-- 修改后 -->
value-format="YYYY-MM-DDTHH:mm:ss"  <!-- ✅ ISO 8601格式 -->
```

**影响**：
- 用户看到的时间显示格式不变（仍是 `2025-11-29 01:39`）
- 但实际发送的数据格式变为：`2025-11-29T01:39:24`
- 符合ISO 8601标准，Spring Boot可正确解析

### 修复2：后端日期注解 (ProjectScheduleRecords.java)

**第49行**：为`acceptanceTime`字段添加`@JsonFormat`注解

```java
/** 验收时间（实际验收发生时间） */
@Excel(name = "验收时间", readConverterExp = "实=际验收发生时间")
@JsonFormat(pattern = "yyyy-MM-dd'T'HH:mm:ss", timezone = "GMT+8")  // ✅ 指定日期格式和时区
private Date acceptanceTime;
```

**参数说明**：
- `pattern`: 指定日期格式模式（使用T分隔符）
- `timezone`: 指定时区为GMT+8（中国时区）
- 覆盖Spring Boot默认配置，确保精确匹配

## 🎯 修复效果

### 修复前
- ❌ 提交验收时报错：`JSON parse error`
- ❌ 日期解析失败，请���被拒绝
- ❌ 用户无法成功提交验收

### 修复后
- ✅ 前端发送格式：`2025-11-29T01:39:24`
- ✅ 后端正确解析：`Date` 对象
- ✅ 验收提交成功，日期存储正确

## 📊 日期格式对比

| 标准 | 格式 | 示例 | 说明 |
|------|------|------|------|
| **ISO 8601** | `yyyy-MM-dd'T'HH:mm:ss` | `2025-11-29T01:39:24` | ✅ 国际标准 |
| **中国常用** | `yyyy-MM-dd HH:mm:ss` | `2025-11-29 01:39:24` | ❌ 非标准 |
| **完整ISO** | `yyyy-MM-dd'T'HH:mm:ss.SSSX` | `2025-11-29T01:39:24.000Z` | 更精确 |

## 🧪 测试建议

### 测试场景1：手动选择时间
1. 打开验收上报对话框
2. 手动选择验收时间（不要使用默认时间）
3. 点击提交验收
4. **预期**：提交成功，无日期解析错误

### 测试场景2：默认时间
1. 打开验收上报对话框（使用自动生成的默认时间）
2. 直接提交验收（不修改时间）
3. **预期**：提交成功，日期正确存储

### 测试场景3：时区测试
1. 在不同时区或修改系统时间后测试
2. 提交验收记录
3. **预期**：时间以GMT+8时区正确存储和显示

## 🔧 技术要点

### Element Plus DatePicker
- `format`: 控制显示格式（用户看到的样子）
- `value-format`: 控制实际值格式（发送数据的格式）
- 默认使用本地化格式，需手动指定ISO 8601

### Spring Boot日期处理
- `@JsonFormat`: Jackson日期序列化/反序列化注解
- `pattern`: 指定日期格式模式
- `timezone`: 指定时区偏移
- 需要与前端格式完全匹配

### ISO 8601标准
- 国际标准日期时间格式
- 结构：`YYYY-MM-DDTHH:mm:ss`
- T作为日期和时间分隔符
- 避免歧义，跨平台兼容性好

## 📝 注意事项

1. **format vs value-format**
   - `format`: 仅影响显示，不影响数据
   - `value-format`: 影响实际发送的数据格式
   - 两个可以不同，但推荐保持一致

2. **时区处理**
   - 中国项目使用 `GMT+8` 时区
   - 如果不指定timezone，默认使用UTC
   - 可能导致时间相差8小时

3. **其他日期字段**
   - 如果项目中有其他Date字段，也需要类似处理
   - 如 `createdAt`、`updatedAt` 等

---

**修复时间**：2025-11-29
**修复文件**：
- `vue3/src/views/evs/projectScheduleRecords/components/AcceptanceReportDialog.vue`
- `sb3/evs-home/src/main/java/com/ruoyi/web/domain/ProjectScheduleRecords.java`
**修复状态**：✅ 完成
**测试状态**：⏳ 待测试
