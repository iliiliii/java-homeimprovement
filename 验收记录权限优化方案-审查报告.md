# 验收记录权限优化方案 - 审查报告

## 一、方案正确性分析

### ✅ 正确的部分

#### 1. 目标1 - createBy字段统一方案

**✅ SecurityUtils.getUserId() 方法存在**
- 位置: `sb3/ruoyi-common/src/main/java/com/ruoyi/common/utils/SecurityUtils.java`
- 返回类型: `Long`
- 需要转换为String: `SecurityUtils.getUserId().toString()`

**✅ 修改位置正确**
- `ProjectScheduleRecordsServiceImpl.insertProjectScheduleRecords()`
- `ProjectScheduleRecordsServiceImpl.updateProjectScheduleRecords()`

**✅ 数据迁移SQL合理**
- 备份策略正确
- 转换逻辑清晰

#### 2. 目标2 - 权限扩展方案

**✅ checkStaffProjectAccess 方法已存在**
- 位置: `AppDashboardMapper.checkStaffProjectAccess()`
- SQL: 查询 `project_members` 表
- 可直接复用

**✅ App模块修改位置正确**
- `AppProjectScheduleServiceImpl.updateAcceptanceRecord()`
- `AppProjectScheduleServiceImpl.deleteAcceptanceRecord()`

**✅ 前端API已存在**
- uni3: `getProjectMembers(projectId)` ✅
- vue3: `listProjectMembers(query)` ✅

---

## 二、发现的问题

### ❌ 问题1: Web模块缺少projectId字段

**问题描述**:
方案中建议在 `ProjectScheduleRecordsServiceImpl` 中添加权限验证：

```java
String projectId = projectScheduleRecords.getProjectId();
if (!isProjectMember(currentUserId, projectId)) {
    throw new ServiceException("只有项目成员可以编辑验收记录");
}
```

**实际情况**:
- `ProjectScheduleRecords` 实体类有 `projectId` 字段 ✅
- 但在 `updateProjectScheduleRecords()` 方法中，传入的对象可能只包含部分字段
- 需要先从数据库查询完整记录才能获取 `projectId`

**修正方案**:
```java
@Override
public int updateProjectScheduleRecords(ProjectScheduleRecords projectScheduleRecords)
{
    // ✅ 先查询完整记录获取projectId
    ProjectScheduleRecords existingRecord = projectScheduleRecordsMapper
        .selectProjectScheduleRecordsById(projectScheduleRecords.getId());
    
    if (existingRecord == null) {
        throw new ServiceException("记录不存在");
    }
    
    // 权限验证
    String currentUserId = SecurityUtils.getUserId().toString();
    String projectId = existingRecord.getProjectId();
    
    if (!isProjectMember(currentUserId, projectId)) {
        throw new ServiceException("只有项目成员可以编辑验收记录");
    }
    
    projectScheduleRecords.setUpdateTime(DateUtils.getNowDate());
    projectScheduleRecords.setUpdateBy(currentUserId);
    return projectScheduleRecordsMapper.updateProjectScheduleRecords(projectScheduleRecords);
}
```

### ❌ 问题2: Web模块Mapper方法缺失

**问题描述**:
方案建议添加 `checkUserIsProjectMember` 方法到 `ProjectScheduleRecordsMapper`

**实际情况**:
- `ProjectScheduleRecordsMapper` 是专门处理验收记录的Mapper
- 项目成员查询应该使用专门的 `ProjectMembersMapper`
- 或者创建一个通用的权限验证Mapper

**修正方案**:

**方案A: 使用ProjectMembersMapper（推荐）**

```java
// ProjectScheduleRecordsServiceImpl.java
@Autowired
private ProjectMembersMapper projectMembersMapper;

private boolean isProjectMember(String userId, String projectId) {
    return projectMembersMapper.checkUserIsProjectMember(userId, projectId);
}
```

```java
// ProjectMembersMapper.java
/**
 * 检查用户是否为项目成员
 */
boolean checkUserIsProjectMember(@Param("userId") String userId, @Param("projectId") String projectId);
```

```xml
<!-- ProjectMembersMapper.xml -->
<select id="checkUserIsProjectMember" resultType="boolean">
    SELECT COUNT(*) > 0
    FROM project_members
    WHERE user_id = #{userId} AND project_id = #{projectId} AND is_active = 1
</select>
```

**方案B: 直接注入AppDashboardMapper（更简单）**

```java
// ProjectScheduleRecordsServiceImpl.java
@Autowired
private AppDashboardMapper dashboardMapper;

private boolean isProjectMember(String userId, String projectId) {
    return dashboardMapper.checkStaffProjectAccess(userId, projectId);
}
```

### ❌ 问题3: 前端权限判断逻辑不完整

**问题描述**:
方案中uni3的修改建议：

```vue
const isProjectMember = computed(() => {
  if (!props.projectMembers || props.projectMembers.length === 0) {
    return true  // ❌ 默认允许操作不安全
  }
  return props.projectMembers.some(member => member.userId === userStore.userId)
})
```

**问题**:
- 如果没有传入 `projectMembers` prop，默认返回 `true` 不安全
- 应该主动查询项目成员列表

**修正方案**:

```vue
<script setup>
import { getProjectMembers } from '@/api/dashboard.js'

const props = defineProps({
  record: { type: Object, required: true },
  projectId: { type: String, required: true }  // ✅ 新增projectId prop
})

// 项目成员列表
const projectMembers = ref([])
const membersLoading = ref(false)

// 加载项目成员
const loadProjectMembers = async () => {
  if (!props.projectId) return
  
  membersLoading.value = true
  try {
    const data = await getProjectMembers(props.projectId, { loading: false })
    projectMembers.value = data || []
  } catch (error) {
    console.error('加载项目成员失败:', error)
    projectMembers.value = []
  } finally {
    membersLoading.value = false
  }
}

// 是否是项目成员
const isProjectMember = computed(() => {
  if (projectMembers.value.length === 0) {
    return false  // ✅ 默认不允许操作，更安全
  }
  return projectMembers.value.some(member => member.userId === userStore.userId)
})

// 是否可以操作记录
const canOperate = computed(() => {
  return isStaff.value && isProjectMember.value
})

// 监听projectId变化
watch(() => props.projectId, (newVal) => {
  if (newVal) {
    loadProjectMembers()
  }
}, { immediate: true })
</script>
```

### ⚠️ 问题4: 数据库字段名不一致

**问题描述**:
方案中SQL使用 `created_by`，但实际可能是 `create_by`

**检查结果**:
```sql
-- AppProjectScheduleMapper.xml 中使用
SELECT created_by FROM project_schedule_records WHERE id = #{recordId}
```

**需要确认**:
- 数据库表实际字段名是 `create_by` 还是 `created_by`
- BaseEntity 中的字段映射

**修正建议**:
统一使用 BaseEntity 的标准字段名：
- `create_by` (创建人)
- `create_time` (创建时间)
- `update_by` (更新人)
- `update_time` (更新时间)

---

## 三、完整修正方案

### 3.1 目标1: 统一createBy字段

#### 修改文件1: ProjectScheduleRecordsServiceImpl.java

```java
package com.ruoyi.web.service.impl;

import java.util.List;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import com.ruoyi.web.mapper.ProjectScheduleRecordsMapper;
import com.ruoyi.web.domain.ProjectScheduleRecords;
import com.ruoyi.web.service.IProjectScheduleRecordsService;
import com.ruoyi.common.utils.DateUtils;
import com.ruoyi.common.utils.SecurityUtils;
import com.ruoyi.common.utils.uuid.IdUtils;

@Service
public class ProjectScheduleRecordsServiceImpl implements IProjectScheduleRecordsService 
{
    @Autowired
    private ProjectScheduleRecordsMapper projectScheduleRecordsMapper;

    @Override
    public ProjectScheduleRecords selectProjectScheduleRecordsById(String id)
    {
        return projectScheduleRecordsMapper.selectProjectScheduleRecordsById(id);
    }

    @Override
    public List<ProjectScheduleRecords> selectProjectScheduleRecordsList(ProjectScheduleRecords projectScheduleRecords)
    {
        return projectScheduleRecordsMapper.selectProjectScheduleRecordsList(projectScheduleRecords);
    }

    @Override
    public int insertProjectScheduleRecords(ProjectScheduleRecords projectScheduleRecords)
    {
        // 如果 id 为空，自动生成 UUID
        if (projectScheduleRecords.getId() == null || projectScheduleRecords.getId().isEmpty()) {
            projectScheduleRecords.setId(IdUtils.fastSimpleUUID());
        }
        projectScheduleRecords.setCreateTime(DateUtils.getNowDate());
        
        // ✅ 修改：存储用户ID而非用户名
        projectScheduleRecords.setCreateBy(SecurityUtils.getUserId().toString());
        
        return projectScheduleRecordsMapper.insertProjectScheduleRecords(projectScheduleRecords);
    }

    @Override
    public int updateProjectScheduleRecords(ProjectScheduleRecords projectScheduleRecords)
    {
        projectScheduleRecords.setUpdateTime(DateUtils.getNowDate());
        
        // ✅ 修改：存储用户ID而非用户名
        projectScheduleRecords.setUpdateBy(SecurityUtils.getUserId().toString());
        
        return projectScheduleRecordsMapper.updateProjectScheduleRecords(projectScheduleRecords);
    }

    @Override
    public int deleteProjectScheduleRecordsByIds(String[] ids)
    {
        return projectScheduleRecordsMapper.deleteProjectScheduleRecordsByIds(ids);
    }

    @Override
    public int deleteProjectScheduleRecordsById(String id)
    {
        return projectScheduleRecordsMapper.deleteProjectScheduleRecordsById(id);
    }
}
```

### 3.2 目标2: 扩展操作权限（Web模块）

#### 修改文件2: ProjectScheduleRecordsServiceImpl.java（完整版）

```java
package com.ruoyi.web.service.impl;

import java.util.List;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import com.ruoyi.web.mapper.ProjectScheduleRecordsMapper;
import com.ruoyi.web.mapper.ProjectMembersMapper;
import com.ruoyi.web.domain.ProjectScheduleRecords;
import com.ruoyi.web.service.IProjectScheduleRecordsService;
import com.ruoyi.common.utils.DateUtils;
import com.ruoyi.common.utils.SecurityUtils;
import com.ruoyi.common.utils.uuid.IdUtils;
import com.ruoyi.common.exception.ServiceException;

@Service
public class ProjectScheduleRecordsServiceImpl implements IProjectScheduleRecordsService 
{
    @Autowired
    private ProjectScheduleRecordsMapper projectScheduleRecordsMapper;
    
    @Autowired
    private ProjectMembersMapper projectMembersMapper;

    @Override
    public ProjectScheduleRecords selectProjectScheduleRecordsById(String id)
    {
        return projectScheduleRecordsMapper.selectProjectScheduleRecordsById(id);
    }

    @Override
    public List<ProjectScheduleRecords> selectProjectScheduleRecordsList(ProjectScheduleRecords projectScheduleRecords)
    {
        return projectScheduleRecordsMapper.selectProjectScheduleRecordsList(projectScheduleRecords);
    }

    @Override
    public int insertProjectScheduleRecords(ProjectScheduleRecords projectScheduleRecords)
    {
        // 如果 id 为空，自动生成 UUID
        if (projectScheduleRecords.getId() == null || projectScheduleRecords.getId().isEmpty()) {
            projectScheduleRecords.setId(IdUtils.fastSimpleUUID());
        }
        projectScheduleRecords.setCreateTime(DateUtils.getNowDate());
        projectScheduleRecords.setCreateBy(SecurityUtils.getUserId().toString());
        
        return projectScheduleRecordsMapper.insertProjectScheduleRecords(projectScheduleRecords);
    }

    @Override
    public int updateProjectScheduleRecords(ProjectScheduleRecords projectScheduleRecords)
    {
        // ✅ 先查询完整记录获取projectId
        ProjectScheduleRecords existingRecord = projectScheduleRecordsMapper
            .selectProjectScheduleRecordsById(projectScheduleRecords.getId());
        
        if (existingRecord == null) {
            throw new ServiceException("记录不存在");
        }
        
        // ✅ 权限验证：检查是否为项目成员
        String currentUserId = SecurityUtils.getUserId().toString();
        String projectId = existingRecord.getProjectId();
        
        if (!isProjectMember(currentUserId, projectId)) {
            throw new ServiceException("只有项目成员可以编辑验收记录");
        }
        
        projectScheduleRecords.setUpdateTime(DateUtils.getNowDate());
        projectScheduleRecords.setUpdateBy(currentUserId);
        
        return projectScheduleRecordsMapper.updateProjectScheduleRecords(projectScheduleRecords);
    }

    @Override
    public int deleteProjectScheduleRecordsByIds(String[] ids)
    {
        // ✅ 批量删除时验证每条记录的权限
        String currentUserId = SecurityUtils.getUserId().toString();
        
        for (String id : ids) {
            ProjectScheduleRecords record = projectScheduleRecordsMapper.selectProjectScheduleRecordsById(id);
            if (record != null) {
                String projectId = record.getProjectId();
                if (!isProjectMember(currentUserId, projectId)) {
                    throw new ServiceException("只有项目成员可以删除验收记录");
                }
            }
        }
        
        return projectScheduleRecordsMapper.deleteProjectScheduleRecordsByIds(ids);
    }

    @Override
    public int deleteProjectScheduleRecordsById(String id)
    {
        // ✅ 单条删除时验证权限
        ProjectScheduleRecords record = projectScheduleRecordsMapper.selectProjectScheduleRecordsById(id);
        
        if (record == null) {
            throw new ServiceException("记录不存在");
        }
        
        String currentUserId = SecurityUtils.getUserId().toString();
        String projectId = record.getProjectId();
        
        if (!isProjectMember(currentUserId, projectId)) {
            throw new ServiceException("只有项目成员可以删除验收记录");
        }
        
        return projectScheduleRecordsMapper.deleteProjectScheduleRecordsById(id);
    }
    
    /**
     * 检查用户是否为项目成员
     */
    private boolean isProjectMember(String userId, String projectId) {
        return projectMembersMapper.checkUserIsProjectMember(userId, projectId);
    }
}
```

#### 新增文件3: ProjectMembersMapper.java（添加方法）

```java
/**
 * 检查用户是否为项目成员
 * 
 * @param userId 用户ID
 * @param projectId 项目ID
 * @return true-是项目成员，false-不是
 */
boolean checkUserIsProjectMember(@Param("userId") String userId, @Param("projectId") String projectId);
```

#### 修改文件4: ProjectMembersMapper.xml（添加SQL）

```xml
<!-- 检查用户是否为项目成员 -->
<select id="checkUserIsProjectMember" resultType="boolean">
    SELECT COUNT(*) > 0
    FROM project_members
    WHERE user_id = #{userId} AND project_id = #{projectId} AND is_active = 1
</select>
```

### 3.3 目标2: 扩展操作权限（App模块）

#### 修改文件5: AppProjectScheduleServiceImpl.java

```java
@Override
public void updateAcceptanceRecord(String token, String projectId, String recordId, AcceptanceRecordRequest request) {
    // 验证Token并检查是否为员工用户
    Map<String, Object> claims = tokenManager.validateToken(extractToken(token));
    String userType = (String) claims.get("userType");
    String userId = claims.get("userId").toString();

    // 仅员工可以编辑验收记录
    if (!"staff".equals(userType)) {
        throw new ServiceException("仅员工可以编辑验收记录", 403);
    }

    // 查询记录是否存在
    ProjectScheduleRecordVO existingRecord = projectScheduleMapper.selectProjectScheduleRecordById(recordId);
    if (existingRecord == null) {
        throw new ServiceException("验收记录不存在", 404);
    }

    // ✅ 修改：检查是否为项目成员（而非仅创建者）
    boolean isMember = dashboardMapper.checkStaffProjectAccess(userId, projectId);
    if (!isMember) {
        throw new ServiceException("只有项目成员可以编辑验收记录", 403);
    }

    // 验证员工是否有权限访问该项目（双重验证）
    boolean hasAccess = dashboardMapper.checkStaffProjectAccess(userId, projectId);
    if (!hasAccess) {
        throw new ServiceException("无权访问该项目", 403);
    }

    try {
        // 对文本字段进行安全清理
        String sanitizedTitle = InputSanitizer.validateAndSanitizeText(request.getAcceptanceTitle(), "验收标题");
        String sanitizedContent = InputSanitizer.validateAndSanitizeContent(request.getAcceptanceContent(), "验收内容");
        String sanitizedAcceptor = InputSanitizer.validateAndSanitizeText(request.getAcceptor(), "验收人");
        String sanitizedImages = InputSanitizer.sanitizeJson(request.getImages());
        
        // 验证图片JSON格式
        if (StringUtils.isNotEmpty(sanitizedImages) && !InputSanitizer.isValidJsonArray(sanitizedImages)) {
            throw new ServiceException("图片数据格式不正确");
        }

        // 解析验收时间
        Date acceptanceTime = null;
        if (StringUtils.isNotEmpty(request.getAcceptanceTime())) {
            try {
                SimpleDateFormat sdf = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
                acceptanceTime = sdf.parse(request.getAcceptanceTime());
            } catch (Exception e) {
                log.warn("解析验收时间失败: {}", request.getAcceptanceTime());
                acceptanceTime = new Date();
            }
        }

        // 更新验收记录
        int rows = projectScheduleMapper.updateAcceptanceRecord(
                recordId,
                sanitizedImages,
                sanitizedTitle,
                sanitizedContent,
                request.getAcceptanceResult(),
                acceptanceTime,
                sanitizedAcceptor,
                userId
        );

        if (rows <= 0) {
            throw new ServiceException("编辑验收记录失败");
        }

        log.info("验收记录编辑成功 - recordId: {}, userId: {}", recordId, userId);
    } catch (ServiceException e) {
        throw e;
    } catch (Exception e) {
        log.error("编辑验收记录异常", e);
        throw new ServiceException("编辑验收记录失败: " + e.getMessage());
    }
}

@Override
public void deleteAcceptanceRecord(String token, String projectId, String recordId) {
    // 验证Token并检查是否为员工用户
    Map<String, Object> claims = tokenManager.validateToken(extractToken(token));
    String userType = (String) claims.get("userType");
    String userId = claims.get("userId").toString();

    // 仅员工可以删除验收记录
    if (!"staff".equals(userType)) {
        throw new ServiceException("仅员工可以删除验收记录", 403);
    }

    // 查询记录是否存在
    ProjectScheduleRecordVO existingRecord = projectScheduleMapper.selectProjectScheduleRecordById(recordId);
    if (existingRecord == null) {
        throw new ServiceException("验收记录不存在", 404);
    }

    // ✅ 修改：检查是否为项目成员（而非仅创建者）
    boolean isMember = dashboardMapper.checkStaffProjectAccess(userId, projectId);
    if (!isMember) {
        throw new ServiceException("只有项目成员可以删除验收记录", 403);
    }

    // 验证员工是否有权限访问该项目（双重验证）
    boolean hasAccess = dashboardMapper.checkStaffProjectAccess(userId, projectId);
    if (!hasAccess) {
        throw new ServiceException("无权访问该项目", 403);
    }

    try {
        // 删除验收记录
        int rows = projectScheduleMapper.deleteAcceptanceRecord(recordId);

        if (rows <= 0) {
            throw new ServiceException("删除验收记录失败");
        }

        log.info("验收记录删除成功 - recordId: {}, userId: {}", recordId, userId);
    } catch (ServiceException e) {
        throw e;
    } catch (Exception e) {
        log.error("删除验收记录异常", e);
        throw new ServiceException("删除验收记录失败: " + e.getMessage());
    }
}
```

### 3.4 前端修改（uni3）

#### 修改文件6: ScheduleRecord.vue

```vue
<script setup>
import { computed, ref, watch } from 'vue'
import { getFullImageUrl } from '@/utils/request'
import { useUserStore } from '@/store/user.js'
import { getProjectMembers } from '@/api/dashboard.js'

const props = defineProps({
  record: {
    type: Object,
    required: true
  },
  projectId: {  // ✅ 新增projectId prop
    type: String,
    required: true
  }
})

const emit = defineEmits(['click', 'preview-images', 'edit', 'delete'])

const userStore = useUserStore()

// 项目成员列表
const projectMembers = ref([])
const membersLoading = ref(false)

// 加载项目成员
const loadProjectMembers = async () => {
  if (!props.projectId) return
  
  membersLoading.value = true
  try {
    const data = await getProjectMembers(props.projectId, { loading: false })
    projectMembers.value = data || []
  } catch (error) {
    console.error('[ScheduleRecord] 加载项目成员失败:', error)
    projectMembers.value = []
  } finally {
    membersLoading.value = false
  }
}

// 是否员工用户
const isStaff = computed(() => userStore.isStaff)

// 是否是项目成员
const isProjectMember = computed(() => {
  if (projectMembers.value.length === 0) {
    return false  // ✅ 默认不允许操作，更安全
  }
  return projectMembers.value.some(member => member.userId === userStore.userId)
})

// 是否可以操作记录（员工 + 项目成员）
const canOperate = computed(() => {
  return isStaff.value && isProjectMember.value
})

// 监听projectId变化
watch(() => props.projectId, (newVal) => {
  if (newVal) {
    loadProjectMembers()
  }
}, { immediate: true })

// ... 其他代码保持不变
</script>

<template>
  <view class="schedule-record">
    <!-- ... 其他内容 ... -->
    
    <!-- 员工操作按钮 -->
    <view v-if="canOperate" class="record-actions" @click.stop>
      <view class="action-btn edit-btn" @click="handleEdit">
        <text>编辑</text>
      </view>
      <view class="action-btn delete-btn" @click="handleDelete">
        <text>删除</text>
      </view>
    </view>
  </view>
</template>
```

---

## 四、总结

### 4.1 原方案问题汇总

| 问题 | 严重程度 | 影响 |
|------|---------|------|
| Web模块未先查询记录获取projectId | 高 | 导致运行时错误 |
| Mapper方法放错位置 | 中 | 代码结构不合理 |
| 前端默认允许操作不安全 | 高 | 安全风险 |
| 数据库字段名可能不一致 | 中 | 需要确认 |

### 4.2 修正后的方案

✅ **目标1完成度**: 100%
- createBy字段统一为用户ID
- 代码修改正确
- 数据迁移方案合理

✅ **目标2完成度**: 95%（需要小幅调整）
- 权限验证逻辑正确
- 需要调整Mapper注入方式
- 前端需要主动加载项目成员

### 4.3 建议实施顺序

1. **阶段1**: 修复createBy字段（1-2小时）
   - 修改Web模块Service
   - 测试新增/更新功能
   - 可选执行数据迁移

2. **阶段2**: 添加Mapper方法（30分钟）
   - 在ProjectMembersMapper添加权限检查方法
   - 测试SQL查询

3. **阶段3**: 修改Web模块权限逻辑（1小时）
   - 修改Service层权限验证
   - 测试编辑/删除功能

4. **阶段4**: 修改App模块权限逻辑（30分钟）
   - 修改权限验证逻辑
   - 测试编辑/删除功能

5. **阶段5**: 修改前端权限判断（1小时）
   - uni3和vue3同步修改
   - 测试按钮显示逻辑

### 4.4 风险提示

⚠️ **高风险点**:
1. 数据库字段名需要确认（`create_by` vs `created_by`）
2. Web模块需要注入ProjectMembersMapper依赖
3. 前端需要确保projectId正确传递

⚠️ **测试重点**:
1. 项目成员可以编辑他人创建的记录
2. 非项目成员无法编辑任何记录
3. 前端按钮显示逻辑正确
4. 批量删除时权限验证正确

---

**审查结论**: 原方案整体思路正确，但存在4个需要修正的问题。修正后可以安全实施。

**审查人**: Kiro AI Assistant  
**审查时间**: 2025-01-15  
**建议**: 按照修正方案实施，预计总耗时3-4小时
