# 微信登录测试完整流程

## 问题解决状态
✅ **AppID不匹配问题已解决**：启用开发模式，跳过微信API调用  
✅ **数据类型错误已修复**：userId字段从Long改为String  
✅ **后端代码已更新**：支持开发模式和数据库存储  

## 当前配置

### 后端配置 (application.yml)
```yaml
app:
  wechat:
    appid: wx607d3206ed1ec9e6  # 与前端匹配
    secret: your_app_secret_here  # 暂时不需要（开发模式）
    dev-mode: true  # 开发模式：使用模拟数据
```

### 开发模式说明
- **跳过微信API调用**：不需要真实的AppSecret
- **生成模拟openid**：基于code生成固定的mock_openid
- **完整功能测试**：可以测试绑定、登录等所有流程
- **数据持久化**：绑定关系正常保存到数据库

## 测试步骤

### 1. 重启后端服务
```bash
cd sb3
java -jar ruoyi-admin/target/ruoyi-admin.jar
```

### 2. 验证服务启动
```bash
curl -X GET "http://192.168.5.102:8080/app/auth/health"
# 预期响应: {"msg":"服务正常运行","code":200}
```

### 3. 测试完整流程

#### 3.1 检查绑定状态（首次）
```bash
curl -X POST "http://192.168.5.102:8080/app/auth/check-openid-binding" \
  -H "Content-Type: application/json" \
  -d '{"code":"test_code_12345","deviceId":"test_device_001"}'
```
**预期响应**：
```json
{
  "msg": "操作成功",
  "code": 200,
  "data": {
    "isBound": false,
    "openid": "mock_openid_6610",
    "userId": null,
    "userType": null,
    "phone": null
  }
}
```

#### 3.2 绑定手机号
```bash
curl -X POST "http://192.168.5.102:8080/app/auth/bind-phone-to-openid" \
  -H "Content-Type: application/json" \
  -d '{"openid":"mock_openid_6610","phone":"13812345678","deviceId":"test_device_001"}'
```
**预期响应**：
```json
{
  "msg": "操作成功",
  "code": 200,
  "data": {
    "accessToken": "eyJ...",
    "refreshToken": "eyJ...",
    "expiresIn": 7200,
    "userType": "customer",
    "userInfo": {
      "id": "C2024110100001",
      "name": "张先生",
      "phone": "138****5678",
      "avatar": null
    },
    "projects": []
  }
}
```

#### 3.3 再次检查绑定状态（已绑定）
```bash
curl -X POST "http://192.168.5.102:8080/app/auth/check-openid-binding" \
  -H "Content-Type: application/json" \
  -d '{"code":"test_code_12345","deviceId":"test_device_001"}'
```
**预期响应**：
```json
{
  "msg": "操作成功",
  "code": 200,
  "data": {
    "isBound": true,
    "openid": "mock_openid_6610",
    "userId": "C2024110100001",
    "userType": "customer",
    "phone": "138****5678"
  }
}
```

#### 3.4 直接登录
```bash
curl -X POST "http://192.168.5.102:8080/app/auth/openid-login" \
  -H "Content-Type: application/json" \
  -d '{"openid":"mock_openid_6610","deviceId":"test_device_001"}'
```
**预期响应**：登录成功，返回Token和用户信息

### 4. 前端测试

#### 4.1 使用测试页面
1. 在微信开发者工具中打开 `/pages/test/wechat-binding`
2. 按顺序点击测试按钮：
   - **获取微信Code** → 应该成功
   - **检查绑定状态** → 应该返回绑定状态
   - **测试绑定手机号** → 输入 13812345678
   - **测试直接登录** → 应该成功登录

#### 4.2 使用正常登录页面
1. 打开 `/pages/login/index-new`
2. 点击"微信登录"按钮
3. 观察流程：
   - 如果未绑定：弹出手机号输入框
   - 如果已绑定：直接登录成功

## 数据库验证

查看绑定记录：
```sql
SELECT * FROM app_wechat_bindings ORDER BY create_time DESC;
```

应该能看到类似记录：
```
id: xxx
open_id: mock_openid_6610
phone: 13812345678
user_id: C2024110100001
user_type: customer
bind_time: 2026-01-19 23:xx:xx
last_login_time: 2026-01-19 23:xx:xx
```

## 错误排查

### 常见错误及解决方案

1. **连接超时**
   - 检查后端服务是否启动
   - 确认IP地址 192.168.5.102:8080 可访问

2. **"微信小程序配置未完成"**
   - 确认 dev-mode: true 已配置
   - 重启后端服务

3. **"用户不存在"**
   - 确认测试手机号在数据库中存在
   - 使用 13812345678 (张先生) 进行测试

4. **前端网络错误**
   - 确认微信开发者工具勾选"不校验合法域名"
   - 检查控制台网络请求日志

## 生产环境配置

当需要部署到生产环境时：
1. 获取真实的微信AppSecret
2. 设置 `dev-mode: false`
3. 更新 `secret: 真实的AppSecret`
4. 重新部署服务

## 下一步测试

完成基础功能测试后，可以测试：
1. 多个不同手机号的绑定
2. 错误手机号的处理
3. 重复绑定的防护
4. Token刷新机制
5. 登出功能