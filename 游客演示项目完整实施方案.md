# 游客演示项目完整实施方案

## 一、方案概述

本方案实现了游客用户查看演示项目数据的完整功能，包括：
- 服务端字典配置管理演示项目
- 服务端API提供演示项目配置
- 服务端权限验证确保游客只能访问演示项目
- 客户端动态获取演示项目配置
- 设计页面和日志页面支持游客查看

---

## 二、服务端实现

### 2.1 新增文件列表

| 文件路径 | 说明 |
|---------|------|
| `sb3/evs-home/src/main/java/com/ruoyi/app/service/IGuestConfigService.java` | 游客配置服务接口 |
| `sb3/evs-home/src/main/java/com/ruoyi/app/service/impl/GuestConfigServiceImpl.java` | 游客配置服务实现 |
| `sb3/evs-home/src/main/java/com/ruoyi/app/controller/AppGuestConfigController.java` | 游客配置API控制器 |

### 2.2 修改文件列表

| 文件路径 | 修改内容 |
|---------|---------|
| `sb3/evs-home/src/main/java/com/ruoyi/app/enums/UserTypeEnum.java` | 添加GUEST游客类型 |
| `sb3/evs-home/src/main/java/com/ruoyi/app/security/AppContext.java` | 添加isGuest()方法 |
| `sb3/evs-home/src/main/java/com/ruoyi/app/service/impl/AppDashboardServiceImpl.java` | 添加游客权限验证 |
| `sb3/evs-home/src/main/java/com/ruoyi/app/service/impl/AppProjectScheduleServiceImpl.java` | 添加游客权限验证 |

### 2.3 字典配置

需要在系统字典中添加 `guest_demo` 字典类型：

**字典类型：** `guest_demo`
**字典名称：** 游客演示项目配置

**字典数据：**

| 字典标签 | 字典键值 | 排序 | 状态 | 备注 |
|---------|---------|------|------|------|
| projects_01 | 实际的项目ID | 1 | 正常 | 默认演示项目 |
| projects_02 | 实际的项目ID | 2 | 正常 | 备用演示项目（可选） |
| projects_03 | 实际的项目ID | 3 | 正常 | 备用演示项目（可选） |

**配置步骤：**

1. 登录后台管理系统
2. 进入 系统管理 > 字典管理
3. 点击"新增"按钮
4. 填写字典类型信息：
   - 字典名称：游客演示项目配置
   - 字典类型：guest_demo
   - 状态：正常
   - 备注：用于配置游客用户可访问的演示项目ID
5. 保存后，点击"字典数据"按钮
6. 添加字典数据：
   - 字典标签：projects_01
   - 字典键值：[实际的演示项目ID]
   - 排序：1
   - 状态：正常
7. 可以继续添加 projects_02, projects_03 等

### 2.4 API接口

#### 2.4.1 获取游客演示项目配置

**接口地址：** `GET /app/guest/demo-projects`

**请求参数：** 无

**响应示例：**
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "projectIds": [
      "9fa800b545b445e4b699b1598bec4619",
      "另一个项目ID"
    ],
    "defaultProjectId": "9fa800b545b445e4b699b1598bec4619",
    "count": 2
  }
}
```

**说明：**
- 无需认证，公开访问
- 从字典 `guest_demo` 中读取配置
- `defaultProjectId` 为 `projects_01` 对应的项目ID
- 如果没有 `projects_01`，返回第一个可用的项目ID

#### 2.4.2 检查项目是否为演示项目

**接口地址：** `GET /app/guest/check-demo-project/{projectId}`

**请求参数：**
- `projectId`: 项目ID（路径参数）

**响应示例：**
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "projectId": "9fa800b545b445e4b699b1598bec4619",
    "isDemo": true
  }
}
```

### 2.5 权限验证逻辑

#### 2.5.1 Dashboard服务权限验证

```java
private void validateTokenAndAccess(String token, String projectId) {
    Map<String, Object> claims = tokenManager.validateToken(extractToken(token));
    String userType = (String) claims.get("userType");
    String userId = claims.get("userId").toString();

    // 游客用户：只能访问演示项目
    if ("guest".equals(userType)) {
        boolean hasAccess = guestConfigService.validateGuestProjectAccess(projectId);
        if (!hasAccess) {
            log.warn("[权限验证] 游客用户 {} 尝试访问非演示项目: {}", userId, projectId);
            throw new ServiceException("游客只能访问演示项目");
        }
        log.info("[权限验证] 游客用户 {} 访问演示项目: {}", userId, projectId);
        return;
    }

    // 客户和员工的权限验证...
}
```

**验证流程：**
1. 解析Token获取用户类型
2. 如果是游客用户（userType='guest'）
3. 调用 `guestConfigService.validateGuestProjectAccess(projectId)`
4. 检查projectId是否在字典配置的演示项目列表中
5. 如果不在列表中，抛出异常"游客只能访问演示项目"
6. 如果在列表中，允许访问

#### 2.5.2 ProjectSchedule服务权限验证

与Dashboard服务相同的验证逻辑，确保游客只能访问演示项目的施工进度数据。

### 2.6 安全隔离

**数据隔离：**
- 游客用户只能访问字典配置的演示项目
- 无法访问其他客户的项目数据
- 无法进行任何写操作（验收上报、问题上报等）

**权限隔离：**
- 前端：游客用户不显示员工专属功能按钮
- 后端：即使前端绕过，后端也会验证用户类型和项目权限

**日志记录：**
- 记录游客用户的访问行为
- 记录游客尝试访问非演示项目的行为
- 便于监控和审计

---

## 三、客户端实现

### 3.1 新增/修改文件列表

| 文件路径 | 修改内容 |
|---------|---------|
| `uni3/src/config/guest.js` | 重写为从服务端动态获取配置 |
| `uni3/src/pages/design/index.vue` | 支持异步获取项目ID |
| `uni3/src/pages/log/index.vue` | 支持异步获取项目ID |

### 3.2 配置模块 (guest.js)

**核心功能：**

1. **动态加载配置**
   - 从服务端API获取演示项目配置
   - 缓存配置避免重复请求
   - 支持并发请求的等待机制

2. **游客判断**
   ```javascript
   export const isGuestUser = () => {
     const guestMode = uni.getStorageSync('guestMode') === true
     const userType = uni.getStorageSync('userType')
     const isGuestType = userType === 'guest'
     return guestMode || isGuestType
   }
   ```

3. **获取项目ID**
   ```javascript
   export const getCurrentProjectId = async (urlProjectId, storeProjectId) => {
     // 优先级：URL参数 > Store项目ID > 游客演示项目ID
     if (urlProjectId) return urlProjectId
     if (storeProjectId) return storeProjectId
     
     // 游客用户使用演示项目ID
     const guestProjectId = await getGuestProjectId()
     if (guestProjectId) return guestProjectId
     
     return ''
   }
   ```

4. **预加载配置**
   ```javascript
   export const preloadGuestConfig = async () => {
     if (isGuestUser()) {
       console.log('[Guest Config] 检测到游客用户，预加载配置')
       await loadGuestConfig()
     }
   }
   ```

### 3.3 设计页面修改

**关键修改：**

1. **异步初始化项目ID**
   ```javascript
   const currentProjectId = ref('')
   
   const initProjectId = async () => {
     const projectId = await getCurrentProjectId(urlProjectId.value, userStore.currentProjectId)
     currentProjectId.value = projectId
   }
   ```

2. **页面加载时初始化**
   ```javascript
   onShow(async () => {
     await initProjectId()
     loadRooms()
   })
   ```

3. **监听项目切换**
   ```javascript
   watch(() => userStore.currentProjectId, async (newId, oldId) => {
     if (newId !== oldId) {
       searchKeyword.value = ''
       await initProjectId()
       loadRooms()
     }
   })
   ```

### 3.4 日志页面修改

与设计页面相同的修改逻辑，确保异步获取项目ID。

### 3.5 数据流程

```
应用启动
  ↓
检测到游客用户
  ↓
调用 preloadGuestConfig()
  ↓
请求 /app/guest/demo-projects
  ↓
缓存演示项目配置
  ↓
用户访问设计/日志页面
  ↓
调用 getCurrentProjectId()
  ↓
返回演示项目ID
  ↓
使用演示项目ID调用API
  ↓
后端验证游客权限
  ↓
返回演示项目数据
  ↓
页面展示数据
```

---

## 四、部署步骤

### 4.1 后端部署

1. **编译代码**
   ```bash
   cd sb3
   mvn clean package
   ```

2. **配置字典**
   - 登录后台管理系统
   - 添加 `guest_demo` 字典类型
   - 添加演示项目ID到字典数据

3. **重启服务**
   ```bash
   # 根据实际部署方式重启
   systemctl restart evs-home
   ```

4. **验证API**
   ```bash
   curl https://hsdlp.gzcelestial.com/prod-api/app/guest/demo-projects
   ```

### 4.2 前端部署

1. **编译代码**
   ```bash
   cd uni3
   npm run build:mp-weixin
   ```

2. **上传小程序**
   - 使用微信开发者工具打开 `uni3/dist/build/mp-weixin`
   - 上传代码到微信平台
   - 提交审核

3. **测试验证**
   - 以游客身份登录
   - 访问设计页面，确认能看到演示项目数据
   - 访问日志页面，确认能看到演示项目数据
   - 确认不能进行编辑操作

---

## 五、测试清单

### 5.1 后端测试

- [ ] 字典配置正确添加
- [ ] API `/app/guest/demo-projects` 返回正确数据
- [ ] API `/app/guest/check-demo-project/{projectId}` 正确判断
- [ ] 游客用户能访问演示项目的设计数据
- [ ] 游客用户能访问演示项目的日志数据
- [ ] 游客用户不能访问非演示项目
- [ ] 客户用户不受影响，能访问自己的项目
- [ ] 员工用户不受影响，能访问负责的项目

### 5.2 前端测试

- [ ] 游客用户能看到设计页面的演示数据
- [ ] 游客用户能看到日志页面的演示数据
- [ ] 游客用户能查看设计图大图
- [ ] 游客用户能查看施工进度详情
- [ ] 游客用户不能进行编辑操作
- [ ] 正常用户看到自己的项目数据
- [ ] 正常用户的项目切换功能正常
- [ ] 员工用户的项目选择功能正常

### 5.3 安全测试

- [ ] 游客用户无法通过修改请求访问非演示项目
- [ ] 游客用户无法进行写操作
- [ ] 后端日志正确记录游客访问行为
- [ ] 后端日志记录游客尝试访问非演示项目的行为

---

## 六、监控和维护

### 6.1 监控指标

1. **API调用监控**
   - `/app/guest/demo-projects` 调用次数
   - `/app/dashboard/project/{projectId}/rooms` 游客调用次数
   - `/app/projectSchedules/list` 游客调用次数

2. **权限验证监控**
   - 游客访问演示项目成功次数
   - 游客尝试访问非演示项目次数
   - 权限验证失败次数

3. **性能监控**
   - API响应时间
   - 字典查询性能
   - 缓存命中率

### 6.2 日志分析

**关键日志：**
```
[游客配置] 获取到 N 个演示项目ID: [...]
[权限验证] 游客用户 xxx 访问演示项目: xxx
[权限验证] 游客用户 xxx 尝试访问非演示项目: xxx
```

**分析要点：**
- 游客用户访问频率
- 游客用户访问的页面分布
- 游客用户尝试访问非演示项目的频率
- 演示项目数据的受欢迎程度

### 6.3 维护任务

1. **定期更新演示数据**
   - 确保演示项目数据完整
   - 更新设计图保持新鲜度
   - 更新施工进度展示最新功能

2. **监控演示项目状态**
   - 确保演示项目存在且有效
   - 检查演示项目数据完整性
   - 验证演示项目不包含敏感信息

3. **字典配置维护**
   - 定期检查字典配置是否正确
   - 根据需要添加或删除演示项目
   - 确保 projects_01 始终指向最佳演示项目

---

## 七、常见问题

### 7.1 游客看不到数据

**可能原因：**
1. 字典配置未添加或配置错误
2. 演示项目ID不存在
3. 演示项目无数据
4. 网络问题导致配置加载失败

**排查步骤：**
1. 检查后台字典配置是否正确
2. 访问 `/app/guest/demo-projects` 确认返回数据
3. 检查演示项目是否存在且有数据
4. 查看浏览器控制台和后端日志

### 7.2 正常用户看到演示数据

**可能原因：**
1. 用户类型判断错误
2. 缓存问题

**排查步骤：**
1. 检查用户的 `userType` 是否正确
2. 清除应用缓存重新登录
3. 检查 `isGuestUser()` 函数逻辑

### 7.3 API调用失败

**可能原因：**
1. 服务端未部署新代码
2. 字典配置未添加
3. 网络问题

**排查步骤：**
1. 确认服务端代码已部署
2. 确认字典配置已添加
3. 检查网络连接
4. 查看后端日志

---

## 八、优化建议

### 8.1 短期优化

1. **添加演示标识**
   - 在页面顶部显示"演示模式"标识
   - 提醒用户当前查看的是演示数据

2. **优化加载体验**
   - 添加加载动画
   - 优化首次加载速度
   - 添加配置加载失败的友好提示

3. **添加引导功能**
   - 在演示页面添加"立即注册"按钮
   - 引导游客用户注册成为正式用户

### 8.2 长期优化

1. **多演示项目支持**
   - 支持游客切换查看不同演示项目
   - 展示不同风格的装修案例

2. **演示数据缓存**
   - 缓存演示项目数据到本地
   - 减少API调用次数
   - 提升加载速度

3. **访问统计分析**
   - 统计游客查看演示数据的行为
   - 分析用户兴趣点
   - 优化演示内容

4. **A/B测试**
   - 测试不同演示项目的转化率
   - 优化演示内容吸引力
   - 提高游客转化率

---

## 九、总结

### 9.1 方案优势

✅ **服务端配置管理** - 通过字典灵活配置演示项目
✅ **动态获取配置** - 客户端动态获取，无需硬编码
✅ **权限安全隔离** - 后端严格验证，确保数据安全
✅ **不影响现有功能** - 正常用户和员工用户不受影响
✅ **易于维护** - 通过后台管理系统即可配置
✅ **易于扩展** - 可以轻松添加更多演示项目

### 9.2 技术亮点

1. **字典驱动配置** - 使用系统字典管理演示项目，灵活可配置
2. **双重权限验证** - 前端判断 + 后端验证，确保安全
3. **异步配置加载** - 客户端动态获取配置，支持热更新
4. **优雅降级** - 配置加载失败时不影响正常用户使用

### 9.3 注意事项

⚠️ **配置演示项目** - 必须在字典中正确配置演示项目ID
⚠️ **选择合适的演示数据** - 确保数据适合公开展示
⚠️ **定期更新演示数据** - 保持演示内容的新鲜度
⚠️ **监控API调用** - 避免演示项目API被过度调用

---

**文档版本：** 2.0  
**创建时间：** 2026-02-13  
**最后更新：** 2026-02-13  
**维护人员：** Kiro AI Assistant
