# 时间轴排序逻辑修复说明（升序版本）

## 问题分析

### 1. 原有排序逻辑复杂
- 使用降序排列，`stageOrder` 值越大越靠前，不够直观
- 上移下移逻辑复杂，容易产生排序值重复
- 新阶段添加位置不可预测

### 2. 新设计思路
采用更直观的升序排列方式：
- **值小的排在前面**（时间轴上方）
- **升序排列**：`stageOrder` 从小到大
- **第一个节点从 100 开始**
- **新增阶段每次 +100**，确保有足够间隔

## 修复方案

### 1. 升序排列逻辑
```javascript
// 升序排序：stageOrder 越小越靠前（上方）
.sort((a, b) => (a.stageOrder || 0) - (b.stageOrder || 0))
```

### 2. 新阶段排序值计算
```javascript
if (!componentItem.id) {
  if (timelineItems.value.length === 0) {
    // 如果没有现有阶段，从100开始
    stageOrder = 100
  } else {
    // 获取现有阶段的最大排序值，新阶段+100
    const maxOrder = Math.max(...timelineItems.value.map(item => item.stageOrder || 0), 0)
    stageOrder = maxOrder + 100
  }
}
```

### 3. 上移下移逻辑
```javascript
// 上移：交换当前项与前一项的stageOrder值
// 下移：交换当前项与后一项的stageOrder值
const tempOrder = currentItem.stageOrder
const newCurrentOrder = prevItem.stageOrder  // 或 nextItem.stageOrder
const newPrevOrder = tempOrder
```

## 排序规则说明

### 显示顺序
- `stageOrder` 值越小，在时间轴中显示越靠前（上方）
- 使用升序排序：`(a.stageOrder || 0) - (b.stageOrder || 0)`

### 排序值分配
- **第一个阶段**：`stageOrder = 100`
- **第二个阶段**：`stageOrder = 200`
- **第三个阶段**：`stageOrder = 300`
- **以此类推**：每次增加 100

### 新阶段位置
- 新添加的阶段自动获得比现有所有阶段更大的 `stageOrder` 值
- 确保新阶段始终显示在时间轴的最下方位置
- 100 的间隔确保有足够空间用于未来的插入操作

### 上移下移操作
- **上移**：当前项目与前一项目交换 `stageOrder` 值（获得更小的值）
- **下移**：当前项目与后一项目交换 `stageOrder` 值（获得更大的值）
- 简单交换，避免复杂计算和排序值重复问题

## 优势

1. **直观性**：数值小的在前面，符合常规理解
2. **可扩展性**：100 的间隔为未来插入操作预留空间
3. **简单性**：排序逻辑简单，易于理解和维护
4. **稳定性**：避免了复杂计算导致的排序值冲突

## 修复文件

- `vue3/src/views/evs/projects/components/ProjectProgress.vue`
  - 修改 `convertFromBackendData` 函数使用升序排列
  - 修改 `convertToBackendData` 函数的新阶段排序逻辑
  - 更新 `handleMoveUp` 和 `handleMoveDown` 函数的注释说明

## 测试建议

1. **新阶段添加测试**：
   - 添加第一个阶段，验证 `stageOrder = 100`
   - 添加第二个阶段，验证 `stageOrder = 200`
   - 验证新阶段出现在时间轴最下方

2. **排序操作测试**：
   - 测试上移功能（获得更小的 stageOrder 值）
   - 测试下移功能（获得更大的 stageOrder 值）
   - 验证排序交换的正确性

3. **边界情况测试**：
   - 空时间轴添加第一个阶段
   - 只有一个阶段时的操作限制
   - 多个阶段的复杂排序操作

## 示例

假设有 3 个阶段：
- 阶段A：`stageOrder = 100`（显示在最上方）
- 阶段B：`stageOrder = 200`（显示在中间）
- 阶段C：`stageOrder = 300`（显示在最下方）

添加新阶段D：`stageOrder = 400`（显示在最下方）

上移阶段C：
- 阶段C 的 `stageOrder` 变为 200
- 阶段B 的 `stageOrder` 变为 300
- 结果：A(100) → C(200) → B(300) → D(400)