# 质量检查项目切换问题修复报告

## 问题描述

在质量检查功能中，点击左侧项目列表切换项目时：
- ✅ 右侧项目基础信息正常刷新
- ❌ 下面的问题列表数据没有刷新
- ❌ 问题列表显示的是所有项目的问题，而不是当前选中项目的问题

---

## 问题分析

### 数据流程

```
用户点击项目
    ↓
selectProject(project)
    ↓
selectedProject.value = project  ← 基础信息更新 ✅
    ↓
loadProjectInspections(projectId)
    ↓
inspectionIssuesMap.value.set(...)  ← 添加新项目的问题
    ↓
allIssues (computed)
    ↓
从 inspectionIssuesMap 提取所有问题  ← 问题所在 ❌
```

### 根本原因

**问题代码**:
```javascript
// 计算所有问题列表（从所有质检记录中提取）
const allIssues = computed(() => {
  const issues = []
  inspectionIssuesMap.value.forEach((issueList) => {
    issues.push(...issueList)  // ❌ 提取Map中的所有问题
  })
  return issues.sort((a, b) => new Date(b.createdAt) - new Date(a.createdAt))
})
```

**问题分析**:
1. `inspectionIssuesMap` 是一个 Map，存储 `质检记录ID → 问题列表` 的映射
2. 当切换项目时，`loadProjectInspections` 会向 Map 中**添加**新项目的数据
3. 但是，**旧项目的数据没有被清除**
4. `allIssues` 计算属性会遍历整个 Map，提取**所有**问题
5. 结果：问题列表包含了所有项目的问题，而不仅仅是当前项目的

### 数据累积示例

```javascript
// 初始状态
inspectionIssuesMap = {}

// 选择项目A
selectProject(projectA)
  → loadProjectInspections(projectA.id)
  → inspectionIssuesMap = {
      inspection1: [issue1, issue2],  // 项目A的问题
      inspection2: [issue3]
    }
  → allIssues = [issue1, issue2, issue3]  ✅ 正确

// 切换到项目B（修复前）
selectProject(projectB)
  → loadProjectInspections(projectB.id)
  → inspectionIssuesMap = {
      inspection1: [issue1, issue2],  // 项目A的问题（未清除）
      inspection2: [issue3],          // 项目A的问题（未清除）
      inspection3: [issue4, issue5]   // 项目B的问题（新增）
    }
  → allIssues = [issue1, issue2, issue3, issue4, issue5]  ❌ 错误！包含了项目A的问题

// 切换到项目B（修复后）
selectProject(projectB)
  → inspectionIssuesMap.clear()  // 清空旧数据
  → loadProjectInspections(projectB.id)
  → inspectionIssuesMap = {
      inspection3: [issue4, issue5]   // 只有项目B的问题
    }
  → allIssues = [issue4, issue5]  ✅ 正确！
```

---

## 解决方案

### 修复代码

**修改前**:
```javascript
/** 选择项目 */
function selectProject(project) {
  selectedProject.value = project
  loadProjectInspections(project.id)
}
```

**修改后**:
```javascript
/** 选择项目 */
function selectProject(project) {
  selectedProject.value = project
  
  // 清空之前项目的数据
  inspectionItems.value = []
  inspectionIssuesMap.value.clear()
  issueFixesMap.value.clear()
  
  // 加载新项目的数据
  loadProjectInspections(project.id)
}
```

### 修复说明

**清空的数据**:
1. `inspectionItems.value = []`
   - 清空质检记录列表
   - 避免在加载新数据前显示旧数据

2. `inspectionIssuesMap.value.clear()`
   - 清空质检记录ID → 问题列表的映射
   - **核心修复**：确保 `allIssues` 只包含当前项目的问题

3. `issueFixesMap.value.clear()`
   - 清空问题ID → 整改记录列表的映射
   - 避免在问题详情抽屉中显示其他项目的整改记录

---

## 修复效果

### 修复前

```
用户操作：项目A → 项目B → 项目C

问题列表显示：
项目A: [问题1, 问题2, 问题3]
项目B: [问题1, 问题2, 问题3, 问题4, 问题5]  ← 包含项目A的问题
项目C: [问题1, 问题2, 问题3, 问题4, 问题5, 问题6, 问题7]  ← 包含项目A和B的问题
```

### 修复后

```
用户操作：项目A → 项目B → 项目C

问题列表显示：
项目A: [问题1, 问题2, 问题3]  ✅
项目B: [问题4, 问题5]  ✅
项目C: [问题6, 问题7]  ✅
```

---

## 相关数据结构

### inspectionIssuesMap
```typescript
// 类型定义
Map<inspectionId: string, issues: Issue[]>

// 示例数据
{
  "inspection-001": [
    { id: "issue-001", title: "墙面问题", ... },
    { id: "issue-002", title: "水管问题", ... }
  ],
  "inspection-002": [
    { id: "issue-003", title: "电线问题", ... }
  ]
}
```

### issueFixesMap
```typescript
// 类型定义
Map<issueId: string, fixes: Fix[]>

// 示例数据
{
  "issue-001": [
    { id: "fix-001", description: "已修复", ... },
    { id: "fix-002", description: "二次整改", ... }
  ],
  "issue-002": [
    { id: "fix-003", description: "已完成", ... }
  ]
}
```

### allIssues (computed)
```typescript
// 计算逻辑
const allIssues = computed(() => {
  const issues = []
  inspectionIssuesMap.value.forEach((issueList) => {
    issues.push(...issueList)
  })
  return issues.sort((a, b) => new Date(b.createdAt) - new Date(a.createdAt))
})

// 输出：当前项目的所有问题，按创建时间倒序排列
```

---

## 测试场景

### 场景1: 切换项目
1. 打开质量检查页面
2. 点击项目A
3. **预期**: 显示项目A的问题列表
4. 点击项目B
5. **预期**: 显示项目B的问题列表（不包含项目A的问题）

### 场景2: 多次切换
1. 依次点击项目A → 项目B → 项目C → 项目A
2. **预期**: 每次切换都只显示当前项目的问题

### 场景3: 空项目
1. 点击一个没有质量问题的项目
2. **预期**: 显示"暂无质量问题"的空状态

### 场景4: 筛选和排序
1. 点击项目A
2. 使用筛选功能（待处理/整改中/已解决）
3. 切换到项目B
4. **预期**: 项目B的问题列表正确显示，筛选状态保持

---

## 潜在的其他问题

### 1. 初始加载
**问题**: 页面首次加载时，如果有默认选中的项目，是否正确加载？

**检查代码**:
```javascript
// 在 getList() 函数中
if (inProgressProjects.value.length > 0) {
  const firstProject = inProgressProjects.value[0]
  selectedProject.value = firstProject
  loadProjectInspections(firstProject.id)  // ✅ 正确
}
```

**结论**: 初始加载逻辑正确。

### 2. 搜索和筛选
**问题**: 搜索或筛选项目后，是否会影响问题列表？

**检查代码**:
```javascript
function handleQuery() {
  queryParams.value.pageNum = 1
  getList()  // 重新加载项目列表
}
```

**结论**: 搜索后会重新加载项目列表，如果当前选中的项目不在新列表中，会清空选择。逻辑正确。

---

## 文件清单

### 修改文件
- ✅ `vue3/src/views/evs/qualityInspections/card.vue` - 修复 `selectProject` 函数

### 修改内容
- 在 `selectProject` 函数中添加数据清空逻辑
- 清空 `inspectionItems`、`inspectionIssuesMap`、`issueFixesMap`

---

## 总结

### 问题根源
切换项目时，旧项目的问题数据没有被清除，导致 `allIssues` 计算属性包含了所有项目的问题。

### 解决方案
在 `selectProject` 函数中，加载新项目数据前，先清空所有相关的数据缓存。

### 修复效果
- ✅ 切换项目时，问题列表正确刷新
- ✅ 只显示当前项目的问题
- ✅ 避免数据累积和混淆
- ✅ 提升用户体验和数据准确性

### 代码改动
- 新增3行代码清空数据缓存
- 无破坏性修改
- 向后兼容

---

## 修复完成时间
2024-12-05
