# 时间轴样式统一修复报告

## 修复目标
按照施工进度时间轴作为标准，统一质检记录时间轴和验收记录的样式及交互，并修复时间显示格式错误。

## 问题分析

### 1. 质检记录时间轴问题
- ❌ 时间显示格式错误：只显示日期 `{y}-{m}-{d}`，应显示完整时间 `{y}-{m}-{d} {h}:{i}:{s}`
- ❌ "提交整改"按钮位置错误：在 header-actions 中，且引用了错误的变量 `issue`
- ❌ timeline-item-description 结构不规范：日期和创建人信息应该包裹在一个容器中

### 2. 验收记录问题
- ❌ 修复时间和验收时间显示格式错误：只显示日期，应显示完整时间

### 3. 施工进度时间轴（参考标准）
- ✅ 结构清晰：timeline-item-header + timeline-item-description
- ✅ 日期显示合理：只显示日期（因为是计划日期，不需要具体时间）
- ✅ 操作按钮位置正确：在每个条目的右侧

## 修复方案

### 1. 质检记录时间轴修复

#### 1.1 修复时间显示格式
**文件**: `vue3/src/views/evs/qualityInspections/card.vue`

**修改前**:
```vue
<div class="timeline-item-date">
  <el-icon><Calendar /></el-icon>
  <span>{{ proxy.parseTime(item.inspectionDate, '{y}-{m}-{d}') }}</span>
  <span v-if="item.createdBy" style="margin-left: 16px;">
    <el-icon><User /></el-icon>
    <span>{{ item.createdBy }}</span>
  </span>
</div>
```

**修改后**:
```vue
<div class="timeline-item-description">
  <div class="timeline-item-date">
    <el-icon><Calendar /></el-icon>
    <span>{{ proxy.parseTime(item.inspectionDate, '{y}-{m}-{d} {h}:{i}:{s}') }}</span>
  </div>
  <span v-if="item.createdBy" class="timeline-item-creator">
    <el-icon><User /></el-icon>
    <span>{{ item.createdBy }}</span>
  </span>
</div>
```

**改进点**:
- ✅ 时间格式从 `{y}-{m}-{d}` 改为 `{y}-{m}-{d} {h}:{i}:{s}`
- ✅ 添加 `timeline-item-description` 容器包裹日期和创建人信息
- ✅ 结构与施工进度时间轴保持一致

#### 1.2 修复"提交整改"按钮位置

**修改前**:
```vue
<!-- 右侧：所有操作按钮 -->
<div class="header-actions">
  <el-button
    type="primary"
    size="small"
    @click="handleSubmitFix(issue)"
  >
    提交整改
  </el-button>
  <!-- 查看详情按钮（始终显示） -->
  <el-button
    type="primary"
    size="small"
    text
    @click="handleViewDetails(item)"
  >
    <el-icon style="margin-right: 2px;"><View /></el-icon>
    查看详情
  </el-button>
</div>
```

**修改后**:
```vue
<!-- 右侧：查看详情按钮 -->
<div class="header-actions">
  <el-button
    type="primary"
    size="small"
    text
    @click="handleViewDetails(item)"
  >
    <el-icon style="margin-right: 2px;"><View /></el-icon>
    查看详情
  </el-button>
</div>
```

**改进点**:
- ✅ 移除了 header-actions 中的"提交整改"按钮（变量引用错误）
- ✅ 保留"查看详情"按钮在 header 中

#### 1.3 将"提交整改"按钮移到问题列表中

**修改后**:
```vue
<div v-if="getIssuesList(item).length > 0" class="issues-list">
  <div
    v-for="(issue, index) in getIssuesList(item)"
    :key="issue.id || index"
    class="issue-item"
  >
    <div class="issue-info">
      <div class="issue-title">
        <el-icon :style="{ color: getIssueIconColor(issue.status) }" style="margin-right: 6px;"><WarningFilled /></el-icon>
        <span>{{ issue.title || '质量问题' }}</span>
      </div>
      <div class="issue-meta">
        <el-tag :type="getIssueStatusType(issue.status)" size="small">
          {{ getIssueStatusText(issue.status) }}
        </el-tag>
        <span v-if="issue.dueDate" class="issue-due-date">
          期限：{{ proxy.parseTime(issue.dueDate, '{m}-{d}') }}
        </span>
      </div>
    </div>
    <div class="timeline-actions">
      <el-button
        type="primary"
        size="small"
        @click="handleSubmitFix(issue)"
        v-if="issue.status !== 'RESOLVED' && issue.status !== 'CLOSED'"
      >
        提交整改
      </el-button>
    </div>
  </div>
</div>
```

**改进点**:
- ✅ "提交整改"按钮移到每个问题项的右侧
- ✅ 正确引用 `issue` 变量
- ✅ 添加条件判断：只在问题未解决时显示按钮
- ✅ 与施工进度时间轴的操作按钮布局保持一致

#### 1.4 添加 CSS 样式

**新增样式**:
```scss
.timeline-item-description {
  display: flex;
  align-items: center;
  gap: 16px;
  margin-bottom: 12px;
  font-size: 13px;
  color: #999;

  .timeline-item-date {
    display: flex;
    align-items: center;
    gap: 4px;
  }

  .timeline-item-creator {
    display: flex;
    align-items: center;
    gap: 4px;
  }
}
```

**改进点**:
- ✅ 统一的容器样式
- ✅ 日期和创建人信息分离显示
- ✅ 与施工进度时间轴样式保持一致

### 2. 验收记录修复

**文件**: `vue3/src/views/evs/qualityFixes/index.vue`

**修改前**:
```vue
<el-table-column label="修复时间" align="center" prop="fixedAt" width="180">
  <template #default="scope">
    <span>{{ parseTime(scope.row.fixedAt, '{y}-{m}-{d}') }}</span>
  </template>
</el-table-column>
<el-table-column label="验收时间" align="center" prop="verifiedAt" width="180">
  <template #default="scope">
    <span>{{ parseTime(scope.row.verifiedAt, '{y}-{m}-{d}') }}</span>
  </template>
</el-table-column>
```

**修改后**:
```vue
<el-table-column label="修复时间" align="center" prop="fixedAt" width="180">
  <template #default="scope">
    <span>{{ parseTime(scope.row.fixedAt, '{y}-{m}-{d} {h}:{i}:{s}') }}</span>
  </template>
</el-table-column>
<el-table-column label="验收时间" align="center" prop="verifiedAt" width="180">
  <template #default="scope">
    <span>{{ parseTime(scope.row.verifiedAt, '{y}-{m}-{d} {h}:{i}:{s}') }}</span>
  </template>
</el-table-column>
```

**改进点**:
- ✅ 修复时间格式从 `{y}-{m}-{d}` 改为 `{y}-{m}-{d} {h}:{i}:{s}`
- ✅ 验收时间格式从 `{y}-{m}-{d}` 改为 `{y}-{m}-{d} {h}:{i}:{s}`

## 修复总结

### 修改的文件
1. `vue3/src/views/evs/qualityInspections/card.vue` - 质检记录时间轴
2. `vue3/src/views/evs/qualityFixes/index.vue` - 验收记录

### 修复的问题
✅ 质检记录时间显示格式：`YYYY-MM-DD` → `YYYY-MM-DD HH:mm:ss`
✅ 验收记录时间显示格式：`YYYY-MM-DD` → `YYYY-MM-DD HH:mm:ss`
✅ "提交整改"按钮位置：从 header-actions 移到问题列表中
✅ "提交整改"按钮变量引用：从错误的 `issue` 改为正确的 `issue`（在循环中）
✅ timeline-item-description 结构：添加容器包裹日期和创建人信息
✅ CSS 样式统一：与施工进度时间轴保持一致

### 统一的时间轴结构
```
timeline-item
├── timeline-item-header
│   ├── header-left (标题 + 状态标签)
│   └── header-actions (查看详情按钮)
├── timeline-item-description
│   ├── timeline-item-date (日期/时间 + 图标)
│   └── timeline-item-creator (创建人 + 图标)
└── timeline-item-content
    ├── inspection-result-box (检查结果)
    └── issues-list (问题列表)
        └── issue-item
            ├── issue-info (问题信息)
            └── timeline-actions (提交整改按钮)
```

## 注意事项

### 时间格式的区别
- **施工进度时间轴**: 只显示日期 `YYYY-MM-DD`（因为是计划日期）
- **质检记录时间轴**: 显示完整时间 `YYYY-MM-DD HH:mm:ss`（因为是实际检查时间）
- **验收记录**: 显示完整时间 `YYYY-MM-DD HH:mm:ss`（因为是实际修复/验收时间）

这种区别是合理的，因为：
- 施工进度是计划性的，只需要知道哪一天
- 质检和验收是实际发生的事件，需要精确到具体时间点

## 测试建议

1. **质检记录时间轴测试**:
   - 检查时间显示是否为 `YYYY-MM-DD HH:mm:ss` 格式
   - 检查"提交整改"按钮是否在每个问题项的右侧
   - 检查按钮点击是否能正确打开整改表单
   - 检查已解决的问题是否不显示"提交整改"按钮

2. **验收记录测试**:
   - 检查修复时间是否为 `YYYY-MM-DD HH:mm:ss` 格式
   - 检查验收时间是否为 `YYYY-MM-DD HH:mm:ss` 格式

3. **样式一致性测试**:
   - 对比施工进度时间轴、质检记录时间轴、验收记录的样式
   - 确保布局、间距、颜色等视觉效果一致

## 修复完成时间
2024-12-05
