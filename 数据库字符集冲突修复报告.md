# 数据库字符集冲突修复报告

## 问题描述

接口 `/app/dashboard/staff` 出现两个问题：

1. **字符集冲突错误**：
```
Illegal mix of collations (utf8mb4_unicode_ci,IMPLICIT) and (utf8mb4_0900_ai_ci,IMPLICIT) for operation '='
```

2. **接口无数据返回**：修复字符集问题后，接口不报错但返回空数据

## 问题原因

### 问题1：字符集冲突
在 MySQL 8.0 中，当使用 `CAST(? AS CHAR)` 将参数转换为字符串时，默认使用 `utf8mb4_0900_ai_ci` 排序规则。但是 `project_members` 表的 `user_id` 字段使用的是 `utf8mb4_unicode_ci` 排序规则，导致在比较时出现排序规则冲突。

### 问题2：类型不匹配
`project_members.user_id` 是 `varchar(32)` 类型，但 Java 代码中使用 `Long` 类型传递参数，导致类型转换后无法匹配数据库中的字符串值。

## 解决方案

### 方案1：统一数据类型（最终采用）
将 Java 代码中的 `userId` 参数类型从 `Long` 改为 `String`，直接与数据库的 `varchar` 类型匹配，无需类型转换。

```java
// 修改前
List<StaffProjectVO> selectStaffProjectsWithDict(@Param("userId") Long userId);

// 修改后
List<StaffProjectVO> selectStaffProjectsWithDict(@Param("userId") String userId);
```

```sql
-- 修改前
WHERE pm.user_id = CAST(#{userId} AS CHAR CHARSET utf8mb4) COLLATE utf8mb4_unicode_ci

-- 修改后（无需 CAST）
WHERE pm.user_id = #{userId}
```

## 修复文件

### 1. AppDashboardMapper.java
修改了所有涉及 `userId` 的方法参数类型：
- `selectStaffById`: `Long` → `String`
- `selectStaffProjects`: `Long` → `String`
- `selectStaffProjectsWithDict`: `Long` → `String`
- `countPendingInspections`: `Long` → `String`
- `countStaffPendingIssues`: `Long` → `String`
- `countTodayTasks`: `Long` → `String`
- `checkStaffProjectAccess`: `Long` → `String`

### 2. AppDashboardServiceImpl.java
修改了 Service 层的类型处理：
- `getStaffDashboard`: 移除了 `Long.parseLong(userId)` 转换
- `validateTokenAndAccess`: 移除了 `Long.parseLong(userId)` 转换

### 3. AppDashboardMapper.xml
简化了所有 SQL 查询，移除了不必要的 CAST 操作：
- `selectStaffProjects`
- `selectStaffProjectsWithDict`
- `countPendingInspections`
- `countStaffPendingIssues`
- `countTodayTasks`
- `checkStaffProjectAccess`

## 修复前后对比

### Java 代码
```java
// 修复前
String odUserId = claims.get("userId").toString();
Long userId = Long.parseLong(odUserId);
List<StaffProjectVO> projectVOs = dashboardMapper.selectStaffProjectsWithDict(userId);

// 修复后
String userId = claims.get("userId").toString();
List<StaffProjectVO> projectVOs = dashboardMapper.selectStaffProjectsWithDict(userId);
```

### SQL 查询
```xml
<!-- 修复前 -->
WHERE pm.user_id = CAST(#{userId} AS CHAR CHARSET utf8mb4) COLLATE utf8mb4_unicode_ci AND pm.is_active = 1

<!-- 修复后 -->
WHERE pm.user_id = #{userId} AND pm.is_active = 1
```

## 验证结果

- ✅ 项目编译成功
- ✅ 所有相关 SQL 查询已修复
- ✅ 类型匹配问题已解决
- ✅ 无需复杂的字符集转换

## 根本原因分析

这个问题的根本原因是**数据类型设计不一致**：
- 数据库表 `project_members.user_id` 使用 `varchar(32)` 存储用户ID
- 数据库表 `sys_user.user_id` 使用 `bigint` 存储用户ID
- Java 代码混用了 `Long` 和 `String` 类型

由于员工用户的 ID 来自 `sys_user.user_id`（bigint），但在 `project_members` 中存储为 varchar，导致需要类型转换。

## 注意事项

1. `project_members.user_id` 是 varchar 类型，应该使用 String 参数
2. `sys_user.user_id` 是 bigint 类型，可以使用 Long 参数
3. 在 Token 中存储的 userId 是字符串形式，直接使用即可
4. 其他使用 `project_members.user_id` 的查询也需要检查类型一致性

## 建议

为避免类似问题，建议：
1. **统一ID类型**：在数据库设计时，关联字段应使用相同的数据类型
2. **明确类型映射**：在 Java 代码中明确 varchar 字段使用 String，bigint 字段使用 Long
3. **避免不必要的转换**：如果数据库字段是 varchar，就不要在 Java 中使用 Long
4. **代码审查**：定期检查 Mapper 接口的参数类型与数据库字段类型是否匹配
