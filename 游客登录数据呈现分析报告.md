# 游客登录数据呈现分析报告

## 分析概述
本报告分析了 `pages/design/index` (设计页面) 和 `pages/log/index` (日志页面) 在游客登录时的数据呈现情况。

---

## 一、游客登录机制分析

### 1.1 游客登录的实现方式

系统中存在两种游客状态：

1. **未登录游客模式** (`guestMode = true`)
   - 存储标识：`uni.getStorageSync('guestMode') === true`
   - 特点：完全未登录，无token

2. **已登录游客用户** (`userType = 'guest'`)
   - 存储标识：`userStore.userType === 'guest'`
   - 特点：通过微信登录但未绑定后台账户
   - 拥有临时token：`guest_token_` + 时间戳
   - 用户ID：`guest_` + 时间戳

### 1.2 游客登录流程（微信登录未绑定场景）

```javascript
// 位置：uni3/src/pages/login/index-new.vue (第483行)
// 未绑定，游客登录成功
const guestLoginInfo = {
  accessToken: 'guest_token_' + Date.now(),
  refreshToken: 'guest_refresh_token_' + Date.now(),
  userType: 'guest', // 设置为游客类型
  userInfo: {
    id: 'guest_' + Date.now(),
    name: '游客用户',
    phone: '',
    avatar: ''
  },
  projects: [] // 游客用户无项目数据
}
```

---

## 二、设计页面 (pages/design/index) 分析

### 2.1 数据获取流程

```javascript
// 位置：uni3/src/pages/design/index.vue (第227行)
const loadRooms = async () => {
  if (!currentProjectId.value) {
    rooms.value = []
    return
  }
  
  loading.value = true
  try {
    // 调用API获取房间列表
    const data = await getProjectRooms(currentProjectId.value)
    rooms.value = data || []
  } catch (error) {
    console.error('加载房间列表失败:', error)
    rooms.value = []
  } finally {
    loading.value = false
  }
}
```

### 2.2 API调用链路

```
getProjectRooms(projectId)
  ↓
GET /app/dashboard/project/{projectId}/rooms
  ↓
request.js 请求拦截器
  ↓
添加 Authorization: Bearer {token}
添加 X-Project-Id: {currentProjectId}
```

### 2.3 游客登录时的问题分析

#### 问题1：缺少项目ID

**原因：**
- 游客用户的 `projects` 数组为空（见登录流程）
- `currentProjectId` 依赖于 `userStore.currentProjectId` 或 URL参数
- 如果没有项目ID，`loadRooms()` 会直接返回空数组

```javascript
// 位置：uni3/src/pages/design/index.vue (第100-110行)
const currentProjectId = computed(() => urlProjectId.value || userStore.currentProjectId)

const loadRooms = async () => {
  if (!currentProjectId.value) {
    rooms.value = []  // 直接返回空数组
    return
  }
  // ...
}
```

#### 问题2：API权限验证

**可能的后端验证：**
1. Token验证：游客token (`guest_token_*`) 可能不被后端接受
2. 项目权限验证：即使有projectId，后端可能验证用户是否有权访问该项目
3. 用户类型验证：后端可能限制 `userType='guest'` 的用户访问某些接口

**请求失败处理：**
```javascript
// 位置：uni3/src/utils/request.js (第210-217行)
const redirectToLogin = () => {
  const isGuest = uni.getStorageSync('guestMode') === true
  
  if (isGuest) {
    // 游客模式下，不跳转登录页，只是返回错误
    console.log('[Request] 游客模式，API调用失败但不跳转登录页')
    return
  }
  // 正常用户会跳转登录页
}
```

### 2.4 游客看不到数据的根本原因

**主要原因：**
1. ✅ **无项目关联** - 游客用户 `projects = []`，没有 `currentProjectId`
2. ✅ **API直接返回** - 没有projectId时，不会发起API请求
3. ⚠️ **后端权限控制** - 即使传入projectId，后端可能拒绝游客token的请求

**数据流断点：**
```
游客登录 → projects=[] → currentProjectId='' → loadRooms()直接返回 → rooms=[] → 显示空状态
```

---

## 三、日志页面 (pages/log/index) 分析

### 3.1 数据获取流程

```javascript
// 位置：uni3/src/pages/log/index.vue (第95行)
const loadSchedules = async () => {
  if (loading.value) return
  
  // 如果没有项目ID，提示用户先选择项目
  if (!currentProjectId.value) {
    if (userStore.isStaff) {
      uni.showToast({
        title: '请先选择项目',
        icon: 'none'
      })
    }
    schedules.value = []
    return
  }
  
  try {
    loading.value = true
    const data = await getProjectScheduleList()
    if (data && Array.isArray(data)) {
      schedules.value = data
    } else {
      schedules.value = []
    }
  } catch (error) {
    console.error('加载施工进度失败:', error)
    schedules.value = []
  } finally {
    loading.value = false
  }
}
```

### 3.2 API调用链路

```
getProjectScheduleList()
  ↓
GET /app/projectSchedules/list
  ↓
request.js 请求拦截器
  ↓
添加 Authorization: Bearer {token}
添加 X-Project-Id: {currentProjectId}
```

### 3.3 游客登录时的问题分析

#### 问题1：缺少项目ID（同设计页面）

**原因：**
- 游客用户 `projects = []`
- `currentProjectId` 为空
- `loadSchedules()` 直接返回空数组

```javascript
// 位置：uni3/src/pages/log/index.vue (第95-107行)
if (!currentProjectId.value) {
  if (userStore.isStaff) {
    uni.showToast({
      title: '请先选择项目',
      icon: 'none'
    })
  }
  schedules.value = []  // 直接返回空数组
  return
}
```

#### 问题2：API依赖项目ID

**API实现：**
```javascript
// 位置：uni3/src/api/projectSchedule.js (第24行)
export const getProjectScheduleList = async () => {
  const data = await get('/app/projectSchedules/list')
  return data
}
```

**请求头中的项目ID：**
```javascript
// 位置：uni3/src/utils/request.js (第42-48行)
// 添加当前项目ID
const currentProjectId = uni.getStorageSync('currentProjectId')
if (currentProjectId) {
  config.header['X-Project-Id'] = currentProjectId
} else {
  console.warn('[Request] 当前项目ID为空')
}
```

#### 问题3：员工操作按钮的权限控制

```javascript
// 位置：uni3/src/pages/log/components/TimelineNode.vue (第119行)
// 是否员工用户
const isStaff = computed(() => userStore.isStaff)

// 是否可以验收
const canAcceptance = computed(() => {
  const status = props.schedule.status?.toLowerCase()
  return status === 'in_progress' || status === '1' || status === 'completed' || status === '2'
})
```

**按钮显示条件：**
```vue
<!-- 位置：uni3/src/pages/log/components/TimelineNode.vue (第45行) -->
<view v-if="isStaff && canAcceptance" class="node-actions">
  <view class="action-btn" @click="handleAcceptance">
    <text class="action-icon">📋</text>
    <text class="action-text">验收上报</text>
  </view>
  <view class="action-btn issue-btn" @click="handleIssueReport">
    <text class="action-icon">⚠️</text>
    <text class="action-text">问题上报</text>
  </view>
</view>
```

**游客用户：**
- `userStore.isStaff = false` (因为 `userType = 'guest'`)
- 操作按钮不会显示（只读模式）

### 3.4 游客看不到数据的根本原因

**主要原因：**
1. ✅ **无项目关联** - 游客用户 `projects = []`，没有 `currentProjectId`
2. ✅ **API直接返回** - 没有projectId时，不会发起API请求
3. ⚠️ **后端权限控制** - 即使传入projectId，后端可能拒绝游客token的请求
4. ✅ **员工功能隐藏** - 游客用户看不到"验收上报"和"问题上报"按钮

**数据流断点：**
```
游客登录 → projects=[] → currentProjectId='' → loadSchedules()直接返回 → schedules=[] → 显示空状态
```

---

## 四、权限控制机制分析

### 4.1 前端权限判断

```javascript
// 位置：uni3/src/utils/permission.js
export const isGuestMode = () => {
  return uni.getStorageSync('guestMode') === true
}

export const isStaff = () => {
  if (isGuestMode()) return false
  const userStore = useUserStore()
  return userStore.userType === 'staff'
}

export const isCustomer = () => {
  if (isGuestMode()) return true // 游客模式视为客户
  const userStore = useUserStore()
  return userStore.userType === 'customer'
}
```

### 4.2 用户Store中的权限计算

```javascript
// 位置：uni3/src/store/user.js (第42-50行)
// 是否已登录
const isLoggedIn = computed(() => !!token.value)

// 是否客户
const isCustomer = computed(() => userType.value === 'customer')

// 是否员工
const isStaff = computed(() => userType.value === 'staff')
```

**游客用户的权限状态：**
- `isLoggedIn = true` (有token)
- `isCustomer = false` (userType='guest')
- `isStaff = false` (userType='guest')

### 4.3 Dashboard页面的游客处理

```javascript
// 位置：uni3/src/pages/dashboard/index.vue (第220-223行)
} else if (userType.value === 'guest') {
  console.log('[Dashboard] 游客用户，设置空项目数据')
  projects.value = []
  currentProject.value = null
```

---

## 五、总结与结论

### 5.1 设计页面 - 游客看不到数据的原因

| 层级 | 原因 | 影响程度 | 说明 |
|------|------|----------|------|
| **数据层** | 游客用户无项目关联 | ⭐⭐⭐⭐⭐ | `projects = []`，这是根本原因 |
| **逻辑层** | 缺少 `currentProjectId` | ⭐⭐⭐⭐⭐ | 导致 `loadRooms()` 直接返回 |
| **API层** | 未发起API请求 | ⭐⭐⭐⭐ | 没有projectId，不会调用后端 |
| **后端层** | 可能的权限验证 | ⭐⭐⭐ | 即使调用，后端可能拒绝游客token |

**结论：** 游客用户无法看到设计页面数据，主要是因为：
1. 游客账户没有关联任何项目
2. 没有项目ID，前端直接返回空数据，不会发起API请求
3. 即使手动传入项目ID，后端也可能因为权限验证而拒绝请求

### 5.2 日志页面 - 游客看不到数据的原因

| 层级 | 原因 | 影响程度 | 说明 |
|------|------|----------|------|
| **数据层** | 游客用户无项目关联 | ⭐⭐⭐⭐⭐ | `projects = []`，这是根本原因 |
| **逻辑层** | 缺少 `currentProjectId` | ⭐⭐⭐⭐⭐ | 导致 `loadSchedules()` 直接返回 |
| **API层** | 未发起API请求 | ⭐⭐⭐⭐ | 没有projectId，不会调用后端 |
| **后端层** | 可能的权限验证 | ⭐⭐⭐ | 即使调用，后端可能拒绝游客token |
| **UI层** | 员工功能隐藏 | ⭐⭐ | 游客看不到"验收上报"等按钮 |

**结论：** 游客用户无法看到日志页面数据，主要是因为：
1. 游客账户没有关联任何项目
2. 没有项目ID，前端直接返回空数据，不会发起API请求
3. 即使手动传入项目ID，后端也可能因为权限验证而拒绝请求
4. 游客用户无法使用员工专属功能（验收上报、问题上报）

### 5.3 核心问题

**游客登录时看不到数据的核心原因是：**

```
游客登录流程设计
  ↓
projects = [] (无项目关联)
  ↓
currentProjectId = '' (无当前项目)
  ↓
前端逻辑判断：if (!currentProjectId) return
  ↓
不发起API请求
  ↓
显示空状态（"暂无设计图" / "暂无项目进度"）
```

### 5.4 设计意图分析

从代码逻辑来看，这是**有意为之的设计**：

1. **游客用户定位**：游客用户是"未绑定后台账户"的临时用户
2. **数据隔离**：游客用户不应该看到任何项目数据
3. **引导绑定**：通过空状态引导用户进行账户绑定（导入历史数据）
4. **安全考虑**：防止未授权用户访问项目敏感信息

### 5.6 验证方法

如果需要验证后端是否也有权限控制，可以：

1. **手动设置项目ID测试**：
   ```javascript
   // 在浏览器控制台或代码中临时设置
   uni.setStorageSync('currentProjectId', '某个真实的项目ID')
   ```

2. **观察API请求**：
   - 查看是否发起了API请求
   - 查看后端返回的状态码（401/403表示权限问题）
   - 查看返回的错误信息

3. **查看后端日志**：
   - 检查后端是否收到请求
   - 检查后端的权限验证逻辑
   - 确认游客token是否被接受

---

## 六、建议

### 6.1 如果要让游客看到数据

需要修改以下几个方面：

1. **游客登录时关联项目**：
   - 修改登录逻辑，为游客用户分配默认项目或演示项目
   - 或者允许游客用户通过URL参数访问特定项目

2. **后端权限调整**：
   - 允许游客token访问特定的只读接口
   - 或者为游客用户创建专门的演示数据

3. **前端逻辑调整**：
   - 移除 `if (!currentProjectId) return` 的限制
   - 或者为游客用户提供默认的项目ID

### 6.2 如果保持当前设计

当前设计是合理的，建议：

1. **优化空状态提示**：
   - 明确告知游客用户需要绑定账户才能查看数据
   - 提供"立即绑定"按钮，引导用户完成账户关联

2. **添加演示模式**：
   - 为游客用户提供演示数据（不调用真实API）
   - 让游客用户体验功能，但数据是模拟的

---

**分析完成时间：** 2026-02-13
**分析人员：** Kiro AI Assistant
