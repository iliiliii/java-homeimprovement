# 编辑回显只显示一个客户问题修复

**修复时间**: 2026-03-01  
**严重程度**: 🔴 严重  
**问题**: 添加多个客户后，编辑时只回显一个客户

---

## ❌ 问题描述

### 现象
1. 新增项目时选择了 4 个客户
2. 提交成功，数据库中正确保存了 4 个客户
3. 编辑该项目时，客户选择框只显示 1 个客户（主客户）

### 测试数据

**提交时的数据**:
```json
{
  "customerIds": [
    "134b81ce28054cbb85d27a74964f0d5d",
    "1eceebfbb6dd42ed985740efbec86b08",
    "48374b34460847d4b8c4616becbdfaef",
    "5c185b32c45349df93e0c926ac58632b"
  ],
  "primaryCustomerId": "134b81ce28054cbb85d27a74964f0d5d"
}
```

**查询返回的数据**:
```json
{
  "code": 200,
  "data": [
    {
      "customerId": "134b81ce28054cbb85d27a74964f0d5d",
      "isPrimary": true
    }
  ]
}
```

**问题**: 只返回了 1 个客户，但数据库中应该有 4 个客户。

---

## 🔍 问题分析

### 根本原因

在 `ProjectEdit.vue` 的 `handleEdit` 方法中，存在以下问题：

**问题代码**:
```javascript
function handleEdit(row) {
  reset()
  const _id = row.id
  getProjects(_id).then(response => {
    form.value = response.data  // ← 问题：直接赋值覆盖整个对象
    
    // 后续设置 customerIds
    getProjectCustomersList(_id).then(res => {
      form.value.customerIds = projectCustomers.map(pc => pc.customerId)
    })
  })
}
```

### 问题详解

1. **`reset()` 初始化**:
   ```javascript
   form.value = {
     customerIds: [],  // 初始化为空数组
     primaryCustomerId: null
   }
   ```

2. **`form.value = response.data` 覆盖**:
   ```javascript
   // response.data 中没有 customerIds 字段
   // 这行代码会把整个 form.value 替换掉
   form.value = response.data
   // 结果：customerIds 变成 undefined
   ```

3. **后续赋值失效**:
   ```javascript
   // 此时 form.value 已经是一个新对象
   // customerIds 字段不存在或为 undefined
   form.value.customerIds = [...]  // 可能不会触发响应式更新
   ```

### 为什么只显示一个客户？

可能的原因：
1. `form.value = response.data` 覆盖了 `customerIds` 字段
2. 响应式更新失效，Vue 没有检测到 `customerIds` 的变化
3. 组件渲染时 `customerIds` 还是空数组或 undefined

---

## ✅ 修复方案

### 方案：逐字段赋值，避免覆盖

**修改文件**: `vue3/src/views/evs/projects/components/ProjectEdit.vue`

**修复后的代码**:
```javascript
function handleEdit(row) {
  reset()
  const _id = row.id
  
  getProjects(_id).then(response => {
    // ✅ 不要直接赋值，而是逐个字段赋值
    const projectData = response.data
    form.value.id = projectData.id
    form.value.name = projectData.name
    form.value.customerId = projectData.customerId
    form.value.projectCode = projectData.projectCode
    form.value.description = projectData.description
    form.value.address = projectData.address
    form.value.area = projectData.area
    form.value.budget = projectData.budget
    form.value.actualCost = projectData.actualCost
    form.value.startDate = projectData.startDate
    form.value.endDate = projectData.endDate
    form.value.actualEndDate = projectData.actualEndDate
    form.value.status = projectData.status
    form.value.priority = projectData.priority
    form.value.progress = projectData.progress
    // ... 其他字段
    
    // ✅ 保留 customerIds 和 primaryCustomerId 字段
    // 这两个字段在后续的 getProjectCustomersList 中设置
    
    // 处理日期范围
    if (form.value.startDate && form.value.endDate) {
      form.value.dateRange = [form.value.startDate, form.value.endDate]
    }
    
    // 获取项目的所有客户
    getProjectCustomersList(_id).then(res => {
      const projectCustomers = res.data || []
      console.log('获取到的项目客户列表:', projectCustomers)
      
      // ✅ 提取客户ID列表
      form.value.customerIds = projectCustomers.map(pc => pc.customerId)
      console.log('提取的客户ID列表:', form.value.customerIds)
      
      // ✅ 找到主客户
      const primary = projectCustomers.find(pc => pc.isPrimary)
      if (primary) {
        form.value.primaryCustomerId = primary.customerId
        console.log('主客户ID:', form.value.primaryCustomerId)
      }
      
      console.log('最终 customerIds:', form.value.customerIds)
    }).catch(err => {
      console.error('获取项目客户列表失败:', err)
      // 兼容处理
      if (form.value.customerId) {
        form.value.customerIds = [form.value.customerId]
        form.value.primaryCustomerId = form.value.customerId
      }
    })
    
    open.value = true
    title.value = "修改项目信息"
  })
}
```

---

## 🎯 修复要点

### 1. 避免直接赋值

**❌ 错误做法**:
```javascript
form.value = response.data  // 覆盖整个对象
```

**✅ 正确做法**:
```javascript
// 逐字段赋值
form.value.id = response.data.id
form.value.name = response.data.name
// ...
```

### 2. 保留响应式字段

```javascript
// customerIds 和 primaryCustomerId 在 reset() 中初始化
// 不要被 response.data 覆盖
// 在 getProjectCustomersList 回调中设置
```

### 3. 添加调试日志

```javascript
console.log('获取到的项目客户列表:', projectCustomers)
console.log('提取的客户ID列表:', form.value.customerIds)
console.log('主客户ID:', form.value.primaryCustomerId)
```

---

## 🧪 验证步骤

### 1. 新增项目测试

1. 新增项目，选择 4 个客户
2. 提交成功
3. 查看数据库，确认 4 个客户都保存了

### 2. 编辑回显测试

1. 点击"编辑"按钮
2. 打开浏览器控制台，查看日志
3. 确认日志输出：
   ```
   获取到的项目客户列表: [4个客户对象]
   提取的客户ID列表: [4个客户ID]
   主客户ID: xxx
   最终 customerIds: [4个客户ID]
   ```
4. 查看客户选择框，确认显示 4 个客户

### 3. 修改客户测试

1. 在编辑对话框中添加或删除客户
2. 修改主客户
3. 提交
4. 再次编辑，确认修改生效

---

## 📊 对比

### 修复前

```
新增项目：选择 4 个客户 ✅
数据库保存：4 个客户 ✅
编辑回显：只显示 1 个客户 ❌
```

### 修复后

```
新增项目：选择 4 个客户 ✅
数据库保存：4 个客户 ✅
编辑回显：显示 4 个客户 ✅
```

---

## ⚠️ 注意事项

### 1. Vue 响应式原理

直接赋值 `form.value = newObject` 会：
- 替换整个对象
- 可能丢失响应式追踪
- 导致某些字段不更新

**建议**：
- 逐字段赋值
- 或使用 `Object.assign(form.value, newData)`
- 或使用 `form.value = { ...form.value, ...newData }`

### 2. 异步操作顺序

```javascript
getProjects().then(() => {
  // 先设置基本字段
  
  getProjectCustomersList().then(() => {
    // 再设置客户字段
  })
})
```

确保客户字段在基本字段之后设置，避免被覆盖。

### 3. 调试技巧

添加 `console.log` 可以帮助追踪数据流：
```javascript
console.log('步骤1: reset后', form.value.customerIds)
console.log('步骤2: 赋值后', form.value.customerIds)
console.log('步骤3: 获取客户后', form.value.customerIds)
```

---

## ✅ 修复结果

- ✅ 编辑时正确显示所有客户
- ✅ 主客户正确标识
- ✅ 可以修改客户列表
- ✅ 可以修改主客户
- ✅ 数据同步正确

---

**修复时间**: 2026-03-01  
**状态**: ✅ 已修复  
**测试**: 待验证
