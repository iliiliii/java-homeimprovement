# 客户名称显示"-"的情况分析

## 问题

在项目详情页面中，什么情况下客户名称会不存在，显示"-"？

---

## 代码逻辑分析

### 1. ProjectDetail.vue 中的客户名称获取逻辑

```javascript
/** 获取客户名称 */
function getCustomerName(customerId) {
  // 从当前项目数据中获取客户信息
  if (currentProject.value.customer && currentProject.value.customer.name) {
    return currentProject.value.customer.name
  }
  // 没有客户信息时返回空
  return ''
}
```

**显示逻辑**:
```vue
<el-descriptions-item label="客户">
  <span v-if="getCustomerName(currentProject.customerId)">
    {{ getCustomerName(currentProject.customerId) }}
  </span>
  <span v-else style="color: #999;">-</span>
</el-descriptions-item>
```

**结论**: 当 `getCustomerName()` 返回空字符串时，显示"-"

---

## 会显示"-"的情况

### 情况1: 项目没有关联客户 ❌ (理论上不可能)

**数据库约束**:
```prisma
model Project {
  customerId  String  // 必填字段，不能为空
  customer    Customer @relation(fields: [customerId], references: [id], onDelete: Cascade)
}
```

**分析**: 
- `customerId` 是必填字段（没有 `?` 标记）
- 数据库层面强制要求每个项目必须关联一个客户
- **这种情况理论上不会发生**

### 情况2: 项目关联的客户被删除 ⚠️ (可能发生)

**数据库约束**:
```prisma
customer Customer @relation(fields: [customerId], references: [id], onDelete: Cascade)
```

**分析**:
- 使用了 `onDelete: Cascade` 级联删除
- 当客户被删除时，关联的项目也会被自动删除
- **这种情况也不会导致显示"-"，因为项目本身会被删除**

### 情况3: API 没有返回客户信息 ✅ (最可能的原因)

**当前使用的API**:
```javascript
// card.vue 中
listProjectsWithMembers(queryParams.value)
```

**API定义**:
```javascript
// vue3/src/api/evs/projects.js
export function listProjectsWithMembers(query) {
  return request({
    url: '/evs/projects/list',
    method: 'get',
    params: {
      ...query,
      includeProjectMembers: true  // ⚠️ 只包含项目成员，不包含客户信息
    }
  })
}
```

**问题所在**:
- `listProjectsWithMembers` 只设置了 `includeProjectMembers: true`
- **没有设置 `includeCustomer: true`**
- 后端可能不会返回 `customer` 对象

**验证**:
```javascript
// 项目数据结构
{
  id: "xxx",
  name: "项目名称",
  customerId: "customer_123",  // ✅ 有客户ID
  customer: undefined          // ❌ 没有客户对象
}
```

### 情况4: 客户数据中没有 name 字段 ⚠️ (数据异常)

**数据库约束**:
```prisma
model Customer {
  name String  // 必填字段
}
```

**分析**:
- `name` 是必填字段
- 理论上不应该为空
- 但如果数据库中存在脏数据（历史遗留、直接SQL操作等），可能出现这种情况

---

## 根本原因

### 主要原因: API 不返回客户信息

**当前数据流**:
```
1. card.vue 调用 listProjectsWithMembers()
   ↓
2. 后端返回项目列表
   {
     id: "xxx",
     customerId: "customer_123",
     customer: undefined  // ❌ 没有客户对象
   }
   ↓
3. ProjectDetail.vue 接收项目数据
   currentProject.value = project
   ↓
4. getCustomerName() 检查
   currentProject.value.customer === undefined  // ❌
   currentProject.value.customer.name === undefined  // ❌
   ↓
5. 返回空字符串 ''
   ↓
6. 显示 "-"
```

---

## 解决方案

### 方案1: 修改 API 调用，包含客户信息 ✅ (推荐)

**修改 card.vue**:
```javascript
// 当前
listProjectsWithMembers(queryParams.value)

// 修改为
listProjectsWithCustomer(queryParams.value)
// 或者同时包含客户和成员信息
```

**修改 API**:
```javascript
export function listProjectsWithMembers(query) {
  return request({
    url: '/evs/projects/list',
    method: 'get',
    params: {
      ...query,
      includeProjectMembers: true,
      includeCustomer: true  // ✅ 添加这一行
    }
  })
}
```

### 方案2: 后端默认返回客户信息

**修改后端逻辑**:
```java
// ProjectsController.java
@GetMapping("/list")
public TableDataInfo list(Projects projects) {
    List<Projects> list = projectsService.selectProjectsList(projects);
    
    // 默认加载客户信息
    for (Projects project : list) {
        if (project.getCustomerId() != null) {
            Customers customer = customersService.selectCustomersById(project.getCustomerId());
            project.setCustomer(customer);
        }
    }
    
    return getDataTable(list);
}
```

### 方案3: 前端降级处理，显示客户ID

**修改 getCustomerName 函数**:
```javascript
function getCustomerName(customerId) {
  // 优先显示客户名称
  if (currentProject.value.customer && currentProject.value.customer.name) {
    return currentProject.value.customer.name
  }
  // 降级显示客户ID
  if (customerId) {
    return `客户ID: ${customerId}`
  }
  // 最后显示空
  return ''
}
```

---

## 测试验证

### 测试场景1: 正常情况
```javascript
// 项目数据
{
  id: "project_1",
  customerId: "customer_1",
  customer: {
    id: "customer_1",
    name: "张三"
  }
}

// 预期结果: 显示 "张三"
```

### 测试场景2: API 不返回客户信息
```javascript
// 项目数据
{
  id: "project_1",
  customerId: "customer_1",
  customer: undefined  // ❌ API 没有返回
}

// 当前结果: 显示 "-"
// 期望结果: 显示 "张三" 或 "客户ID: customer_1"
```

### 测试场景3: 客户名称为空（数据异常）
```javascript
// 项目数据
{
  id: "project_1",
  customerId: "customer_1",
  customer: {
    id: "customer_1",
    name: ""  // ❌ 数据异常
  }
}

// 当前结果: 显示 "-"
// 期望结果: 显示 "客户ID: customer_1" 或提示数据异常
```

---

## 推荐方案

### 最佳实践: 方案1 + 方案3 组合

**1. 修改 API 调用（方案1）**:
```javascript
// vue3/src/api/evs/projects.js
export function listProjectsWithMembers(query) {
  return request({
    url: '/evs/projects/list',
    method: 'get',
    params: {
      ...query,
      includeProjectMembers: true,
      includeCustomer: true  // ✅ 添加客户信息
    }
  })
}
```

**2. 添加降级处理（方案3）**:
```javascript
// ProjectDetail.vue
function getCustomerName(customerId) {
  // 优先显示客户名称
  if (currentProject.value.customer && currentProject.value.customer.name) {
    return currentProject.value.customer.name
  }
  // 降级显示客户ID（用于调试和异常情况）
  if (customerId) {
    return `客户ID: ${customerId}`
  }
  // 最后显示空
  return ''
}
```

**优点**:
- ✅ 正常情况下显示客户名称
- ✅ API 异常时显示客户ID，便于排查问题
- ✅ 数据异常时也能显示有用信息
- ✅ 提升系统健壮性

---

## 总结

### 显示"-"的根本原因

**最可能的原因**: API 没有返回客户信息（`customer` 对象为 `undefined`）

**发生条件**:
1. 使用 `listProjectsWithMembers` API
2. 后端没有设置 `includeCustomer: true`
3. 后端没有默认加载客户信息

### 建议修复

1. **短期方案**: 修改 API 调用，添加 `includeCustomer: true`
2. **长期方案**: 后端默认返回客户信息，前端添加降级处理
3. **监控方案**: 添加日志，记录客户信息缺失的情况

### 影响范围

- ✅ 项目详情页面
- ✅ 项目列表页面（card.vue 中也使用了 `getCustomerName`）
- ⚠️ 其他使用项目数据的页面

---

**分析日期**: 2025年12月5日  
**分析人员**: Kiro AI Assistant  
**建议优先级**: P1 - 应该修复（影响用户体验）
