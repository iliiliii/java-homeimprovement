# 改用物理删除的完整方案

**修复时间**: 2026-03-01  
**方案**: 将客户关联的删除从软删除改为物理删除

---

## 🔍 问题根源

### 软删除 + 唯一约束 = 冲突

**数据库约束**:
```sql
UNIQUE KEY `uk_project_customer` (`project_id`, `customer_id`)
```

**问题场景**:
1. 项目有客户A
2. 删除客户A（软删除，`deleted_at` 设置为当前时间）
3. 再次添加客户A
4. ❌ 违反唯一约束！因为约束不考虑 `deleted_at` 字段

---

## ✅ 解决方案：改用物理删除

### 优点
- ✅ 简单直接，逻辑清晰
- ✅ 不会有唯一约束冲突
- ✅ 可以随意添加/删除/再添加客户
- ✅ 不需要修改数据库结构

### 缺点
- ❌ 无法保留删除历史
- ❌ 无法恢复误删除的客户
- ❌ 审计日志不完整

### 权衡
对于项目客户关联这种场景，**不需要保留删除历史**，因为：
- 客户关联是可以随时修改的
- 不是敏感数据
- 如果需要审计，可以在应用层记录日志

---

## 📝 修改内容

### 1. 前端修改

**文件**: `vue3/src/views/evs/projects/components/ProjectEdit.vue`

#### 修改 `handleCustomerRelations` 方法

**新增功能**:
- ✅ 支持移除客户
- ✅ 检查不能移除主客户
- ✅ 先移除，再添加，最后更新主客户

**关键代码**:
```javascript
// 1. 先移除客户（物理删除）
if (toRemove.length > 0) {
  for (const customerId of toRemove) {
    await removeCustomerFromProject({
      projectId: projectId,
      customerId: customerId
    })
  }
}

// 2. 添加新客户
if (toAdd.length > 0) {
  await addCustomersToProject({
    projectId: projectId,
    customerIds: toAdd,
    primaryCustomerId: newPrimaryCustomerId
  })
}

// 3. 更新主客户
if (主客户变化) {
  await setPrimaryCustomer({
    projectId: projectId,
    customerId: newPrimaryCustomerId
  })
}
```

---

### 2. 后端修改

#### 2.1 修改 `removeProjectCustomer` 方法

**文件**: `ProjectCustomersServiceImpl.java`

**修改前**:
```java
// 软删除
int result = projectCustomersMapper.softDelete(list.get(0).getId());
```

**修改后**:
```java
// 物理删除（不是软删除）
int result = projectCustomersMapper.deleteProjectCustomersById(list.get(0).getId());
log.info("成功从项目{}移除客户{}（物理删除）", projectId, customerId);
```

---

#### 2.2 简化 `addProjectCustomers` 方法

**文件**: `ProjectCustomersServiceImpl.java`

**修改前**:
```java
// 查询所有记录（包括软删除的）
List<ProjectCustomers> allRecords = projectCustomersMapper.selectAllRecords(projectId, customerId);

// 如果有软删除的记录，先物理删除
if (!allRecords.isEmpty()) {
    for (ProjectCustomers record : allRecords) {
        if (record.getDeletedAt() != null) {
            projectCustomersMapper.deleteProjectCustomersById(record.getId());
        }
    }
}
```

**修改后**:
```java
// 不再需要检查软删除记录，因为移除时使用物理删除
// 直接检查是否已存在即可
boolean exists = projectCustomersMapper.checkCustomerInProject(projectId, customerId);
if (exists) {
    log.warn("客户{}已关联到项目{}，跳过", customerId, projectId);
    continue;
}
```

---

#### 2.3 修改 Mapper XML

**文件**: `ProjectCustomersMapper.xml`

**修改前**:
```xml
<update id="deleteProjectCustomersById" parameterType="String">
    UPDATE project_customers
    SET deleted_at = NOW(), updated_at = NOW()
    WHERE id = #{id}
</update>
```

**修改后**:
```xml
<delete id="deleteProjectCustomersById" parameterType="String">
    DELETE FROM project_customers WHERE id = #{id}
</delete>
```

---

## 🔄 完整的编辑流程

### 场景 1: 添加客户

**初始状态**: 项目有客户A、B
**操作**: 添加客户C
**流程**:
```
1. 获取当前客户: [A, B]
2. 对比新客户: [A, B, C]
3. toAdd = [C]
4. toRemove = []
5. 调用 addCustomersToProject([C])
6. 完成
```

---

### 场景 2: 移除客户

**初始状态**: 项目有客户A、B、C
**操作**: 移除客户C
**流程**:
```
1. 获取当前客户: [A, B, C]
2. 对比新客户: [A, B]
3. toAdd = []
4. toRemove = [C]
5. 调用 removeCustomerFromProject(C)  // 物理删除
6. 完成
```

---

### 场景 3: 删除后再添加（关键场景）

**初始状态**: 项目有客户A、B、C
**操作 1**: 移除客户C
**操作 2**: 再次添加客户C

**流程**:
```
操作 1:
1. 移除客户C
2. 物理删除记录  // ✅ 记录被完全删除

操作 2:
3. 添加客户C
4. 检查是否存在: false  // ✅ 因为已被物理删除
5. 插入新记录  // ✅ 成功，不会违反唯一约束
6. 完成
```

---

### 场景 4: 修改主客户

**初始状态**: 项目有客户A（主）、B、C
**操作**: 修改主客户为B
**流程**:
```
1. 获取当前客户: [A(主), B, C]
2. 对比新客户: [A, B, C]
3. toAdd = []
4. toRemove = []
5. 主客户变化: A → B
6. 调用 setPrimaryCustomer(B)
   - clearPrimaryCustomers()  // 清除所有主客户标记
   - setPrimaryCustomer(B)    // 设置B为主客户
7. 完成
```

---

### 场景 5: 同时添加、删除、修改主客户

**初始状态**: 项目有客户A（主）、B、C
**操作**: 删除C，添加D，修改主客户为B

**流程**:
```
1. 获取当前客户: [A(主), B, C]
2. 对比新客户: [A, B, D]
3. toAdd = [D]
4. toRemove = [C]
5. 主客户变化: A → B

6. 先移除客户C
   - removeCustomerFromProject(C)  // 物理删除

7. 添加客户D
   - addCustomersToProject([D])

8. 更新主客户为B
   - setPrimaryCustomer(B)

9. 完成
```

---

## 🧪 测试场景

### 测试 1: 基本添加删除

```
1. 新增项目，选择客户A、B、C
2. 编辑项目，删除客户C
3. 保存
4. 查询数据库，应该只有A、B
5. 再次编辑，添加客户C
6. 保存
7. 查询数据库，应该有A、B、C  // ✅ 不会报错
```

---

### 测试 2: 不能删除主客户

```
1. 项目有客户A（主）、B、C
2. 编辑项目，尝试删除客户A
3. 保存
4. 应该提示错误："不能移除主客户，请先设置其他客户为主客户"
```

---

### 测试 3: 先修改主客户，再删除旧主客户

```
1. 项目有客户A（主）、B、C
2. 编辑项目：
   - 修改主客户为B
   - 删除客户A
3. 保存
4. 应该成功
5. 查询数据库，应该只有B（主）、C
```

---

### 测试 4: 删除所有非主客户

```
1. 项目有客户A（主）、B、C
2. 编辑项目，删除B、C
3. 保存
4. 应该成功
5. 查询数据库，应该只有A（主）
```

---

### 测试 5: 批量操作

```
1. 项目有客户A（主）、B
2. 编辑项目：
   - 删除B
   - 添加C、D、E
   - 修改主客户为C
3. 保存
4. 应该成功
5. 查询数据库，应该有A、C（主）、D、E
```

---

## ⚠️ 注意事项

### 1. 数据迁移

如果数据库中有软删除的记录，需要清理：

```sql
-- 查看软删除的记录
SELECT * FROM project_customers WHERE deleted_at IS NOT NULL;

-- 物理删除所有软删除的记录
DELETE FROM project_customers WHERE deleted_at IS NOT NULL;
```

---

### 2. 审计需求

如果需要保留删除历史，可以：

**方案 A**: 在应用层记录日志
```java
log.info("用户{}删除了项目{}的客户{}", username, projectId, customerId);
```

**方案 B**: 创建单独的审计表
```sql
CREATE TABLE project_customers_audit (
    id VARCHAR(50) PRIMARY KEY,
    project_id VARCHAR(50),
    customer_id VARCHAR(50),
    operation VARCHAR(20),  -- ADD, REMOVE, UPDATE_PRIMARY
    operated_by VARCHAR(50),
    operated_at DATETIME
);
```

---

### 3. 并发控制

当前实现没有并发控制，如果需要：

**方案 A**: 使用乐观锁
```java
@Version
private Integer version;
```

**方案 B**: 使用数据库锁
```sql
SELECT * FROM project_customers WHERE id = ? FOR UPDATE;
```

---

### 4. 性能考虑

- 物理删除比软删除稍快（不需要更新 `deleted_at`）
- 不需要在查询时过滤 `deleted_at IS NULL`
- 索引更简单，性能更好

---

## 📋 修改文件清单

### 前端
1. ✅ `vue3/src/views/evs/projects/components/ProjectEdit.vue`
   - 修改 `handleCustomerRelations` 方法
   - 添加移除客户逻辑
   - 添加主客户检查

### 后端
1. ✅ `sb3/evs-home/src/main/java/com/ruoyi/web/service/impl/ProjectCustomersServiceImpl.java`
   - 修改 `removeProjectCustomer` 方法（改为物理删除）
   - 简化 `addProjectCustomers` 方法（移除软删除处理）

2. ✅ `sb3/evs-home/src/main/resources/mapper/evs/ProjectCustomersMapper.xml`
   - 修改 `deleteProjectCustomersById`（改为 DELETE）
   - 修改 `deleteProjectCustomersByIds`（改为 DELETE）

---

## ✅ 总结

### 修改内容
1. ✅ 前端支持移除客户
2. ✅ 后端改用物理删除
3. ✅ 简化了逻辑，移除了软删除处理
4. ✅ 解决了唯一约束冲突问题

### 优点
- ✅ 逻辑简单清晰
- ✅ 不会有唯一约束冲突
- ✅ 可以随意添加/删除/再添加客户
- ✅ 性能更好

### 下一步
1. ⏳ 重启后端服务
2. ⏳ 清理数据库中的软删除记录
3. ⏳ 测试所有场景

---

**修复时间**: 2026-03-01  
**状态**: ✅ 代码已修复，编译通过，需要重启测试
