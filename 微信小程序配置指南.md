# 微信小程序配置指南

## 当前问题
❌ 后端微信配置中的AppSecret不正确，导致 "invalid appid" 错误

## 解决步骤

### 1. 获取正确的AppSecret
需要从微信公众平台获取与AppID `wx607d3206ed1ec9e6` 对应的AppSecret：

1. 登录 [微信公众平台](https://mp.weixin.qq.com/)
2. 进入小程序管理后台
3. 导航到 **开发** → **开发管理** → **开发设置**
4. 在 **开发者ID** 部分找到：
   - AppID: `wx607d3206ed1ec9e6` (已确认)
   - AppSecret: 点击"重置"或"查看"获取

### 2. 更新后端配置
将获取到的AppSecret更新到配置文件：

**文件**: `sb3/ruoyi-admin/src/main/resources/application.yml`

```yaml
app:
  wechat:
    appid: wx607d3206ed1ec9e6  # ✅ 已更新
    secret: 你的真实AppSecret   # ❌ 需要更新
```

### 3. 重启后端服务
更新配置后需要重启Java应用：

```bash
# 停止当前服务 (Ctrl+C)
# 然后重新启动
cd sb3
java -jar ruoyi-admin/target/ruoyi-admin.jar
```

## 临时测试方案

如果暂时无法获取AppSecret，可以使用以下方法进行测试：

### 方案1: 使用测试AppID
微信提供了测试用的AppID，但功能有限：
- 测试AppID: `wxd101a85aa106f53e`
- 测试AppSecret: `a9b512742f0c39ce8b3d74dd3c3b3950`

### 方案2: 模拟微信响应
临时修改后端代码，跳过微信API调用进行功能测试。

## 配置验证

配置完成后，可以通过以下方式验证：

1. **查看后端日志**：启动时应该没有微信配置相关的错误
2. **测试接口**：使用测试页面 `/pages/test/wechat-binding` 进行测试
3. **检查响应**：应该能正常获取openid而不是 "invalid appid" 错误

## 安全注意事项

⚠️ **重要**: AppSecret是敏感信息，请注意：
- 不要将AppSecret提交到代码仓库
- 生产环境建议使用环境变量或配置中心
- 定期更换AppSecret以确保安全

## 环境变量配置（推荐）

为了安全，建议使用环境变量：

```bash
export APP_WECHAT_APPID=wx607d3206ed1ec9e6
export APP_WECHAT_SECRET=你的真实AppSecret
```

然后在application.yml中：

```yaml
app:
  wechat:
    appid: ${APP_WECHAT_APPID}
    secret: ${APP_WECHAT_SECRET}
```

## 下一步

1. 获取正确的AppSecret
2. 更新配置文件
3. 重启后端服务
4. 重新测试微信登录流程