# 下一步操作清单

## ✅ 已完成

1. **修复API地址问题**
   - `guest.js` 已正确使用 `getBaseUrl()` 函数
   - 自动适配开发/生产环境
   - 添加了详细的日志输出

2. **添加降级方案**
   - API失败时使用硬编码项目ID
   - 确保应用不会完全无法使用

3. **后端代码已创建**
   - `AppGuestConfigController` - 提供API接口
   - `GuestConfigServiceImpl` - 业务逻辑实现
   - 健康检查接口 `/app/guest/health`

---

## ⚠️ 待完成（按优先级排序）

### 优先级1：验证后端部署

**操作：** 测试健康检查接口

```bash
# 开发环境
curl http://192.168.5.102:8080/app/guest/health

# 生产环境
curl https://hsdlp.gzcelestial.com/prod-api/app/guest/health
```

**预期结果：**
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "status": "ok",
    "timestamp": 1234567890,
    "message": "游客配置服务正常运行"
  }
}
```

**如果返回404或500：**
- 后端代码没有部署
- 需要重新编译和部署后端

---

### 优先级2：配置字典

**操作步骤：**

1. 登录后台管理系统
2. 进入 **系统管理 > 字典管理**
3. 点击 **新增** 添加字典类型：
   - 字典名称：`游客演示项目`
   - 字典类型：`guest_demo`
   - 状态：正常
4. 点击字典类型的 **字典数据** 按钮
5. 点击 **新增** 添加字典数据：
   - 字典标签：`projects_01`
   - 字典键值：`实际的演示项目ID`
   - 字典排序：1
   - 状态：正常
   - 备注：默认演示项目

**示例：**
```
字典标签：projects_01
字典键值：9fa800b545b445e4b699b1598bec4619
```

**如果需要多个演示项目：**
```
字典标签：projects_01
字典键值：9fa800b545b445e4b699b1598bec4619

字典标签：projects_02
字典键值：另一个项目ID

字典标签：projects_03
字典键值：第三个项目ID
```

---

### 优先级3：测试API接口

**操作：** 测试演示项目配置接口

```bash
# 开发环境
curl http://192.168.5.102:8080/app/guest/demo-projects

# 生产环境
curl https://hsdlp.gzcelestial.com/prod-api/app/guest/demo-projects
```

**预期结果：**
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "projectIds": ["9fa800b545b445e4b699b1598bec4619"],
    "defaultProjectId": "9fa800b545b445e4b699b1598bec4619",
    "count": 1
  }
}
```

**如果返回空数据：**
- 字典配置不存在或配置错误
- 检查字典类型是否为 `guest_demo`
- 检查字典标签是否以 `projects_` 开头

---

### 优先级4：测试前端功能

**操作步骤：**

1. **清除缓存**
   - 小程序：删除小程序 > 重新编译
   - H5：清除浏览器缓存 > 刷新页面

2. **重新启动应用**
   - 观察控制台日志

3. **查看日志输出**
   ```
   [Guest Config] 预加载配置开始
   [Guest Config] API地址: http://192.168.5.102:8080/app/guest/demo-projects
   [Guest Config] 配置加载成功: { projectIds: [...], defaultProjectId: '...', count: 1 }
   ```

4. **测试页面功能**
   - 访问设计页面：`/pages/design/index`
   - 访问日志页面：`/pages/log/index`
   - 确认能看到演示项目的数据

**如果看不到数据：**
- 检查控制台是否有错误
- 检查是否使用了降级方案（硬编码项目ID）
- 检查项目ID是否正确

---

### 优先级5：删除降级代码（可选）

**前提条件：** API接口正常工作

**操作：** 修改 `uni3/src/config/guest.js`

**删除这段代码：**
```javascript
} catch (error) {
  console.error('[Guest Config] 配置加载异常:', error)
  
  // ===== 临时方案：使用硬编码的项目ID =====
  // TODO: 后端部署后删除这段代码
  console.warn('[Guest Config] API调用失败，使用硬编码的演示项目ID（临时方案）')
  GUEST_DEMO_PROJECT_IDS = ['9fa800b545b445e4b699b1598bec4619']
  DEFAULT_GUEST_PROJECT_ID = '9fa800b545b445e4b699b1598bec4619'
  CONFIG_LOADED = true
  console.log('[Guest Config] 使用硬编码配置:', {
    projectIds: GUEST_DEMO_PROJECT_IDS,
    defaultProjectId: DEFAULT_GUEST_PROJECT_ID
  })
  // ===== 临时方案结束 =====
  
  // 通知所有等待的回调
  LOAD_CALLBACKS.forEach(callback => callback())
  LOAD_CALLBACKS = []
}
```

**改为：**
```javascript
} catch (error) {
  console.error('[Guest Config] 配置加载异常:', error)
  
  // 通知所有等待的回调（失败）
  LOAD_CALLBACKS.forEach(callback => callback())
  LOAD_CALLBACKS = []
  
  throw error // 抛出错误，让调用方处理
} finally {
  CONFIG_LOADING = false
}
```

**注意：** 只有在API完全稳定后才删除降级代码！

---

## 快速验证命令

```bash
# 1. 测试健康检查（开发环境）
curl http://192.168.5.102:8080/app/guest/health

# 2. 测试健康检查（生产环境）
curl https://hsdlp.gzcelestial.com/prod-api/app/guest/health

# 3. 测试演示项目配置（开发环境）
curl http://192.168.5.102:8080/app/guest/demo-projects

# 4. 测试演示项目配置（生产环境）
curl https://hsdlp.gzcelestial.com/prod-api/app/guest/demo-projects

# 5. 检查特定项目是否为演示项目
curl http://192.168.5.102:8080/app/guest/check-demo-project/9fa800b545b445e4b699b1598bec4619
```

---

## 故障排查

### 问题1：健康检查返回404

**原因：** 后端代码没有部署

**解决：**
```bash
cd sb3
mvn clean package
# 重启服务
systemctl restart evs-home
```

### 问题2：API返回空数据

**原因：** 字典配置不存在

**解决：**
1. 检查字典类型 `guest_demo` 是否存在
2. 检查字典数据是否正确
3. 检查字典标签是否为 `projects_01`

### 问题3：前端使用硬编码项目ID

**原因：** API调用失败，触发降级方案

**解决：**
1. 检查网络连接
2. 确认API地址正确
3. 查看控制台日志

### 问题4：看不到数据

**原因：** 项目ID不正确或权限问题

**解决：**
1. 确认项目ID存在且有数据
2. 检查后端日志
3. 测试API接口返回的项目ID

---

## 联系支持

如果遇到问题，请提供：

1. **健康检查结果**
   ```bash
   curl http://192.168.5.102:8080/app/guest/health
   ```

2. **API测试结果**
   ```bash
   curl http://192.168.5.102:8080/app/guest/demo-projects
   ```

3. **字典配置截图**
   - 字典类型列表
   - 字典数据列表

4. **前端控制台日志**
   - 包含 `[Guest Config]` 的所有日志

5. **后端日志**
   - 包含 `[游客配置API]` 的所有日志

---

**文档版本：** 1.0  
**创建时间：** 2026-02-13  
**维护人员：** Kiro AI Assistant
