# 微信绑定解绑问题修复说明

## 问题分析

### 原始问题
客户管理页面解绑微信时失败，请求URL为：
```
POST http://localhost/dev-api/app/auth/unbind-wechat
{
  "openid": "ozgYy3Utof8Z4Z9cl3TxIA5QyUjY",
  "deviceId": "admin-web"
}
```

### 问题原因
原有的 `/app/auth/unbind-wechat` 接口是为小程序端设计的，需要额外的身份验证参数：

**UnbindWechatRequest 完整参数：**
```java
{
  "openid": "微信openId",           // 必填
  "deviceId": "设备ID",             // 必填
  "verifyType": "验证方式",         // 必填：phone 或 password
  "phone": "手机号",                // verifyType=phone 时必填
  "code": "验证码",                 // verifyType=phone 时必填
  "password": "密码"                // verifyType=password 时必填
}
```

**验证逻辑：**
1. 如果 `verifyType` 为 `phone`：需要提供手机号和验证码
2. 如果 `verifyType` 为 `password`：需要提供密码（仅员工支持）
3. 如果 `verifyType` 为空或其他值：抛出"验证方式不正确"异常

这个设计是为了保护小程序用户的安全，防止未授权的解绑操作。但对于管理后台，这个验证流程不适用，因为：
- 管理员已经通过系统登录验证
- 管理员有相应的权限控制
- 不需要再次验证手机号或密码

## 解决方案

### 方案选择
采用**方案一**：在后端添加管理员专用的解绑接口

**优点：**
- 职责分离：小程序端和管理端使用不同的接口
- 安全性高：通过权限注解控制访问
- 不影响现有小程序端逻辑
- 符合RESTful设计规范

### 实现内容

#### 1. 后端Controller新增接口
**文件**: `sb3/evs-home/src/main/java/com/ruoyi/web/controller/AppWechatBindingsController.java`

新增两个管理员专用解绑接口：

```java
/**
 * 管理员解除微信绑定（通过userId）
 */
@PreAuthorize("@ss.hasPermi('evs:appWechatBindings:remove')")
@Log(title = "解除微信绑定", businessType = BusinessType.DELETE)
@DeleteMapping("/unbind/user/{userId}")
public AjaxResult unbindByUserId(@PathVariable String userId)

/**
 * 管理员解除微信绑定（通过openId）
 */
@PreAuthorize("@ss.hasPermi('evs:appWechatBindings:remove')")
@Log(title = "解除微信绑定", businessType = BusinessType.DELETE)
@DeleteMapping("/unbind/openid/{openId}")
public AjaxResult unbindByOpenId(@PathVariable String openId)
```

**特点：**
- 使用 DELETE 方法，符合RESTful规范
- 通过 `@PreAuthorize` 注解进行权限控制
- 记录操作日志
- 路径参数传递，简洁明了

#### 2. Service层新增方法
**文件**: `sb3/evs-home/src/main/java/com/ruoyi/web/service/IAppWechatBindingsService.java`

新增接口方法：
```java
/**
 * 根据userId删除微信绑定信息
 */
public int deleteAppWechatBindingsByUserId(String userId);
```

**文件**: `sb3/evs-home/src/main/java/com/ruoyi/web/service/impl/AppWechatBindingsServiceImpl.java`

实现方法：
```java
@Override
public int deleteAppWechatBindingsByUserId(String userId)
{
    AppWechatBindings query = new AppWechatBindings();
    query.setUserId(userId);
    List<AppWechatBindings> list = appWechatBindingsMapper.selectAppWechatBindingsList(query);
    if (!list.isEmpty()) {
        return appWechatBindingsMapper.deleteAppWechatBindingsById(list.get(0).getId());
    }
    return 0;
}
```

#### 3. 前端API更新
**文件**: `vue3/src/api/evs/wechatBindings.js`

新增管理员解绑接口：
```javascript
// 管理员解除微信绑定（通过openId）
export function unbindWechatByOpenId(openId) {
  return request({
    url: '/evs/appWechatBindings/unbind/openid/' + openId,
    method: 'delete'
  })
}

// 管理员解除微信绑定（通过userId）
export function unbindWechatByUserId(userId) {
  return request({
    url: '/evs/appWechatBindings/unbind/user/' + userId,
    method: 'delete'
  })
}
```

保留原有小程序端接口供参考：
```javascript
// 小程序端解除微信绑定（需要验证）
export function unbindWechat(data) {
  return request({
    url: '/app/auth/unbind-wechat',
    method: 'post',
    data: data
  })
}
```

#### 4. 页面调用更新

**客户管理页面** (`vue3/src/views/evs/customers/card.vue`)：
```javascript
import { listWechatBindings, unbindWechatByOpenId } from "@/api/evs/wechatBindings"

function handleUnbindWechat(customer) {
  const binding = getWechatBinding(customer.id)
  if (!binding) {
    proxy.$modal.msgWarning('该客户未绑定微信')
    return
  }

  proxy.$modal.confirm(`是否确认解除客户 ${customer.name} 的微信绑定？`).then(() => {
    return unbindWechatByOpenId(binding.openId)
  }).then(() => {
    proxy.$modal.msgSuccess('解除绑定成功')
    getWechatBindings()
  }).catch(() => {})
}
```

**用户管理页面** (`vue3/src/views/system/user/card.vue`)：
```javascript
import { listWechatBindings, unbindWechatByOpenId } from "@/api/evs/wechatBindings"

function handleUnbindWechat(user) {
  const binding = getWechatBinding(user.userId)
  if (!binding) {
    proxy.$modal.msgWarning('该用户未绑定微信')
    return
  }

  proxy.$modal.confirm(`是否确认解除用户 ${user.nickName || user.userName} 的微信绑定？`).then(() => {
    return unbindWechatByOpenId(binding.openId)
  }).then(() => {
    proxy.$modal.msgSuccess('解除绑定成功')
    getWechatBindings()
  }).catch(() => {})
}
```

## API对比

### 小程序端解绑接口（原有）
- **URL**: `POST /app/auth/unbind-wechat`
- **权限**: 无需权限（通过openId验证身份）
- **参数**: 
  ```json
  {
    "openid": "必填",
    "deviceId": "必填",
    "verifyType": "必填：phone/password",
    "phone": "条件必填",
    "code": "条件必填",
    "password": "条件必填"
  }
  ```
- **用途**: 小程序用户自主解绑

### 管理员解绑接口（新增）
- **URL**: `DELETE /evs/appWechatBindings/unbind/openid/{openId}`
- **权限**: `evs:appWechatBindings:remove`
- **参数**: 路径参数 `openId`
- **用途**: 管理员后台解绑

- **URL**: `DELETE /evs/appWechatBindings/unbind/user/{userId}`
- **权限**: `evs:appWechatBindings:remove`
- **参数**: 路径参数 `userId`
- **用途**: 管理员后台解绑（通过用户ID）

## 安全性说明

### 权限控制
1. **管理员接口**：通过 `@PreAuthorize("@ss.hasPermi('evs:appWechatBindings:remove')")` 注解控制
2. **小程序接口**：通过验证码或密码验证用户身份

### 操作日志
- 所有解绑操作都会记录操作日志
- 日志标题：`解除微信绑定`
- 业务类型：`BusinessType.DELETE`

### 数据验证
- 检查绑定信息是否存在
- 验证用户权限
- 确认操作前需要用户确认

## 测试建议

### 功能测试
1. **客户管理页面**
   - 测试已绑定客户的解绑功能
   - 测试未绑定客户点击解绑的提示
   - 验证解绑后数据刷新

2. **用户管理页面**
   - 测试已绑定用户的解绑功能
   - 测试未绑定用户点击解绑的提示
   - 验证解绑后数据刷新

### 权限测试
1. 测试有权限用户能否正常解绑
2. 测试无权限用户是否看不到解绑按钮
3. 测试无权限用户直接调用API是否被拦截

### 边界测试
1. 测试不存在的openId解绑
2. 测试不存在的userId解绑
3. 测试网络异常时的错误处理

## 部署说明

### 后端部署
1. 重新编译后端项目
2. 重启服务

### 前端部署
1. 无需重新构建（热更新即可）
2. 清除浏览器缓存

### 权限配置
确保相关角色拥有 `evs:appWechatBindings:remove` 权限：
1. 登录管理后台
2. 进入系统管理 -> 角色管理
3. 编辑相关角色
4. 勾选"微信绑定管理"下的"删除"权限
5. 保存

## 总结

通过添加管理员专用的解绑接口，解决了原有接口需要额外验证参数的问题。新接口：
- ✅ 符合RESTful设计规范
- ✅ 权限控制完善
- ✅ 操作日志完整
- ✅ 不影响小程序端现有逻辑
- ✅ 代码简洁易维护

现在管理员可以直接在后台解除用户的微信绑定，无需额外的验证步骤。
