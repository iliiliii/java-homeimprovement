# 网络请求调试指南

## 🔍 问题分析

根据错误信息 `[微信登录] 失败: Error: 检查绑定状态失败请求并为发起`，这个问题可能有以下几个原因：

### 1. 网络连接问题
- 小程序无法访问本地开发服务器
- IP地址配置错误
- 端口被防火墙阻止

### 2. 微信小程序域名限制
- 小程序只能访问配置的合法域名
- 开发环境需要勾选"不校验合法域名"

### 3. 后端服务问题
- 后端服务未启动
- 接口路径错误
- CORS跨域问题

## 🛠️ 调试步骤

### 步骤1：检查网络配置

1. **确认IP地址**
   ```bash
   # 在终端中查看本机IP
   ifconfig | grep inet
   # 或者
   ipconfig getifaddr en0
   ```

2. **确认后端服务运行**
   ```bash
   # 检查端口是否被占用
   lsof -i :8080
   # 或者
   netstat -an | grep 8080
   ```

3. **测试API连接**
   ```bash
   # 使用curl测试健康检查接口
   curl http://192.168.5.102:8080/app/auth/health
   ```

### 步骤2：微信开发者工具配置

1. **勾选不校验合法域名**
   - 打开微信开发者工具
   - 点击右上角"详情"
   - 勾选"不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书"

2. **检查网络状态**
   - 在开发者工具的控制台中查看网络请求
   - 确认请求是否真的发出

### 步骤3：使用测试页面

我已经创建了两个测试页面：

1. **网络连接测试** (`/pages/test/network-test`)
   - 测试基础网络连接
   - 测试API健康检查
   - 测试检查绑定接口

2. **微信登录测试** (`/pages/test/wechat-login`)
   - 测试完整的微信登录流程
   - 查看详细的API调用日志

### 步骤4：修改网络配置

如果IP地址不正确，请修改 `uni3/src/utils/request.js`：

```javascript
// 修改为正确的IP地址
const getBaseUrl = () => {
  if (isDev) {
    // #ifdef MP-WEIXIN
    return 'http://YOUR_ACTUAL_IP:8080'  // 替换为实际IP
    // #endif
  }
  // ...
}
```

## 🔧 常见解决方案

### 方案1：使用正确的IP地址

1. 查看本机IP地址
2. 修改 `request.js` 中的IP配置
3. 重新编译小程序

### 方案2：启动后端服务

1. 确保后端服务正在运行
2. 检查端口8080是否可访问
3. 确认接口路径正确

### 方案3：配置微信开发者工具

1. 勾选"不校验合法域名"
2. 清除缓存重新编译
3. 检查控制台网络请求

### 方案4：使用代理或ngrok

如果本地IP无法访问，可以使用ngrok等工具：

```bash
# 安装ngrok
npm install -g ngrok

# 启动代理
ngrok http 8080

# 使用ngrok提供的URL替换request.js中的地址
```

## 📝 调试日志

我已经在代码中添加了详细的调试日志：

1. **API调用日志** - 在 `auth.js` 中
2. **网络请求日志** - 在 `request.js` 中
3. **前端流程日志** - 在登录页面中

查看这些日志可以帮助定位具体问题。

## 🚀 快速测试

1. 打开微信开发者工具
2. 导航到 `/pages/test/network-test`
3. 依次点击测试按钮
4. 查看日志输出，确定问题所在

## 📞 如果问题仍然存在

请提供以下信息：

1. 微信开发者工具的控制台日志
2. 网络测试页面的日志输出
3. 后端服务的启动日志
4. 本机的IP地址配置

这样可以更准确地定位和解决问题。