# 项目多客户关联优化方案 - 实施指南

## 七、实施步骤

### 阶段一：数据库准备（预计1天）

#### Step 1: 创建新表
```sql
-- 1. 创建 project_customers 表
-- 执行前面提供的建表SQL

-- 2. 验证表结构
DESC project_customers;
SHOW INDEX FROM project_customers;
```

#### Step 2: 数据迁移
```sql
-- 1. 备份现有数据
CREATE TABLE projects_backup AS SELECT * FROM projects;

-- 2. 迁移现有 customer_id 到 project_customers
INSERT INTO project_customers 
  (id, project_id, customer_id, role, is_primary, join_date, is_active, created_at, created_by)
SELECT 
  UUID() as id,
  p.id as project_id,
  p.customer_id,
  'OWNER' as role,
  1 as is_primary,
  COALESCE(p.created_at, NOW()) as join_date,
  1 as is_active,
  NOW() as created_at,
  'system' as created_by
FROM projects p
WHERE p.customer_id IS NOT NULL 
  AND p.deleted_at IS NULL;

-- 3. 验证迁移结果
SELECT 
  COUNT(*) as total_projects,
  COUNT(DISTINCT project_id) as migrated_projects
FROM project_customers;

-- 4. 检查数据一致性
SELECT p.id, p.customer_id, pc.customer_id as pc_customer_id
FROM projects p
LEFT JOIN project_customers pc ON p.id = pc.project_id AND pc.is_primary = 1
WHERE p.customer_id IS NOT NULL 
  AND p.deleted_at IS NULL
  AND (pc.customer_id IS NULL OR p.customer_id != pc.customer_id);
```

### 阶段二：后端开发（预计3-4天）

#### Step 1: 创建实体类和Mapper（1天）
1. 创建 `ProjectCustomers.java` 实体类
2. 创建 `ProjectCustomersMapper.java` 接口
3. 创建 `ProjectCustomersMapper.xml` 配置文件
4. 编写单元测试验证Mapper功能

#### Step 2: 实现Service层（1天）
1. 创建 `IProjectCustomersService.java` 接口
2. 实现 `ProjectCustomersServiceImpl.java`
3. 编写Service层单元测试
4. 测试事务回滚机制

#### Step 3: 修改现有代码（1-2天）
1. 修改 `AppAuthServiceImpl.java` 的 `findUserProjects()` 方法
2. 修改权限验证逻辑（多处）
3. 修改 `ProjectsServiceImpl.java` 的 `insertProjects()` 方法
4. 修改 `AppProjectMapper.java` 的查询方法
5. 更新 `Projects.java` 实体类，添加客户列表字段

#### Step 4: 创建Controller（0.5天）
1. 创建 `ProjectCustomersController.java`
2. 添加API接口
3. 使用Postman测试所有接口

### 阶段三：前端开发（预计2-3天）

#### Step 1: API接口封装（0.5天）
1. 创建 `src/api/evs/projectCustomers.js`
2. 定义所有API方法
3. 测试API调用

#### Step 2: 组件开发（1.5天）
1. 创建 `ProjectEditMultiCustomer.vue` 组件
2. 创建 `ProjectCustomerList.vue` 组件
3. 修改现有项目列表页面
4. 修改项目详情页面

#### Step 3: 集成测试（1天）
1. 测试项目创建流程
2. 测试客户添加/移除
3. 测试主客户设置
4. 测试权限控制

### 阶段四：测试与优化（预计2天）

#### Step 1: 功能测试
- [ ] 创建项目时选择多个客户
- [ ] 设置主客户
- [ ] 添加客户到现有项目
- [ ] 移除客户（非主客户）
- [ ] 更新客户角色和份额
- [ ] 客户登录查看项目列表
- [ ] 客户访问项目详情
- [ ] 权限验证（越权访问测试）

#### Step 2: 性能测试
- [ ] 测试10个客户的项目查询性能
- [ ] 测试批量查询项目的客户数量
- [ ] 检查SQL执行计划
- [ ] 优化慢查询

#### Step 3: 兼容性测试
- [ ] 测试旧数据的兼容性
- [ ] 测试单客户项目的正常运行
- [ ] 测试数据迁移的完整性

### 阶段五：上线部署（预计1天）

#### Step 1: 准备工作
1. 备份生产数据库
2. 准备回滚方案
3. 编写部署文档

#### Step 2: 部署流程
1. 停止应用服务
2. 执行数据库迁移脚本
3. 部署新版本代码
4. 启动应用服务
5. 验证核心功能

#### Step 3: 监控观察
1. 监控错误日志
2. 检查性能指标
3. 收集用户反馈

## 八、注意事项

### 8.1 数据一致性

**问题**: projects.customer_id 与 project_customers 表数据不一致

**解决方案**:
```java
// 在 Service 层添加数据一致性检查
@Scheduled(cron = "0 0 2 * * ?") // 每天凌晨2点执行
public void syncCustomerData() {
    // 1. 查询所有项目
    List<Projects> projects = projectsMapper.selectAllProjects();
    
    for (Projects project : projects) {
        // 2. 查询主客户
        ProjectCustomers primary = projectCustomersMapper.selectPrimaryByProjectId(project.getId());
        
        // 3. 同步 customer_id
        if (primary != null && !primary.getCustomerId().equals(project.getCustomerId())) {
            project.setCustomerId(primary.getCustomerId());
            projectsMapper.updateProjects(project);
            log.warn("同步项目 {} 的主客户ID: {} -> {}", 
                project.getId(), project.getCustomerId(), primary.getCustomerId());
        }
    }
}
```

### 8.2 性能优化

**问题**: 查询项目列表时需要关联多个客户，性能下降

**解决方案**:
1. 使用批量查询减少数据库访问次数
2. 添加 Redis 缓存客户数量
3. 使用分页查询
4. 优化索引

```java
// 批量查询优化示例
public List<Projects> selectProjectsListOptimized(Projects projects) {
    // 1. 查询项目列表
    List<Projects> projectsList = projectsMapper.selectProjectsList(projects);
    
    if (projectsList.isEmpty()) {
        return projectsList;
    }
    
    // 2. 提取项目ID
    List<String> projectIds = projectsList.stream()
            .map(Projects::getId)
            .collect(Collectors.toList());
    
    // 3. 批量查询客户数量（一次SQL）
    Map<String, Integer> customerCountMap = 
        projectCustomersMapper.countByProjectIds(projectIds)
            .stream()
            .collect(Collectors.toMap(
                m -> (String) m.get("projectId"),
                m -> ((Number) m.get("customerCount")).intValue()
            ));
    
    // 4. 设置客户数量
    projectsList.forEach(p -> 
        p.setCustomerCount(customerCountMap.getOrDefault(p.getId(), 0))
    );
    
    return projectsList;
}
```

### 8.3 事务管理

**问题**: 添加多个客户时，部分成功部分失败

**解决方案**:
```java
@Transactional(rollbackFor = Exception.class)
public int addProjectCustomers(String projectId, List<String> customerIds, String primaryCustomerId) {
    try {
        // 业务逻辑
        // ...
        
        return result;
    } catch (Exception e) {
        // 记录日志
        log.error("添加项目客户失败: projectId={}, customerIds={}", projectId, customerIds, e);
        // 抛出异常，触发事务回滚
        throw new ServiceException("添加项目客户失败: " + e.getMessage());
    }
}
```

### 8.4 权限控制

**问题**: 多客户场景下的权限判断

**解决方案**:
```java
// 统一的权限验证方法
public boolean checkProjectAccess(String projectId, String userId, String userType) {
    if ("customer".equals(userType)) {
        // 检查客户是否关联到项目
        return projectCustomersMapper.checkCustomerInProject(projectId, userId);
    } else if ("staff".equals(userType)) {
        // 检查员工是否为项目成员
        return projectMembersMapper.checkStaffInProject(projectId, userId);
    }
    return false;
}
```

### 8.5 数据迁移风险

**风险点**:
1. 迁移过程中数据丢失
2. 迁移后数据不一致
3. 迁移失败无法回滚

**防范措施**:
```sql
-- 1. 迁移前备份
CREATE TABLE projects_backup_20260301 AS SELECT * FROM projects;
CREATE TABLE project_customers_backup_20260301 AS SELECT * FROM project_customers;

-- 2. 迁移后验证
SELECT 
  (SELECT COUNT(*) FROM projects WHERE customer_id IS NOT NULL AND deleted_at IS NULL) as projects_count,
  (SELECT COUNT(DISTINCT project_id) FROM project_customers WHERE is_primary = 1) as migrated_count;

-- 3. 回滚脚本
-- 如果迁移失败，执行以下脚本回滚
TRUNCATE TABLE project_customers;
DROP TABLE projects;
RENAME TABLE projects_backup_20260301 TO projects;
```

## 九、回滚方案

### 9.1 数据库回滚

```sql
-- 1. 停止应用
-- 2. 恢复备份
DROP TABLE project_customers;
DROP TABLE projects;
RENAME TABLE projects_backup_20260301 TO projects;

-- 3. 验证数据
SELECT COUNT(*) FROM projects;
SELECT * FROM projects LIMIT 10;
```

### 9.2 代码回滚

```bash
# 1. 回滚到上一个版本
git revert <commit-hash>

# 2. 重新部署
mvn clean package
# 部署到服务器

# 3. 重启服务
systemctl restart evs-home
```

## 十、FAQ

### Q1: 是否必须保留 projects.customer_id 字段？

**A**: 推荐保留。理由：
1. 向后兼容，减少代码改动
2. 快速查询主客户，无需JOIN
3. 作为冗余字段，提高查询性能
4. 便于数据一致性检查

### Q2: 如何处理客户退出项目的情况？

**A**: 使用软删除：
- 设置 `is_active = 0`
- 记录 `leave_date`
- 保留历史记录用于审计

### Q3: 份额比例总和必须等于100%吗？

**A**: 不强制要求，由业务决定：
- 如果需要严格控制，在Service层添加验证
- 如果只是参考值，可以不限制

### Q4: 主客户可以退出项目吗？

**A**: 不允许。必须先设置其他客户为主客户，才能移除当前主客户。

### Q5: 如何限制客户数量不超过10个？

**A**: 在Service层添加验证：
```java
int currentCount = projectCustomersMapper.countByProjectId(projectId);
if (currentCount + customerIds.size() > MAX_CUSTOMERS_PER_PROJECT) {
    throw new ServiceException("项目客户数量不能超过10个");
}
```

## 十一、总结

### 优化效果

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| 客户关联 | 1个 | 最多10个 |
| 数据模型 | 一对一 | 多对多 |
| 权限控制 | 单一客户 | 多客户共享 |
| 扩展性 | 低 | 高 |
| 业务场景 | 单一业主 | 合伙、家庭、投资等 |

### 技术亮点

1. **中间表设计**: 标准的多对多关系实现
2. **主客户机制**: 保留主客户概念，兼容现有业务
3. **冗余字段**: projects.customer_id 作为快捷查询字段
4. **软删除**: 保留历史记录，支持审计
5. **批量查询**: 优化性能，减少数据库访问
6. **事务控制**: 保证数据一致性
7. **向后兼容**: 最小化代码改动

### 后续优化方向

1. 添加客户权限级别（只读、编辑、管理）
2. 支持客户邀请机制
3. 添加客户操作日志
4. 实现客户消息通知
5. 支持客户分组管理

---

**文档版本**: v1.0  
**最后更新**: 2026-03-01  
**作者**: AI Assistant  
**审核状态**: 待审核
