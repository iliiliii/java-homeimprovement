# 代码同步方案说明

**创建时间**: 2026-03-01  
**方案**: 在应用代码中处理 projects.customer_id 与 project_customers 表的同步

---

## 📋 方案概述

### 核心思路

**在应用代码中处理同步**，不使用数据库触发器：
- ✅ 在 `ProjectsServiceImpl` 中添加同步逻辑
- ✅ 在 `ProjectCustomersServiceImpl` 中添加同步逻辑
- ✅ 使用事务保证数据一致性

### 优点
1. ✅ 代码逻辑清晰，易于理解和维护
2. ✅ 不依赖数据库特性，跨数据库兼容
3. ✅ 便于调试和测试
4. ✅ 可以添加详细的日志记录
5. ✅ 异常处理更灵活

### 缺点
1. ⚠️ 需要修改多处代码
2. ⚠️ 如果直接操作数据库会绕过同步逻辑
3. ⚠️ 需要确保所有修改 projects.customer_id 的地方都添加同步

---

## 🔧 已实现的同步逻辑

### 1. ProjectsServiceImpl.java ✅

#### 1.1 insertProjects - 创建项目时同步

```java
@Override
@Transactional(rollbackFor = Exception.class)
public int insertProjects(Projects projects) {
    // ... 创建项目 ...
    int result = projectsMapper.insertProjects(projects);
    
    // 同步创建 project_customers 关联
    if (result > 0 && projects.getCustomerId() != null) {
        syncProjectCustomer(projects.getId(), projects.getCustomerId(), "insert");
    }
    
    return result;
}
```

**功能**:
- ✅ 创建项目时，自动在 project_customers 表中创建关联
- ✅ 设置为主客户（is_primary = 1）
- ✅ 避免重复创建（检查是否已存在）

---

#### 1.2 updateProjects - 更新项目时同步

```java
@Override
@Transactional(rollbackFor = Exception.class)
public int updateProjects(Projects projects) {
    // 获取旧的 customer_id
    Projects existing = selectProjectsById(projects.getId());
    String oldCustomerId = existing.getCustomerId();
    String newCustomerId = projects.getCustomerId();
    
    // 检查是否发生变化
    boolean customerChanged = /* 判断逻辑 */;
    
    // 更新项目
    int result = projectsMapper.updateProjects(projects);
    
    // 如果 customer_id 发生变化，同步更新
    if (result > 0 && customerChanged) {
        syncProjectCustomer(projects.getId(), newCustomerId, "update", oldCustomerId);
    }
    
    return result;
}
```

**功能**:
- ✅ 检测 customer_id 是否发生变化
- ✅ 软删除旧的主客户关联
- ✅ 创建新的主客户关联
- ✅ 如果新客户已存在，设置为主客户

---

#### 1.3 softDeleteProjectsById - 软删除项目时同步

```java
@Override
@Transactional(rollbackFor = Exception.class)
public int softDeleteProjectsById(String id) {
    // 软删除项目
    projects.setDeletedAt(DateUtils.getNowDate());
    int result = projectsMapper.updateProjects(projects);
    
    // 同步软删除所有客户关联
    if (result > 0) {
        syncProjectCustomer(id, null, "delete");
    }
    
    return result;
}
```

**功能**:
- ✅ 软删除项目时，同步软删除所有 project_customers 记录
- ✅ 保持数据一致性

---

#### 1.4 syncProjectCustomer - 核心同步方法

```java
private void syncProjectCustomer(String projectId, String customerId, String operation, String... oldCustomerId) {
    try {
        if ("insert".equals(operation)) {
            // 创建新关联
        } else if ("update".equals(operation)) {
            // 软删除旧关联 + 创建新关联
        } else if ("delete".equals(operation)) {
            // 软删除所有关联
        }
    } catch (Exception e) {
        log.error("同步失败：{}", e.getMessage(), e);
        // 不抛出异常，避免影响主流程
    }
}
```

**功能**:
- ✅ 统一的同步逻辑
- ✅ 支持 insert、update、delete 三种操作
- ✅ 详细的日志记录
- ✅ 异常不影响主流程

---

### 2. ProjectCustomersServiceImpl.java ✅

#### 2.1 setPrimaryCustomer - 设置主客户时同步

```java
@Override
@Transactional(rollbackFor = Exception.class)
public int setPrimaryCustomer(String projectId, String customerId) {
    // 设置主客户
    int result = projectCustomersMapper.setPrimaryCustomer(projectId, customerId);
    
    // 同步更新 projects.customer_id
    try {
        Projects project = new Projects();
        project.setId(projectId);
        project.setCustomerId(customerId);
        projectsMapper.updateProjects(project);
        log.info("同步更新 projects.customer_id 成功");
    } catch (Exception e) {
        log.error("同步更新 projects.customer_id 失败：{}", e.getMessage(), e);
    }
    
    return result;
}
```

**功能**:
- ✅ 设置主客户时，同步更新 projects.customer_id
- ✅ 保持数据一致性
- ✅ 异常不影响主流程

---

## 🔄 数据流向

### 场景 1: 创建新项目

```
用户创建项目（设置 customer_id）
    ↓
ProjectsServiceImpl.insertProjects()
    ↓
插入 projects 表
    ↓
syncProjectCustomer("insert")
    ↓
插入 project_customers 表（主客户）
    ↓
数据一致 ✅
```

---

### 场景 2: 修改项目客户

```
用户修改 projects.customer_id
    ↓
ProjectsServiceImpl.updateProjects()
    ↓
检测 customer_id 是否变化
    ↓
更新 projects 表
    ↓
syncProjectCustomer("update")
    ↓
软删除旧的 project_customers 记录
    ↓
创建新的 project_customers 记录（主客户）
    ↓
数据一致 ✅
```

---

### 场景 3: 设置主客户

```
用户通过多客户管理设置主客户
    ↓
ProjectCustomersServiceImpl.setPrimaryCustomer()
    ↓
更新 project_customers 表（设置主客户）
    ↓
同步更新 projects.customer_id
    ↓
数据一致 ✅
```

---

### 场景 4: 软删除项目

```
用户软删除项目
    ↓
ProjectsServiceImpl.softDeleteProjectsById()
    ↓
软删除 projects 表
    ↓
syncProjectCustomer("delete")
    ↓
软删除所有 project_customers 记录
    ↓
数据一致 ✅
```

---

## ⚠️ 注意事项

### 1. 事务处理

**重要**: 所有同步操作都在事务中执行

```java
@Transactional(rollbackFor = Exception.class)
```

**好处**:
- ✅ 如果同步失败，整个操作回滚
- ✅ 保证数据一致性
- ✅ 避免部分成功的情况

---

### 2. 异常处理

**策略**: 同步异常不影响主流程

```java
try {
    // 同步逻辑
} catch (Exception e) {
    log.error("同步失败：{}", e.getMessage(), e);
    // 不抛出异常
}
```

**原因**:
- ✅ 避免因同步失败导致主操作失败
- ✅ 记录日志便于排查问题
- ✅ 可以通过定期检查修复不一致数据

---

### 3. 重复检查

**避免重复创建**:

```java
boolean exists = projectCustomersMapper.checkCustomerInProject(projectId, customerId);
if (!exists) {
    // 创建新记录
}
```

**好处**:
- ✅ 避免唯一约束冲突
- ✅ 支持幂等操作
- ✅ 可以重复执行

---

### 4. 日志记录

**详细的日志**:

```java
log.info("同步创建 project_customers 记录：projectId={}, customerId={}", projectId, customerId);
log.error("同步失败：projectId={}, error={}", projectId, e.getMessage(), e);
```

**好处**:
- ✅ 便于排查问题
- ✅ 监控同步状态
- ✅ 审计操作记录

---

## 📋 需要注意的地方

### 1. 所有修改 projects.customer_id 的地方

**必须添加同步逻辑的方法**:
- ✅ insertProjects - 已添加
- ✅ updateProjects - 已添加
- ✅ softDeleteProjectsById - 已添加

**其他可能的地方**:
- ⚠️ 批量导入项目
- ⚠️ 数据迁移脚本
- ⚠️ 管理后台的批量操作

---

### 2. 直接操作数据库

**风险**: 如果直接通过 SQL 修改 projects.customer_id，不会触发同步

**解决方案**:
1. ✅ 禁止直接修改数据库（通过权限控制）
2. ✅ 定期运行数据一致性检查
3. ✅ 发现不一致时手动修复

---

### 3. 性能考虑

**影响**: 每次操作多执行 1-2 次数据库查询

**优化**:
- ✅ 使用事务减少往返次数
- ✅ 只在必要时执行同步（检查是否变化）
- ✅ 批量操作时可以优化为批量同步

---

## 🔍 数据一致性检查

### 定期检查（建议每周）

```sql
-- 运行数据一致性检查（数据迁移-简化版.sql 步骤 2）
```

### 检查内容
1. ✅ 缺少 project_customers 记录的项目
2. ✅ 主客户不一致的项目
3. ✅ 没有主客户的项目
4. ✅ 有多个主客户的项目

### 修复方法
如果发现不一致：
1. 运行检查 SQL 找出问题记录
2. 手动修复数据
3. 检查应用日志找出原因
4. 修复代码逻辑

---

## 📊 对比：代码同步 vs 触发器

| 特性 | 代码同步 | 触发器 |
|------|---------|--------|
| 实现复杂度 | 中等 | 简单 |
| 维护难度 | 容易 | 困难 |
| 调试难度 | 容易 | 困难 |
| 跨数据库 | ✅ 支持 | ❌ 不支持 |
| 性能影响 | 小 | 极小 |
| 日志记录 | ✅ 详细 | ❌ 有限 |
| 异常处理 | ✅ 灵活 | ❌ 有限 |
| 绕过风险 | ⚠️ 存在 | ✅ 无 |
| 测试难度 | 容易 | 困难 |

---

## ✅ 总结

### 优点
1. ✅ 代码逻辑清晰，易于理解
2. ✅ 不依赖数据库特性
3. ✅ 便于调试和测试
4. ✅ 详细的日志记录
5. ✅ 灵活的异常处理
6. ✅ 使用事务保证一致性

### 注意事项
1. ⚠️ 确保所有修改 projects.customer_id 的地方都添加同步
2. ⚠️ 定期运行数据一致性检查
3. ⚠️ 禁止直接操作数据库
4. ⚠️ 监控应用日志

### 建议
- 在测试环境充分测试
- 部署后监控日志
- 定期运行一致性检查
- 发现问题及时修复

---

**创建时间**: 2026-03-01  
**最后更新**: 2026-03-01
