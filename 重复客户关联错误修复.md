# 重复客户关联错误修复

**问题时间**: 2026-03-01  
**错误**: Duplicate entry for key 'uk_project_customer'

---

## ❌ 错误信息

```
Duplicate entry 'd7d22e4f91384e9aa4095d22d51b286c-134b81ce28054cbb85d27a74964f0d5' 
for key 'project_customers.uk_project_customer'
```

### 触发场景

在编辑项目时修改主客户，调用 `addCustomersToProject` 接口时发生。

### 请求数据
```json
{
  "projectId": "d7d22e4f91384e9aa4095d22d51b286c",
  "customerIds": ["134b81ce28054cbb85d27a74964f0d5d"],
  "primaryCustomerId": "1eceebfbb6dd42ed985740efbec86b08"
}
```

---

## 🔍 问题分析

### 唯一约束

数据库中有唯一约束 `uk_project_customer`，防止同一个项目和客户的重复关联：

```sql
UNIQUE KEY `uk_project_customer` (`project_id`, `customer_id`)
```

### 可能的原因

#### 原因 1: 软删除记录导致冲突

如果之前删除过这个客户关联（软删除），数据库中会有：
```sql
project_id = 'd7d22e4f91384e9aa4095d22d51b286c'
customer_id = '134b81ce28054cbb85d27a74964f0d5d'
deleted_at = '2026-03-01 10:00:00'  -- 已软删除
```

当尝试再次插入时，唯一约束会冲突，因为约束不考虑 `deleted_at` 字段。

#### 原因 2: 检查逻辑有漏洞

后端代码中的检查：
```java
boolean exists = projectCustomersMapper.checkCustomerInProject(projectId, customerId);
```

SQL:
```sql
SELECT COUNT(*) > 0
FROM project_customers
WHERE project_id = #{projectId} 
  AND customer_id = #{customerId}
  AND deleted_at IS NULL  -- 只检查未删除的记录
```

如果有软删除的记录，`exists` 会返回 `false`，但插入时仍会违反唯一约束。

#### 原因 3: 前端逻辑问题

前端在编辑时可能错误地将已存在的客户ID添加到 `toAdd` 数组中。

---

## ✅ 解决方案

### 方案 1: 修改唯一约束（推荐）

将唯一约束改为只对未删除的记录生效。

#### MySQL 8.0+ 方案（函数索引）

```sql
-- 删除旧的唯一约束
ALTER TABLE project_customers DROP INDEX uk_project_customer;

-- 创建新的唯一索引（只对未删除的记录）
CREATE UNIQUE INDEX uk_project_customer_active 
ON project_customers (project_id, customer_id, (CASE WHEN deleted_at IS NULL THEN 1 ELSE NULL END));
```

#### MySQL 5.7 方案（触发器）

由于 MySQL 5.7 不支持函数索引，需要使用其他方案。

**方案 A**: 修改表结构，添加 `is_deleted` 字段
```sql
-- 添加字段
ALTER TABLE project_customers ADD COLUMN is_deleted TINYINT(1) DEFAULT 0;

-- 更新现有数据
UPDATE project_customers SET is_deleted = 1 WHERE deleted_at IS NOT NULL;

-- 删除旧约束
ALTER TABLE project_customers DROP INDEX uk_project_customer;

-- 创建新约束（包含 is_deleted）
CREATE UNIQUE INDEX uk_project_customer_active 
ON project_customers (project_id, customer_id, is_deleted);
```

**缺点**: 需要修改代码，所有软删除的地方都要同时更新 `is_deleted` 字段。

---

### 方案 2: 修改后端逻辑（临时方案）

在插入前，先删除（物理删除）已软删除的记录。

#### 修改 `addProjectCustomers` 方法

```java
@Override
@Transactional(rollbackFor = Exception.class)
public int addProjectCustomers(String projectId, List<String> customerIds, String primaryCustomerId)
{
    // ... 参数验证 ...
    
    // 构建批量插入数据
    List<ProjectCustomers> list = new ArrayList<>();
    Date now = new Date();
    
    for (String customerId : customerIds)
    {
        if (StringUtils.isEmpty(customerId))
        {
            continue;
        }
        
        // 检查是否已存在（包括软删除的记录）
        boolean exists = projectCustomersMapper.checkCustomerInProject(projectId, customerId);
        if (exists)
        {
            log.warn("客户{}已关联到项目{}，跳过", customerId, projectId);
            continue;
        }
        
        // 【新增】检查是否有软删除的记录
        ProjectCustomers deleted = projectCustomersMapper.selectDeletedRecord(projectId, customerId);
        if (deleted != null)
        {
            // 物理删除旧记录
            projectCustomersMapper.deleteProjectCustomersById(deleted.getId());
            log.info("删除旧的软删除记录：projectId={}, customerId={}", projectId, customerId);
        }
        
        ProjectCustomers pc = new ProjectCustomers();
        pc.setId(IdUtils.fastSimpleUUID());
        pc.setProjectId(projectId);
        pc.setCustomerId(customerId);
        pc.setRole("OWNER");
        pc.setIsPrimary(customerId.equals(primaryCustomerId));
        pc.setCreatedAt(now);
        
        list.add(pc);
    }
    
    // ... 批量插入 ...
}
```

#### 添加 Mapper 方法

**Mapper 接口**:
```java
/**
 * 查询软删除的记录
 */
public ProjectCustomers selectDeletedRecord(@Param("projectId") String projectId, 
                                           @Param("customerId") String customerId);
```

**Mapper XML**:
```xml
<select id="selectDeletedRecord" resultMap="ProjectCustomersResult">
    SELECT *
    FROM project_customers
    WHERE project_id = #{projectId} 
      AND customer_id = #{customerId}
      AND deleted_at IS NOT NULL
    LIMIT 1
</select>
```

---

### 方案 3: 修改前端逻辑（最简单）

确保前端不会将已存在的客户ID传递给后端。

#### 问题定位

添加详细日志后，查看控制台输出：
```javascript
=== 开始处理客户关联（编辑模式）===
当前客户ID列表: ["客户1", "客户2", "客户3"]
需要添加的客户: ["客户1"]  // ← 如果这里包含已存在的客户，说明前端逻辑有问题
```

#### 可能的原因

1. `getProjectCustomersList` 返回的数据不完整
2. `currentCustomerIds` 提取错误
3. `filter` 逻辑有问题

---

## 🧪 调试步骤

### 步骤 1: 查看前端日志

1. 打开浏览器控制台
2. 编辑项目，修改主客户
3. 点击"确定"
4. 查看日志输出

**关键日志**:
```javascript
=== 开始处理客户关联（编辑模式）===
projectId: "xxx"
newCustomerIds: ["客户1", "客户2", "客户3"]
newPrimaryCustomerId: "客户2"

当前客户列表: [{customerId: "客户1", ...}, ...]
当前客户ID列表: ["客户1", "客户2", "客户3"]

需要添加的客户: []  // ← 应该是空数组
需要移除的客户: []
```

### 步骤 2: 查询数据库

```sql
-- 查询项目的所有客户关联（包括软删除的）
SELECT * FROM project_customers 
WHERE project_id = 'd7d22e4f91384e9aa4095d22d51b286c';

-- 查询特定客户的关联
SELECT * FROM project_customers 
WHERE project_id = 'd7d22e4f91384e9aa4095d22d51b286c'
  AND customer_id = '134b81ce28054cbb85d27a74964f0d5d';
```

**检查**:
- 是否有 `deleted_at IS NOT NULL` 的记录？
- 是否有重复的记录？

### 步骤 3: 查看后端日志

```
客户134b81ce28054cbb85d27a74964f0d5d已关联到项目d7d22e4f91384e9aa4095d22d51b286c，跳过
```

如果看到这条日志，说明后端检查到了重复，但仍然尝试插入，可能是并发问题。

---

## 🔧 临时解决方案

### 方案 A: 清理软删除记录

```sql
-- 物理删除所有软删除的记录
DELETE FROM project_customers WHERE deleted_at IS NOT NULL;
```

**注意**: 这会永久删除数据，无法恢复。

### 方案 B: 修改唯一约束

```sql
-- 临时删除唯一约束
ALTER TABLE project_customers DROP INDEX uk_project_customer;

-- 重新创建（不推荐，会失去数据完整性保护）
-- 或者使用方案 1 的方法
```

---

## 📋 推荐方案

### 短期方案（立即可用）

1. **清理软删除记录**
   ```sql
   DELETE FROM project_customers WHERE deleted_at IS NOT NULL;
   ```

2. **添加前端日志**（已完成）
   - 查看是否是前端逻辑问题

3. **修改后端逻辑**
   - 在插入前物理删除软删除的记录

### 长期方案（需要规划）

1. **修改唯一约束**
   - 使用方案 1 的方法
   - 只对未删除的记录生效

2. **改用物理删除**
   - 不使用软删除
   - 或者软删除时同时修改 `customer_id` 为 `customer_id_deleted_timestamp`

---

## ✅ 立即执行的修复

我将实现方案 2（修改后端逻辑），在插入前检查并删除软删除的记录。

---

**创建时间**: 2026-03-01  
**状态**: ⏳ 等待选择方案
