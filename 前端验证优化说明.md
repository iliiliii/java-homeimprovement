# 前端验证优化说明

**优化时间**: 2026-03-01  
**目的**: 在提交前进行客户关联验证，提供更好的用户体验

---

## 🎯 优化目标

在用户点击"确定"按钮后、发送请求前，进行以下验证：

1. ✅ 项目至少需要一个客户
2. ✅ 不能移除主客户（除非先修改主客户）
3. ✅ 主客户必须在客户列表中

---

## 📝 实现方案

### 1. 保存原始主客户ID

**新增变量**:
```javascript
const originalPrimaryCustomerId = ref(null)  // 保存原始主客户ID
```

**在 `handleEdit` 中保存**:
```javascript
// 找到主客户
const primary = projectCustomers.find(pc => pc.isPrimary)
if (primary) {
  form.value.primaryCustomerId = primary.customerId
  originalPrimaryCustomerId.value = primary.customerId  // ✅ 保存原始值
}
```

---

### 2. 提交前验证

**在 `submitForm` 中添加验证逻辑**:

```javascript
// 编辑模式下，提交前验证客户关联
if (form.value.id != null) {
  const newCustomerIds = form.value.customerIds || []
  const newPrimaryId = form.value.primaryCustomerId
  
  // 验证 1: 至少有一个客户
  if (newCustomerIds.length === 0) {
    proxy.$modal.msgError("项目至少需要一个客户")
    return
  }
  
  // 验证 2: 不能删除原始主客户（除非修改了主客户）
  if (originalPrimaryCustomerId.value && 
      !newCustomerIds.includes(originalPrimaryCustomerId.value) &&
      newPrimaryId === originalPrimaryCustomerId.value) {
    proxy.$modal.msgError("不能移除主客户，请先选择其他客户作为主客户")
    return
  }
  
  // 验证 3: 主客户必须在客户列表中
  if (newPrimaryId && !newCustomerIds.includes(newPrimaryId)) {
    proxy.$modal.msgError("主客户必须在客户列表中")
    return
  }
}
```

---

## 🧪 测试场景

### 场景 1: 删除所有客户 ❌

**操作**:
1. 编辑项目
2. 删除所有客户
3. 点击"确定"

**预期结果**:
- ❌ 提示："项目至少需要一个客户"
- ❌ 不发送请求

---

### 场景 2: 删除主客户（未修改主客户）❌

**初始状态**: 项目有客户A（主）、B、C

**操作**:
1. 编辑项目
2. 删除客户A
3. 主客户仍然是A（未修改）
4. 点击"确定"

**预期结果**:
- ❌ 提示："不能移除主客户，请先选择其他客户作为主客户"
- ❌ 不发送请求

---

### 场景 3: 先修改主客户，再删除旧主客户 ✅

**初始状态**: 项目有客户A（主）、B、C

**操作**:
1. 编辑项目
2. 修改主客户为B
3. 删除客户A
4. 点击"确定"

**预期结果**:
- ✅ 验证通过
- ✅ 发送请求
- ✅ 成功保存

**原因**: 主客户已经从A改为B，所以可以删除A

---

### 场景 4: 主客户不在客户列表中 ❌

**初始状态**: 项目有客户A（主）、B、C

**操作**:
1. 编辑项目
2. 删除客户A、B
3. 主客户仍然是A（未修改）
4. 点击"确定"

**预期结果**:
- ❌ 提示："主客户必须在客户列表中"
- ❌ 不发送请求

---

### 场景 5: 正常修改 ✅

**初始状态**: 项目有客户A（主）、B、C

**操作**:
1. 编辑项目
2. 添加客户D
3. 删除客户C
4. 修改主客户为B
5. 点击"确定"

**预期结果**:
- ✅ 验证通过
- ✅ 发送请求
- ✅ 成功保存

---

## 🔍 验证逻辑详解

### 验证 1: 至少有一个客户

```javascript
if (newCustomerIds.length === 0) {
  proxy.$modal.msgError("项目至少需要一个客户")
  return
}
```

**目的**: 确保项目不会没有客户

**触发条件**: 用户删除了所有客户

---

### 验证 2: 不能删除原始主客户

```javascript
if (originalPrimaryCustomerId.value &&                    // 有原始主客户
    !newCustomerIds.includes(originalPrimaryCustomerId.value) &&  // 新列表中没有原始主客户
    newPrimaryId === originalPrimaryCustomerId.value) {   // 主客户没有改变
  proxy.$modal.msgError("不能移除主客户，请先选择其他客户作为主客户")
  return
}
```

**目的**: 防止用户删除主客户但忘记修改主客户

**触发条件**: 
- 删除了原始主客户
- 但主客户仍然指向原始主客户

**不触发条件**:
- 删除了原始主客户
- 但主客户已经改为其他客户 ✅ 允许

---

### 验证 3: 主客户必须在客户列表中

```javascript
if (newPrimaryId && !newCustomerIds.includes(newPrimaryId)) {
  proxy.$modal.msgError("主客户必须在客户列表中")
  return
}
```

**目的**: 确保主客户ID有效

**触发条件**: 主客户ID不在客户列表中（理论上不应该发生）

---

## 💡 用户体验改进

### 改进前

```
用户操作 → 点击确定 → 发送请求 → 后端验证失败 → 返回错误
                                              ↓
                                    "不能移除主客户"
```

**问题**:
- 需要等待网络请求
- 错误提示在请求返回后才显示
- 用户体验差

---

### 改进后

```
用户操作 → 点击确定 → 前端验证 → 发现错误 → 立即提示
                         ↓
                    验证通过 → 发送请求 → 成功
```

**优点**:
- ✅ 立即反馈，无需等待
- ✅ 减少无效请求
- ✅ 用户体验好

---

## 🎨 交互优化建议

### 建议 1: 禁用删除按钮

在客户选择框中，如果某个客户是主客户，可以禁用其删除按钮。

**实现**:
```vue
<el-select v-model="form.customerIds" multiple>
  <el-option
    v-for="customer in customersList"
    :key="customer.id"
    :label="customer.name"
    :value="customer.id"
    :disabled="customer.id === form.primaryCustomerId"
  />
</el-select>
```

**效果**: 主客户无法被取消选择

---

### 建议 2: 自动切换主客户

当用户尝试删除主客户时，自动将主客户切换到第一个剩余客户。

**实现**:
```javascript
watch(() => form.value.customerIds, (newIds, oldIds) => {
  // 如果主客户被删除
  if (form.value.primaryCustomerId && !newIds.includes(form.value.primaryCustomerId)) {
    // 自动切换到第一个客户
    if (newIds.length > 0) {
      form.value.primaryCustomerId = newIds[0]
      proxy.$modal.msgWarning(`主客户已自动切换为 ${getCustomerName(newIds[0])}`)
    }
  }
})
```

**效果**: 用户删除主客户时，自动切换主客户，无需手动选择

---

### 建议 3: 提示信息优化

当用户选择主客户时，显示提示信息。

**实现**:
```vue
<el-form-item label="主客户" prop="primaryCustomerId">
  <el-select v-model="form.primaryCustomerId">
    <!-- options -->
  </el-select>
  <div style="margin-top: 5px; font-size: 12px; color: #909399">
    <el-icon><InfoFilled /></el-icon>
    主客户将显示在项目列表中，且不能被直接删除
  </div>
</el-form-item>
```

---

## ✅ 总结

### 修改内容
1. ✅ 新增 `originalPrimaryCustomerId` 变量
2. ✅ 在 `handleEdit` 中保存原始主客户ID
3. ✅ 在 `submitForm` 中添加3个验证

### 优点
- ✅ 立即反馈，无需等待网络请求
- ✅ 减少无效请求
- ✅ 提供更好的用户体验
- ✅ 错误提示更清晰

### 后续优化
- 可以考虑自动切换主客户
- 可以考虑禁用主客户的删除
- 可以添加更多提示信息

---

**优化时间**: 2026-03-01  
**状态**: ✅ 已完成
