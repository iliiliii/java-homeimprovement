# 字典功能集成说明

## 功能概述
在API中集成字典查询功能，将数据库中的字典值转换为对应的中文显示文本后返回给小程序。

## 集成的字典类型

### 1. 施工阶段字典
- **字典类型**: `decoration_construction_stage`
- **用途**: 将阶段代码转换为中文名称
- **示例**: `DISMANTLING` → `拆除工程`

### 2. 阶段状态字典  
- **字典类型**: `decoration_construction_stage_status`
- **用途**: 将状态代码转换为中文状态
- **示例**: `IN_PROGRESS` → `进行中`

### 3. 记录类型字典
- **字典类型**: `decoration_record_type`
- **用途**: 将记录类型代码转换为中文类型
- **示例**: `ACCEPTANCE` → `验收`

### 4. 验收结果（硬编码）
- **QUALIFIED** → `合格`
- **UNQUALIFIED** → `不合格`

## 实现方式

### 1. 添加字典查询方法
```java
private String getDictLabel(String dictType, String dictValue) {
    if (StringUtils.isEmpty(dictValue)) {
        return dictValue;
    }
    String label = dashboardMapper.selectDictLabel(dictType, dictValue);
    return label != null ? label : dictValue;
}
```

### 2. 封装具体的字典查询方法
- `getStageText()` - 查询阶段名称
- `getStatusText()` - 查询状态名称  
- `getTypeText()` - 查询记录类型名称
- `getInspectionStatusText()` - 获取验收结果名称

### 3. 在数据返回前设置文本字段
在所有查询方法中，为每个对象设置对应的文本字段：
- `schedule.setStageName()` - 设置阶段名称
- `schedule.setStatusText()` - 设置状态文本
- `record.setTypeText()` - 设置记录类型文本
- `record.setInspectionStatusText()` - 设置验收状态文本

## API返回数据结构

### 项目进度对象 (ProjectScheduleVO)
```json
{
  "id": "schedule_001",
  "stage": "DISMANTLING",
  "stageName": "拆除工程",        // 新增：阶段中文名称
  "status": "COMPLETED", 
  "statusText": "已完成",        // 新增：状态中文文本
  "latestRecords": [...]
}
```

### 验收记录对象 (ProjectScheduleRecordVO)
```json
{
  "id": "record_001",
  "stage": "DISMANTLING",
  "stageName": "拆除工程",           // 新增：阶段中文名称
  "type": "ACCEPTANCE",
  "typeText": "验收",               // 新增：类型中文文本
  "inspectionStatus": "QUALIFIED",
  "inspectionStatusText": "合格"    // 新增：验收结果中文文本
}
```

## 优势
1. **前端简化**: 小程序直接使用中文文本，无需本地字典转换
2. **数据一致性**: 所有文本统一从后端字典表获取
3. **易于维护**: 字典修改只需在后端进行，前端自动更新
4. **性能优化**: 使用缓存机制避免重复查询

## 注意事项
1. 确保数据库中存在对应的字典数据
2. 字典类型名称必须与常量定义一致
3. 如果字典查询失败，返回原始值作为兜底