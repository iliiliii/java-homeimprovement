# 优化版微信登录流程设计

## 核心流程

### 1. 用户点击微信登录
```
点击登录按钮 → 获取微信code → 检查openid绑定状态 → 已绑定直接登录 / 未绑定弹窗绑定手机号
```

### 2. 静默登录检查
```
小程序启动 → 检查本地token → 有效直接进入 / 无效检查微信绑定 → 已绑定静默登录 / 未绑定显示登录界面
```

## 前端实现

### 状态管理
```javascript
// 绑定状态
const bindingStep = ref('check') // check | normal
// check: 检查中
// normal: 正常登录界面

// 用户openid
const userOpenid = ref('')
const isExistingUser = ref(false)
```

### 核心函数

#### 1. 静默登录检查
```javascript
const attemptSilentLogin = async () => {
  // 1. 检查本地token
  const token = uni.getStorageSync('token')
  if (token) {
    try {
      await userStore.validateToken()
      navigateToHome() // token有效，直接进入
      return
    } catch (error) {
      userStore.logout() // token无效，清除
    }
  }
  
  // 2. 尝试微信静默登录
  await checkWechatLoginStatus()
}
```

#### 2. 微信登录状态检查
```javascript
const checkWechatLoginStatus = async () => {
  try {
    bindingStep.value = 'check'
    
    // 获取微信code
    const loginRes = await uni.login({ provider: 'weixin' })
    
    // 检查openid绑定状态
    const checkResult = await checkOpenidBindingStatus(loginRes.code)
    
    if (checkResult.isBound) {
      // 已绑定，直接登录
      await performDirectLogin(checkResult)
    } else {
      // 未绑定，显示登录界面
      userOpenid.value = checkResult.openid
      bindingStep.value = 'normal'
    }
  } catch (error) {
    bindingStep.value = 'normal'
  }
}
```

#### 3. 微信登录处理
```javascript
const handleWechatLogin = async () => {
  // 1. 获取微信code
  const loginRes = await uni.login({ provider: 'weixin' })
  
  // 2. 检查openid绑定状态
  const checkResult = await checkOpenidBindingStatus(loginRes.code)
  
  if (checkResult.isBound) {
    // 已绑定，直接登录
    await performDirectLogin(checkResult)
  } else {
    // 未绑定，显示手机号绑定弹窗
    userOpenid.value = checkResult.openid
    showPhoneBindingModal()
  }
}
```

#### 4. 手机号绑定弹窗
```javascript
const showPhoneBindingModal = () => {
  uni.showModal({
    title: '绑定手机号',
    editable: true,
    placeholderText: '请输入手机号',
    success: async (res) => {
      if (res.confirm && res.content) {
        const phone = res.content.trim()
        
        // 验证手机号格式
        if (!/^1[3-9]\d{9}$/.test(phone)) {
          uni.showToast({ title: '请输入正确的手机号', icon: 'none' })
          setTimeout(() => showPhoneBindingModal(), 1500)
          return
        }
        
        // 执行绑定
        await performPhoneBind(phone)
      }
    }
  })
}
```

## 后端API设计

### 1. 检查openid绑定状态
**接口：** `POST /app/auth/check-openid-binding`

**请求参数：**
```json
{
  "code": "微信登录凭证",
  "deviceId": "设备ID"
}
```

**响应：**
```json
{
  "code": 200,
  "data": {
    "isBound": true,
    "openid": "微信openid",
    "userId": 123,
    "userType": "customer",
    "phone": "138****8888"
  }
}
```

### 2. openid直接登录
**接口：** `POST /app/auth/openid-login`

**请求参数：**
```json
{
  "openid": "微信openid",
  "deviceId": "设备ID"
}
```

**响应：**
```json
{
  "code": 200,
  "data": {
    "accessToken": "JWT令牌",
    "refreshToken": "刷新令牌",
    "userInfo": {...},
    "projects": [...]
  }
}
```

### 3. 绑定手机号到openid
**接口：** `POST /app/auth/bind-phone-to-openid`

**请求参数：**
```json
{
  "openid": "微信openid",
  "phone": "手机号",
  "deviceId": "设备ID"
}
```

**响应：**
```json
{
  "code": 200,
  "data": {
    "accessToken": "JWT令牌",
    "refreshToken": "刷新令牌",
    "userInfo": {...},
    "projects": [...]
  }
}
```

## 后端实现要点

### 1. 绑定状态检查
```java
@Override
public OpenidBindingCheckResponse checkOpenidBinding(CheckOpenidBindingRequest request, String ipAddress) {
    // 1. 通过code获取openid
    IWechatService.WxSession wxSession = wechatService.code2Session(request.getCode());
    String openId = wxSession.getOpenId();
    
    // 2. 检查Redis中的绑定信息
    String bindingKey = WECHAT_BINDING_KEY + openId;
    Map<String, String> binding = redisCache.getCacheMap(bindingKey);
    
    OpenidBindingCheckResponse response = new OpenidBindingCheckResponse();
    response.setOpenid(openId);
    
    if (binding != null && !binding.isEmpty()) {
        // 已绑定
        response.setIsBound(true);
        response.setUserId(Long.valueOf(binding.get("userId")));
        response.setUserType(binding.get("userType"));
        response.setPhone(maskPhone(binding.get("phone")));
    } else {
        // 未绑定
        response.setIsBound(false);
    }
    
    return response;
}
```

### 2. 手机号绑定
```java
@Override
public AppLoginResponse bindPhoneToOpenid(BindPhoneToOpenidRequest request, String ipAddress) {
    String openId = request.getOpenid();
    String phone = request.getPhone();
    
    // 1. 检查手机号是否存在
    UserInfo userInfo = findUserByPhone(phone);
    if (userInfo == null) {
        throw new ServiceException("该手机号未在系统中注册，请联系管理员添加");
    }
    
    // 2. 保存绑定关系到Redis
    Map<String, String> binding = new HashMap<>();
    binding.put("openId", openId);
    binding.put("phone", phone);
    binding.put("userId", userInfo.getUserId().toString());
    binding.put("userType", userInfo.getUserType());
    binding.put("bindTime", String.valueOf(System.currentTimeMillis()));
    
    String bindingKey = WECHAT_BINDING_KEY + openId;
    redisCache.setCacheMap(bindingKey, binding);
    
    // 3. 生成登录响应
    return buildLoginResponse(userInfo);
}
```

## 用户体验优化

### 1. 静默登录
- 已绑定用户无感知登录
- 启动即可直接进入应用

### 2. 简化绑定流程
- 一键点击微信登录
- 弹窗输入手机号即可绑定
- 无需复杂的多步骤操作

### 3. 智能错误处理
- 区分不同错误类型
- 提供针对性解决方案
- 联系管理员功能

### 4. 安全保障
- 白名单机制：只有预先添加的手机号可绑定
- 防重复绑定：一个openid只能绑定一个账号
- 绑定记录：完整的操作日志

## 部署配置

### 1. Redis配置
```yaml
# 绑定信息存储
app:
  wechat:
    binding:
      expire: 0  # 永久有效，或根据业务需求设置过期时间
```

### 2. 微信小程序配置
- 确保服务器域名在白名单中
- 配置正确的AppID和AppSecret
- 开启手机号获取权限（如需要）

### 3. 前端配置
```javascript
// config/contact.js
export const CONTACT_CONFIG = {
  admin: {
    name: '系统管理员',
    phone: '400-xxx-xxxx',
    workTime: '工作日 9:00-18:00'
  }
}
```

## 测试用例

### 1. 首次登录流程
1. 用户首次打开小程序
2. 点击微信登录
3. 弹出手机号输入框
4. 输入已注册手机号
5. 绑定成功，登录完成

### 2. 已绑定用户登录
1. 已绑定用户打开小程序
2. 自动检查绑定状态
3. 静默登录成功
4. 直接进入应用

### 3. 异常情况处理
1. 输入未注册手机号 → 提示联系管理员
2. 网络异常 → 提示检查网络
3. 微信授权失败 → 提示重新打开小程序

这个优化版本大大简化了用户操作流程，提升了登录体验，同时保持了安全性和可控性。