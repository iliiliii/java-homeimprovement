<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.web.mapper.CustomersMapper">
    
    <resultMap type="Customers" id="CustomersResult">
        <result property="id"    column="id"    />
        <result property="name"    column="name"    />
        <result property="phone"    column="phone"    />
        <result property="email"    column="email"    />
        <result property="address"    column="address"    />
        <result property="level"    column="level"    />
        <result property="source"    column="source"    />
        <result property="remarks"    column="remarks"    />
        <result property="avatar"    column="avatar"    />
        <result property="isActive"    column="is_active"    />
        <result property="createdAt"    column="created_at"    />
        <result property="updatedAt"    column="updated_at"    />
        <result property="deletedAt"    column="deleted_at"    />
        <result property="createdBy"    column="created_by"    />
        <result property="updatedBy"    column="updated_by"    />
    </resultMap>

    <resultMap type="Customers" id="CustomersWithProjectCountResult" extends="CustomersResult">
        <result property="projectCount" column="project_count"/>
    </resultMap>

    <sql id="selectCustomersVo">
        select id, name, phone, email, address, level, source, remarks, avatar, is_active, created_at, updated_at, deleted_at, created_by, updated_by from customers
    </sql>

    <select id="selectCustomersList" parameterType="Customers" resultMap="CustomersResult">
        <include refid="selectCustomersVo"/>
        <where>  
            <if test="name != null  and name != ''"> and name like concat('%', #{name}, '%')</if>
            <if test="phone != null  and phone != ''"> and phone like concat('%', #{phone}, '%')</if>
            <if test="source != null  and source != ''"> and source = #{source}</if>
        </where>
    </select>
    
    <select id="selectCustomersById" parameterType="String" resultMap="CustomersResult">
        <include refid="selectCustomersVo"/>
        where id = #{id}
    </select>

    <select id="selectCustomersByPhone" parameterType="String" resultMap="CustomersResult">
        <include refid="selectCustomersVo"/>
        where phone = #{phone}
        limit 1
    </select>

    <insert id="insertCustomers" parameterType="Customers">
        insert into customers
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="name != null and name != ''">name,</if>
            <if test="phone != null and phone != ''">phone,</if>
            <if test="email != null">email,</if>
            <if test="address != null">address,</if>
            <if test="level != null">level,</if>
            <if test="source != null">source,</if>
            <if test="remarks != null">remarks,</if>
            <if test="avatar != null">avatar,</if>
            <if test="isActive != null">is_active,</if>
            <if test="createdAt != null">created_at,</if>
            <if test="updatedAt != null">updated_at,</if>
            <if test="deletedAt != null">deleted_at,</if>
            <if test="createdBy != null">created_by,</if>
            <if test="updatedBy != null">updated_by,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="name != null and name != ''">#{name},</if>
            <if test="phone != null and phone != ''">#{phone},</if>
            <if test="email != null">#{email},</if>
            <if test="address != null">#{address},</if>
            <if test="level != null">#{level},</if>
            <if test="source != null">#{source},</if>
            <if test="remarks != null">#{remarks},</if>
            <if test="avatar != null">#{avatar},</if>
            <if test="isActive != null">#{isActive},</if>
            <if test="createdAt != null">#{createdAt},</if>
            <if test="updatedAt != null">#{updatedAt},</if>
            <if test="deletedAt != null">#{deletedAt},</if>
            <if test="createdBy != null">#{createdBy},</if>
            <if test="updatedBy != null">#{updatedBy},</if>
         </trim>
    </insert>

    <update id="updateCustomers" parameterType="Customers">
        update customers
        <trim prefix="SET" suffixOverrides=",">
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="phone != null and phone != ''">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="address != null">address = #{address},</if>
            <if test="level != null">level = #{level},</if>
            <if test="source != null">source = #{source},</if>
            <if test="remarks != null">remarks = #{remarks},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="isActive != null">is_active = #{isActive},</if>
            <if test="createdAt != null">created_at = #{createdAt},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
            <if test="deletedAt != null">deleted_at = #{deletedAt},</if>
            <if test="createdBy != null">created_by = #{createdBy},</if>
            <if test="updatedBy != null">updated_by = #{updatedBy},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteCustomersById" parameterType="String">
        delete from customers where id = #{id}
    </delete>

    <delete id="deleteCustomersByIds" parameterType="String">
        delete from customers where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 关联查询客户信息（包含项目数量） -->
    <sql id="selectCustomersWithProjectCountVo">
        SELECT
            c.id, c.name, c.phone, c.email, c.address, c.level, c.source,
            c.remarks, c.avatar, c.is_active, c.created_at, c.updated_at,
            c.deleted_at, c.created_by, c.updated_by,
            COUNT(p.id) as project_count
        FROM customers c
        LEFT JOIN projects p ON c.id = p.customer_id AND p.deleted_at IS NULL
    </sql>

    <select id="selectCustomersWithProjectCount" parameterType="Customers" resultMap="CustomersWithProjectCountResult">
        <include refid="selectCustomersWithProjectCountVo"/>
        <where>
            <if test="name != null and name != ''"> and c.name like concat('%', #{name}, '%')</if>
            <if test="phone != null and phone != ''"> and c.phone like concat('%', #{phone}, '%')</if>
            <if test="source != null and source != ''"> and c.source = #{source}</if>
        </where>
        GROUP BY c.id, c.name, c.phone, c.email, c.address, c.level, c.source,
                 c.remarks, c.avatar, c.is_active, c.created_at, c.updated_at,
                 c.deleted_at, c.created_by, c.updated_by
        ORDER BY c.created_at DESC
    </select>

    <select id="selectCustomersWithProjectCountById" parameterType="String" resultMap="CustomersWithProjectCountResult">
        <include refid="selectCustomersWithProjectCountVo"/>
        WHERE c.id = #{id}
        GROUP BY c.id, c.name, c.phone, c.email, c.address, c.level, c.source,
                 c.remarks, c.avatar, c.is_active, c.created_at, c.updated_at,
                 c.deleted_at, c.created_by, c.updated_by
    </select>
</mapper>