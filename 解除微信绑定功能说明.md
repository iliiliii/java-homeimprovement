# 解除微信绑定功能说明

## 功能概述
新增了解除微信绑定功能，允许用户安全地解除微信账号与手机号的绑定关系。

## 🔧 技术实现

### 后端接口
- **接口路径**: `POST /app/auth/unbind-wechat`
- **功能**: 解除指定openid的绑定关系
- **验证方式**: 支持手机号验证码和密码验证

### 验证方式

#### 1. 手机号验证码验证
- 适用于所有用户类型（客户和员工）
- 需要输入绑定的手机号
- 发送验证码到手机号
- 输入正确验证码完成验证

#### 2. 密码验证
- 仅适用于员工用户
- 输入登录密码进行验证
- 验证通过后解除绑定

## 📋 请求参数

```json
{
  "openid": "微信openid",
  "deviceId": "设备ID", 
  "verifyType": "验证方式(phone/password)",
  "phone": "手机号(手机号验证时必填)",
  "code": "验证码(手机号验证时必填)",
  "password": "密码(密码验证时必填)"
}
```

## 🔒 安全特性

### 身份验证
- **双重验证**: 必须提供openid和身份验证信息
- **验证码保护**: 手机号验证需要正确的短信验证码
- **密码保护**: 员工可使用登录密码验证身份

### 权限控制
- **绑定验证**: 只能解除已存在的绑定关系
- **手机号匹配**: 手机号验证时必须与绑定手机号一致
- **用户类型检查**: 密码验证仅限员工用户

### 操作审计
- **详细日志**: 记录解除绑定的操作日志
- **错误追踪**: 记录失败原因和错误信息

## 🧪 测试方法

### 1. API测试
```bash
# 手机号验证码方式
curl -X POST "http://192.168.5.102:8080/app/auth/unbind-wechat" \
  -H "Content-Type: application/json" \
  -d '{
    "openid": "mock_openid_6610",
    "deviceId": "test_device_001", 
    "verifyType": "phone",
    "phone": "13812345678",
    "code": "123456"
  }'

# 密码验证方式（员工）
curl -X POST "http://192.168.5.102:8080/app/auth/unbind-wechat" \
  -H "Content-Type: application/json" \
  -d '{
    "openid": "mock_openid_xxx",
    "deviceId": "test_device_001",
    "verifyType": "password", 
    "password": "admin123"
  }'
```

### 2. 前端测试
在测试页面 `/pages/test/wechat-binding` 中：
1. 确保已有绑定关系
2. 点击"解除绑定"按钮
3. 选择验证方式
4. 完成身份验证
5. 确认解除绑定成功

## 📱 用户体验

### 操作流程
1. **确认对话框**: 显示解除绑定的后果提醒
2. **验证方式选择**: 手机号验证码或密码验证
3. **身份验证**: 根据选择的方式进行验证
4. **结果反馈**: 显示解除绑定结果

### 错误处理
- **绑定不存在**: 提示该微信账号未绑定
- **手机号不匹配**: 提示输入的手机号与绑定手机号不一致
- **验证码错误**: 提示验证码错误或已过期
- **密码错误**: 提示密码不正确
- **权限不足**: 提示当前用户类型不支持该验证方式

## 🔄 完整测试流程

### 测试场景1：手机号验证码解除绑定
1. 确保有绑定关系
2. 选择手机号验证方式
3. 输入绑定的手机号
4. 获取并输入验证码
5. 确认解除绑定
6. 验证绑定关系已删除

### 测试场景2：密码验证解除绑定（员工）
1. 使用员工账号绑定微信
2. 选择密码验证方式
3. 输入正确的登录密码
4. 确认解除绑定
5. 验证绑定关系已删除

### 测试场景3：错误处理
1. 输入错误手机号
2. 输入错误验证码
3. 输入错误密码
4. 客户尝试使用密码验证
5. 验证错误提示正确

## 📊 数据库影响

### 操作结果
- **删除记录**: 从`app_wechat_bindings`表中删除对应记录
- **数据一致性**: 确保删除操作的原子性
- **审计日志**: 在应用日志中记录操作详情

### 验证查询
```sql
-- 查看解除绑定前的记录
SELECT * FROM app_wechat_bindings WHERE open_id = 'mock_openid_6610';

-- 解除绑定后验证记录已删除
SELECT COUNT(*) FROM app_wechat_bindings WHERE open_id = 'mock_openid_6610';
-- 应该返回 0
```

## 🚀 部署说明

### 更新内容
- ✅ 新增`UnbindWechatRequest` DTO
- ✅ 新增`unbindWechat`服务方法
- ✅ 新增`/app/auth/unbind-wechat`接口
- ✅ 前端新增`unbindWechat` API方法
- ✅ 测试页面新增解除绑定功能

### 重启服务
```bash
cd sb3
java -jar ruoyi-admin/target/ruoyi-admin.jar
```

### 测试验证
```bash
./simple-test.sh
```

## 🎯 使用建议

### 安全建议
1. **生产环境**: 建议启用短信验证码功能
2. **验证码有效期**: 设置合理的验证码过期时间
3. **操作频率限制**: 考虑添加解除绑定的频率限制
4. **通知机制**: 解除绑定后可发送通知短信

### 用户体验建议
1. **操作确认**: 多次确认避免误操作
2. **后果说明**: 清楚说明解除绑定的影响
3. **重新绑定**: 提供便捷的重新绑定入口
4. **帮助文档**: 提供详细的操作说明

---

**功能状态**: ✅ 开发完成，可进行测试