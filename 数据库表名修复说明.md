# 数据库表名修复说明

## 问题分析
API调用失败的原因是表名不匹配：
- **错误**: 我们的Mapper XML使用了 `project_schedule` (单数)
- **正确**: 实际数据库表名是 `project_schedules` (复数)

## 已修复的内容

### 1. 修正了 AppProjectScheduleMapper.xml 中的表名
- `project_schedule` → `project_schedules`
- `project_schedule_record` → `project_schedule_records`

### 2. 修正了字段映射
- 去掉了不存在的 `del_flag` 字段过滤
- 修正了字段名映射：
  - `stage_name` → `stage` (作为别名)
  - `title` → `acceptance_title`
  - `description` → `acceptance_content`
  - `type` → `record_type`
  - `inspection_status` → `acceptance_result`

### 3. 修正了图片数据处理
- 实际表中图片存储在 `images` 字段（JSON格式）
- 不是单独的附件表
- 添加了 `parseImagesJson()` 方法解析JSON数据

### 4. 修正了 Mapper 接口
- `selectRecordImages()` 返回类型从 `List<String>` 改为 `String`

## 需要执行的SQL脚本

请在数据库中执行以下脚本插入测试数据：
```
insert_test_data_correct_tables.sql
```

这个脚本会：
1. 清空现有测试数据
2. 插入5个施工阶段的进度记录
3. 插入6条验收记录（包含图片JSON数据）
4. 验证数据插入结果

## 测试验证

执行SQL脚本后，重新启动后端服务，API应该能正常工作：
- `/app/projectSchedules/list` - 获取项目进度列表
- `/app/projectScheduleRecords/list` - 获取验收记录列表

前端页面应该能正确显示：
- 施工进度时间轴
- 各阶段的状态（待开始/进行中/已完成）
- 验收记录和图片

## 注意事项
1. 测试数据使用项目ID: `5066c822136f45ec896d840cb725e110`
2. 如果你的项目ID不同，请修改SQL脚本中的 `project_id` 值
3. 图片URL使用了示例地址，实际使用时需要替换为真实的图片URL