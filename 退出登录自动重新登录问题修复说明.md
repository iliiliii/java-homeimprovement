# 退出登录自动重新登录问题修复说明

## 问题描述
用户点击退出登录后，回到登录页面时，页面会立即检测登录状态并自动重新登录，导致用户无法真正退出。

## 问题原因分析

### 原始流程
1. 用户点击退出登录
2. `userStore.logout()` 清除所有登录信息
3. 跳转到登录页面 `/pages/login/index-new`
4. 登录页面 `onMounted` 执行 `attemptSilentLogin()`
5. `attemptSilentLogin()` 调用 `checkWechatLoginStatus()`
6. `checkWechatLoginStatus()` 自动获取微信 code 并检查绑定状态
7. 如果微信账号已绑定，自动执行登录

### 问题根源
登录页面无法区分用户是：
- **主动退出登录** - 不应该自动重新登录
- **正常进入登录页** - 可以进行静默登录检查

## 解决方案

### 1. 添加退出登录标记
在 `userStore.logout()` 中添加标记：

```javascript
const logout = () => {
  // ... 清除登录状态的代码 ...
  
  // 设置主动退出标记，防止自动重新登录
  uni.setStorageSync('manualLogout', true)
  
  // 清除其他存储但保留退出标记
  // ... 清除存储的代码 ...
  
  // 重新设置退出标记（因为clearStorageSync会清除所有）
  uni.setStorageSync('manualLogout', manualLogout)
  
  // 跳转登录页
  uni.reLaunch({ url: '/pages/login/index-new' })
}
```

### 2. 修改登录页面逻辑
在登录页面 `onMounted` 中检查退出标记：

```javascript
onMounted(() => {
  // 检查是否是主动退出登录
  const manualLogout = uni.getStorageSync('manualLogout')
  if (manualLogout) {
    // 清除退出标记
    uni.removeStorageSync('manualLogout')
    // 主动退出登录，不进行自动登录检查，直接显示登录界面
    console.log('[登录页面] 检测到主动退出登录，跳过自动登录检查')
    bindingStep.value = 'normal'
    return
  }
  
  // 正常进入登录页面，尝试静默登录检查
  attemptSilentLogin()
})
```

## 修复后的流程

### 主动退出登录流程
1. 用户点击退出登录
2. `userStore.logout()` 设置 `manualLogout = true`
3. 清除登录信息并跳转登录页
4. 登录页面检测到 `manualLogout = true`
5. 清除标记，直接显示登录界面（`bindingStep = 'normal'`）
6. **不执行自动登录检查**

### 正常进入登录页流程
1. 用户首次打开小程序或其他方式进入登录页
2. 登录页面检测到 `manualLogout` 不存在或为 `false`
3. 执行 `attemptSilentLogin()` 进行静默登录检查
4. 如果有有效 token 或微信已绑定，自动登录

## 技术实现细节

### 存储管理
- 使用 `uni.setStorageSync('manualLogout', true)` 设置标记
- 在 `uni.clearStorageSync()` 后重新设置标记（避免被清除）
- 登录页面检查后立即清除标记 `uni.removeStorageSync('manualLogout')`

### 状态控制
- `bindingStep.value = 'normal'` - 直接显示登录界面
- 跳过 `attemptSilentLogin()` 和 `checkWechatLoginStatus()` 的执行

### 日志记录
添加控制台日志便于调试：
```javascript
console.log('[登录页面] 检测到主动退出登录，跳过自动登录检查')
```

## 测试验证

### 测试场景1：主动退出登录
1. 用户正常登录进入系统
2. 点击退出登录按钮
3. 跳转到登录页面
4. **预期结果**：显示登录界面，不自动重新登录

### 测试场景2：正常进入登录页
1. 首次打开小程序
2. 或清除缓存后打开
3. **预期结果**：执行静默登录检查，如果已绑定则自动登录

### 测试场景3：Token过期
1. 用户在使用过程中 token 过期
2. 系统自动跳转到登录页
3. **预期结果**：执行静默登录检查

## 优势

### 用户体验
- ✅ 用户主动退出后不会被强制重新登录
- ✅ 保持正常的静默登录功能
- ✅ 明确的登录状态控制

### 技术优势
- ✅ 简单可靠的实现方案
- ✅ 不影响现有的登录流程
- ✅ 易于维护和调试

### 兼容性
- ✅ 向后兼容，不影响现有功能
- ✅ 适用于所有登录方式（微信、短信、密码）

---

**修复完成时间**: 2025年1月20日  
**问题类型**: 用户体验优化  
**影响范围**: 登录/退出流程