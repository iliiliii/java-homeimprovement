# 验收记录权限优化方案

## 一、问题分析

### 1.1 目标1: 修复createBy字段不一致问题

**当前问题**:
- App模块: 存储用户ID（从Token中提取的userId）
- Web模块: 存储用户名（从SecurityUtils.getUsername()获取）

**影响**:
- 数据类型不一致导致权限验证逻辑混乱
- 无法准确判断记录创建者
- 影响编辑/删除权限判断

### 1.2 目标2: 扩展操作权限范围

**当前逻辑**:
- 仅记录创建人可以编辑/删除

**需求变更**:
- 项目团队成员均可编辑/删除验收记录
- 需要查询项目成员列表进行权限判断

---

## 二、API对比分析

### 2.1 vue3项目成员API

**API路径**: `/evs/projectMembers/list`

**文件**: `vue3/src/api/evs/projectMembers.js`

```javascript
// 查询项目成员列表
export function listProjectMembers(query) {
  return request({
    url: '/evs/projectMembers/list',
    method: 'get',
    params: query  // 可传入 projectId 进行筛选
  })
}
```

**后端控制器**: `ProjectMembersController.java`
- 路径: `/evs/projectMembers/list`
- 方法: GET
- 权限: `@PreAuthorize("@ss.hasPermi('evs:projectMembers:list')")`
- 返回: 分页数据 `TableDataInfo`

### 2.2 uni3项目成员API

**API路径**: `/app/dashboard/project/{projectId}/members`

**文件**: `uni3/src/api/dashboard.js`

```javascript
/**
 * 获取项目成员列表
 * @param {String} projectId - 项目ID
 * @returns {Promise} 返回项目成员列表
 */
export const getProjectMembers = (projectId, options = {}) => {
  return get(`/app/dashboard/project/${projectId}/members`, {}, options)
}
```

**后端控制器**: `AppDashboardController.java`
- 路径: `/app/dashboard/project/{projectId}/members`
- 方法: GET
- 权限: 无需认证，公开访问
- 返回: 直接返回成员列表数组

**后端服务**: `AppProjectMemberServiceImpl.java`
- 查询SQL: `userMapper.selectProjectMembers(projectId)`
- 返回VO: `ProjectMemberVO`（包含id, userId, name, post, avatar, role）

### 2.3 API差异总结

| 特性 | vue3 API | uni3 API |
|------|----------|----------|
| 路径 | `/evs/projectMembers/list` | `/app/dashboard/project/{projectId}/members` |
| 参数传递 | query参数 | 路径参数 |
| 权限要求 | 需要权限验证 | 无需认证 |
| 返回格式 | 分页数据（TableDataInfo） | 直接数组 |
| 数据来源 | project_members表 | project_members表 + sys_user表联查 |
| 包含信息 | 完整成员信息 | 用户信息+岗位+角色 |

---

## 三、权限验证逻辑分析

### 3.1 当前权限验证方法

**checkStaffProjectAccess** (AppDashboardMapper.xml)

```sql
SELECT COUNT(*) > 0
FROM project_members
WHERE user_id = #{userId} AND project_id = #{projectId} AND is_active = 1
```

**用途**:
- 验证员工是否有权限访问项目
- 用于项目进度、质检记录等模块的权限控制

**调用位置**:
- `AppProjectScheduleServiceImpl.validateTokenAndAccess()`
- `AppQualityIssueServiceImpl.validateTokenAndAccess()`
- `AppDashboardServiceImpl.validateTokenAndAccess()`

### 3.2 项目成员查询方法

**selectProjectMembers** (AppUserMapper)

```java
List<Map<String, Object>> selectProjectMembers(String projectId);
```

**返回字段**:
- id: 成员记录ID
- userId: 用户ID
- nickName: 昵称
- userName: 用户名
- postNames: 岗位名称
- avatar: 头像
- role: 项目角色

---

## 四、修复方案

### 4.1 目标1: 统一createBy字段

#### 方案选择

**推荐方案**: 统一存储用户ID

**理由**:
1. 用户ID是唯一标识，更适合做关联查询
2. 用户名可能重复或变更
3. 与project_members表的user_id字段保持一致
4. 便于后续扩展权限验证逻辑

#### 修改步骤

**步骤1: 修改App模块（已正确）**

App模块已经存储userId，无需修改。

**步骤2: 修改Web模块**

文件: `sb3/evs-home/src/main/java/com/ruoyi/web/service/impl/ProjectScheduleRecordsServiceImpl.java`

```java
@Override
public int insertProjectScheduleRecords(ProjectScheduleRecords projectScheduleRecords)
{
    // 如果 id 为空，自动生成 UUID
    if (projectScheduleRecords.getId() == null || projectScheduleRecords.getId().isEmpty()) {
        projectScheduleRecords.setId(IdUtils.fastSimpleUUID());
    }
    projectScheduleRecords.setCreateTime(DateUtils.getNowDate());
    
    // ✅ 修改：存储用户ID而非用户名
    projectScheduleRecords.setCreateBy(SecurityUtils.getUserId().toString());
    
    return projectScheduleRecordsMapper.insertProjectScheduleRecords(projectScheduleRecords);
}

@Override
public int updateProjectScheduleRecords(ProjectScheduleRecords projectScheduleRecords)
{
    projectScheduleRecords.setUpdateTime(DateUtils.getNowDate());
    
    // ✅ 修改：存储用户ID而非用户名
    projectScheduleRecords.setUpdateBy(SecurityUtils.getUserId().toString());
    
    return projectScheduleRecordsMapper.updateProjectScheduleRecords(projectScheduleRecords);
}
```

**步骤3: 数据迁移（可选）**

如果需要迁移历史数据，执行以下SQL：

```sql
-- 备份原createBy数据
ALTER TABLE project_schedule_records ADD COLUMN create_by_backup VARCHAR(64);
UPDATE project_schedule_records SET create_by_backup = create_by;

-- 将用户名转换为用户ID
UPDATE project_schedule_records psr
INNER JOIN sys_user su ON psr.create_by = su.user_name
SET psr.create_by = CAST(su.user_id AS CHAR)
WHERE psr.create_by NOT REGEXP '^[0-9]+$';

-- 验证迁移结果
SELECT 
  COUNT(*) as total,
  SUM(CASE WHEN create_by REGEXP '^[0-9]+$' THEN 1 ELSE 0 END) as user_id_format,
  SUM(CASE WHEN create_by NOT REGEXP '^[0-9]+$' THEN 1 ELSE 0 END) as username_format
FROM project_schedule_records;
```

### 4.2 目标2: 扩展操作权限

#### 权限判断逻辑

**原逻辑**: 仅创建人可操作
```java
String createBy = projectScheduleMapper.selectRecordCreateBy(recordId);
if (!userId.equals(createBy)) {
    throw new ServiceException("只能编辑自己创建的记录", 403);
}
```

**新逻辑**: 项目团队成员均可操作
```java
// 1. 查询记录所属项目
ProjectScheduleRecordVO record = projectScheduleMapper.selectProjectScheduleRecordById(recordId);
String projectId = record.getProjectId();

// 2. 检查用户是否为项目成员
boolean isMember = dashboardMapper.checkStaffProjectAccess(userId, projectId);
if (!isMember) {
    throw new ServiceException("只有项目成员可以操作验收记录", 403);
}
```

#### 修改文件

**文件1**: `AppProjectScheduleServiceImpl.java`

```java
@Override
public void updateAcceptanceRecord(String token, String projectId, String recordId, AcceptanceRecordRequest request) {
    // 验证Token并检查是否为员工用户
    Map<String, Object> claims = tokenManager.validateToken(extractToken(token));
    String userType = (String) claims.get("userType");
    String userId = claims.get("userId").toString();

    // 仅员工可以编辑验收记录
    if (!"staff".equals(userType)) {
        throw new ServiceException("仅员工可以编辑验收记录", 403);
    }

    // 查询记录是否存在
    ProjectScheduleRecordVO existingRecord = projectScheduleMapper.selectProjectScheduleRecordById(recordId);
    if (existingRecord == null) {
        throw new ServiceException("验收记录不存在", 404);
    }

    // ✅ 修改：检查是否为项目成员（而非仅创建者）
    boolean isMember = dashboardMapper.checkStaffProjectAccess(userId, projectId);
    if (!isMember) {
        throw new ServiceException("只有项目成员可以编辑验收记录", 403);
    }

    try {
        // ... 后续更新逻辑
    }
}

@Override
public void deleteAcceptanceRecord(String token, String projectId, String recordId) {
    // 验证Token并检查是否为员工用户
    Map<String, Object> claims = tokenManager.validateToken(extractToken(token));
    String userType = (String) claims.get("userType");
    String userId = claims.get("userId").toString();

    // 仅员工可以删除验收记录
    if (!"staff".equals(userType)) {
        throw new ServiceException("仅员工可以删除验收记录", 403);
    }

    // 查询记录是否存在
    ProjectScheduleRecordVO existingRecord = projectScheduleMapper.selectProjectScheduleRecordById(recordId);
    if (existingRecord == null) {
        throw new ServiceException("验收记录不存在", 404);
    }

    // ✅ 修改：检查是否为项目成员（而非仅创建者）
    boolean isMember = dashboardMapper.checkStaffProjectAccess(userId, projectId);
    if (!isMember) {
        throw new ServiceException("只有项目成员可以删除验收记录", 403);
    }

    try {
        // ... 后续删除逻辑
    }
}
```

**文件2**: `ProjectScheduleRecordsServiceImpl.java` (Web模块)

```java
@Override
public int updateProjectScheduleRecords(ProjectScheduleRecords projectScheduleRecords)
{
    // ✅ 新增：权限验证
    String currentUserId = SecurityUtils.getUserId().toString();
    String projectId = projectScheduleRecords.getProjectId();
    
    // 检查是否为项目成员
    if (!isProjectMember(currentUserId, projectId)) {
        throw new ServiceException("只有项目成员可以编辑验收记录");
    }
    
    projectScheduleRecords.setUpdateTime(DateUtils.getNowDate());
    projectScheduleRecords.setUpdateBy(currentUserId);
    return projectScheduleRecordsMapper.updateProjectScheduleRecords(projectScheduleRecords);
}

@Override
public int deleteProjectScheduleRecordsByIds(String[] ids)
{
    // ✅ 新增：批量删除时验证每条记录的权限
    String currentUserId = SecurityUtils.getUserId().toString();
    
    for (String id : ids) {
        ProjectScheduleRecords record = projectScheduleRecordsMapper.selectProjectScheduleRecordsById(id);
        if (record != null) {
            String projectId = record.getProjectId();
            if (!isProjectMember(currentUserId, projectId)) {
                throw new ServiceException("只有项目成员可以删除验收记录");
            }
        }
    }
    
    return projectScheduleRecordsMapper.deleteProjectScheduleRecordsByIds(ids);
}

/**
 * 检查用户是否为项目成员
 */
private boolean isProjectMember(String userId, String projectId) {
    // 调用Mapper查询
    return projectScheduleRecordsMapper.checkUserIsProjectMember(userId, projectId);
}
```

**文件3**: `ProjectScheduleRecordsMapper.java`

```java
/**
 * 检查用户是否为项目成员
 * 
 * @param userId 用户ID
 * @param projectId 项目ID
 * @return true-是项目成员，false-不是
 */
boolean checkUserIsProjectMember(@Param("userId") String userId, @Param("projectId") String projectId);
```

**文件4**: `ProjectScheduleRecordsMapper.xml`

```xml
<!-- 检查用户是否为项目成员 -->
<select id="checkUserIsProjectMember" resultType="boolean">
    SELECT COUNT(*) > 0
    FROM project_members
    WHERE user_id = #{userId} AND project_id = #{projectId} AND is_active = 1
</select>
```

#### 前端修改

**uni3修改**: `uni3/src/pages/log/components/ScheduleRecord.vue`

```vue
<script setup>
// 是否是项目成员（通过项目成员列表判断）
const isProjectMember = computed(() => {
  // 如果没有项目成员数据，默认允许操作（兼容旧逻辑）
  if (!props.projectMembers || props.projectMembers.length === 0) {
    return true
  }
  
  // 检查当前用户是否在项目成员列表中
  return props.projectMembers.some(member => member.userId === userStore.userId)
})

// 是否可以操作记录（员工 + 项目成员）
const canOperate = computed(() => {
  return isStaff.value && isProjectMember.value
})

// 修改操作按钮显示条件
// 原: v-if="isStaff && isOwnRecord"
// 新: v-if="canOperate"
</script>
```

**vue3修改**: `vue3/src/views/evs/projectScheduleRecords/components/AcceptanceRecordsDrawer.vue`

```vue
<script setup>
import { useProjectAuth } from '@/utils/projectAuth'

// 权限控制
const { isAdmin, isProjectMember } = useProjectAuth()

// 是否可以操作（管理员或项目成员）
const canOperate = computed(() => {
  return isAdmin.value || isProjectMember.value
})

// 修改操作按钮显示条件
// 原: v-if="isAdmin"
// 新: v-if="canOperate"
</script>
```

---

## 五、测试验证

### 5.1 createBy字段验证

**测试步骤**:
1. Web端新增验收记录，检查create_by字段是否为用户ID
2. App端新增验收记录，检查create_by字段是否为用户ID
3. 查询数据库，确认两端数据格式一致

**验证SQL**:
```sql
-- 检查最近10条记录的createBy格式
SELECT 
  id,
  create_by,
  CASE 
    WHEN create_by REGEXP '^[0-9]+$' THEN 'user_id'
    ELSE 'username'
  END as format_type,
  create_time
FROM project_schedule_records
ORDER BY create_time DESC
LIMIT 10;
```

### 5.2 权限验证测试

**测试场景1**: 项目成员编辑他人创建的记录
- 用户A创建验收记录
- 用户B（同项目成员）尝试编辑
- 预期: 编辑成功

**测试场景2**: 非项目成员尝试编辑
- 用户A创建验收记录
- 用户C（非项目成员）尝试编辑
- 预期: 提示"只有项目成员可以编辑验收记录"

**测试场景3**: 项目成员删除他人创建的记录
- 用户A创建验收记录
- 用户B（同项目成员）尝试删除
- 预期: 删除成功

**测试场景4**: 前端按钮显示
- 项目成员: 显示编辑/删除按钮
- 非项目成员: 不显示编辑/删除按钮

---

## 六、实施计划

### 6.1 实施顺序

1. **阶段1**: 修复createBy字段（优先级：高）
   - 修改Web模块Service层
   - 执行数据迁移SQL（可选）
   - 测试验证

2. **阶段2**: 扩展操作权限（优先级：中）
   - 修改后端权限验证逻辑
   - 添加Mapper方法
   - 修改前端权限判断
   - 测试验证

### 6.2 风险评估

**风险1**: 数据迁移可能失败
- **影响**: 部分历史记录无法关联到用户
- **缓解**: 先备份数据，迁移失败可回滚

**风险2**: 权限逻辑变更影响现有功能
- **影响**: 可能导致部分用户无法操作记录
- **缓解**: 充分测试，灰度发布

**风险3**: 前端兼容性问题
- **影响**: 旧版本前端可能显示异常
- **缓解**: 前后端同步发布

### 6.3 回滚方案

**createBy字段回滚**:
```sql
-- 恢复原createBy数据
UPDATE project_schedule_records 
SET create_by = create_by_backup
WHERE create_by_backup IS NOT NULL;
```

**权限逻辑回滚**:
- 恢复原Service层代码
- 重新部署

---

## 七、总结

### 7.1 修改文件清单

**后端文件**:
1. `ProjectScheduleRecordsServiceImpl.java` - 修改createBy存储逻辑
2. `AppProjectScheduleServiceImpl.java` - 修改权限验证逻辑
3. `ProjectScheduleRecordsMapper.java` - 新增权限检查方法
4. `ProjectScheduleRecordsMapper.xml` - 新增SQL查询

**前端文件**:
1. `uni3/src/pages/log/components/ScheduleRecord.vue` - 修改权限判断
2. `vue3/src/views/evs/projectScheduleRecords/components/AcceptanceRecordsDrawer.vue` - 修改权限判断

**数据库**:
1. 数据迁移SQL（可选）

### 7.2 预期效果

1. ✅ createBy字段统一为用户ID，数据一致性提升
2. ✅ 项目团队成员均可编辑/删除验收记录，协作效率提升
3. ✅ 权限验证逻辑清晰，安全性提升
4. ✅ 前后端权限判断一致，用户体验提升

---

**文档版本**: v1.0
**创建时间**: 2025-01-15
**作者**: Kiro AI Assistant
