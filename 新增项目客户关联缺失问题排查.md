# 新增项目客户关联缺失问题排查

**问题时间**: 2026-03-01  
**问题描述**: 新增项目时选择3个客户，但只调用了 `POST /evs/projects` 接口，没有调用 `addCustomersToProject` 接口

---

## 🔍 问题现象

### 用户操作
1. 点击"新增"按钮
2. 填写项目名称："1"
3. 选择3个客户
4. 填写地址："123"
5. 填写面积："1"
6. 点击"确定"

### 实际请求
```
只有一个请求：
POST http://localhost/dev-api/evs/projects
```

### 预期请求
```
应该有两个请求：
1. POST http://localhost/dev-api/evs/projects
2. POST http://localhost/dev-api/evs/projectCustomers/addCustomers
```

### 结果
- ✅ 项目创建成功
- ❌ 客户关联未创建
- ❌ 编辑时只显示1个客户（主客户）

---

## 🔍 排查步骤

### 步骤 1: 检查前端代码

**文件**: `vue3/src/views/evs/projects/components/ProjectEdit.vue`

**检查点 1**: 导入语句
```javascript
import { getProjectCustomersList, addCustomersToProject, setPrimaryCustomer } from '@/api/evs/projectCustomers'
```
✅ 导入正确

**检查点 2**: API 定义
```javascript
// vue3/src/api/evs/projectCustomers.js
export function addCustomersToProject(data) {
  return request({
    url: '/evs/projectCustomers/addCustomers',
    method: 'post',
    data: data
  })
}
```
✅ API 定义正确

**检查点 3**: 提交逻辑
```javascript
addProjects(submitData).then(response => {
  const projectId = response.data?.id || response.data
  
  if (projectId && submitData.customerIds && submitData.customerIds.length > 0) {
    addCustomersToProject({
      projectId: projectId,
      customerIds: submitData.customerIds,
      primaryCustomerId: submitData.primaryCustomerId
    })
  }
})
```
✅ 逻辑正确

---

### 步骤 2: 添加详细日志

我已经在代码中添加了非常详细的日志，包括：

1. **流程开始**
   ```javascript
   console.log('=== 开始新增项目流程 ===')
   ```

2. **项目创建成功**
   ```javascript
   console.log('=== 项目创建成功 ===')
   console.log('完整响应:', response)
   console.log('response.data:', response.data)
   ```

3. **提取项目ID**
   ```javascript
   console.log('=== 提取项目ID ===')
   console.log('projectId:', projectId)
   console.log('projectId 类型:', typeof projectId)
   ```

4. **检查条件**
   ```javascript
   console.log('=== 检查添加客户条件 ===')
   console.log('projectId 存在?', !!projectId)
   console.log('customerIds 存在?', !!submitData.customerIds)
   console.log('customerIds 长度 > 0?', submitData.customerIds && submitData.customerIds.length > 0)
   ```

5. **调用 API**
   ```javascript
   console.log('=== 调用 addCustomersToProject ===')
   console.log('请求数据:', JSON.stringify(customerData, null, 2))
   ```

6. **成功或失败**
   ```javascript
   console.log('=== 客户关联添加成功 ===')
   // 或
   console.error('=== 客户关联添加失败 ===')
   ```

---

## 🧪 测试步骤

### 1. 清除浏览器缓存
```
1. 打开浏览器开发者工具（F12）
2. 右键点击刷新按钮
3. 选择"清空缓存并硬性重新加载"
```

### 2. 打开控制台
```
1. 切换到 Console 标签
2. 清空现有日志
3. 确保没有过滤器（显示所有级别的日志）
```

### 3. 执行新增操作
```
1. 点击"新增"按钮
2. 填写项目信息
3. 选择 3 个客户
4. 点击"确定"
```

### 4. 查看控制台日志

**预期看到的日志顺序**:
```
=== 开始新增项目流程 ===
提交数据: {name: "1", customerIds: [...], ...}

=== 项目创建成功 ===
完整响应: {code: 200, msg: "操作成功", data: {...}}
response.data: {id: "xxx", name: "1", ...}
response.data?.id: "xxx"

=== 提取项目ID ===
projectId: "xxx"
projectId 类型: "string"
customerIds: ["客户1", "客户2", "客户3"]
customerIds 长度: 3
primaryCustomerId: "客户1"

=== 检查添加客户条件 ===
projectId 存在? true
customerIds 存在? true
customerIds 长度 > 0? true

=== 条件满足，准备添加客户 ===
请求数据: {
  "projectId": "xxx",
  "customerIds": ["客户1", "客户2", "客户3"],
  "primaryCustomerId": "客户1"
}

=== 调用 addCustomersToProject ===

=== 客户关联添加成功 ===
添加响应: {code: 200, msg: "操作成功", data: 3}
```

---

## 🔍 可能的问题原因

### 原因 1: JavaScript 错误导致代码中断

**症状**: 
- 只看到部分日志
- 在某个 `console.log` 之后就没有日志了

**解决**:
- 查看控制台是否有红色错误信息
- 检查是否有语法错误或运行时错误

---

### 原因 2: 条件判断失败

**症状**:
- 看到 `=== 跳过客户关联 ===` 日志
- 条件检查显示某个值为 false

**可能情况**:

#### 情况 A: projectId 为空或 undefined
```javascript
// 检查日志
projectId: undefined  // ❌
projectId 类型: "undefined"
```

**原因**: 后端返回的数据结构不符合预期

**解决**: 检查后端返回的数据格式
```java
// 确保返回的是完整的项目对象
return AjaxResult.success(project);  // ✅
// 而不是
return AjaxResult.success();  // ❌
```

#### 情况 B: customerIds 为空数组
```javascript
// 检查日志
customerIds: []  // ❌
customerIds 长度: 0
```

**原因**: submitData 中的 customerIds 被清空了

**解决**: 检查 submitData 的构建逻辑

#### 情况 C: projectId 是对象而不是字符串
```javascript
// 检查日志
projectId: {id: "xxx", name: "1", ...}  // ❌
projectId 类型: "object"
```

**原因**: `response.data` 本身就是项目对象

**解决**: 修改提取逻辑
```javascript
const projectId = response.data?.id || response.data?.id || (typeof response.data === 'string' ? response.data : null)
```

---

### 原因 3: addCustomersToProject 函数未定义

**症状**:
- 看到 `=== 调用 addCustomersToProject ===` 日志
- 然后出现错误：`addCustomersToProject is not a function`

**解决**:
- 检查导入语句
- 检查 API 文件是否正确导出

---

### 原因 4: 网络请求失败

**症状**:
- 看到 `=== 调用 addCustomersToProject ===` 日志
- 然后看到 `=== 客户关联添加失败 ===` 日志
- 错误信息显示网络错误或 500 错误

**解决**:
- 查看 Network 标签，检查请求是否发送
- 检查请求参数是否正确
- 检查后端接口是否正常

---

### 原因 5: 后端接口问题

**症状**:
- 请求发送成功
- 但返回 400/500 错误

**检查**:
1. 后端接口路径是否正确
2. 参数格式是否匹配
3. 后端是否有权限控制
4. 后端日志是否有错误

---

## 📋 检查清单

### 前端检查
- [ ] 浏览器控制台是否有 JavaScript 错误？
- [ ] 是否看到 `=== 开始新增项目流程 ===` 日志？
- [ ] 是否看到 `=== 项目创建成功 ===` 日志？
- [ ] projectId 的值和类型是什么？
- [ ] customerIds 的值和长度是什么？
- [ ] 是否看到 `=== 条件满足，准备添加客户 ===` 日志？
- [ ] 是否看到 `=== 调用 addCustomersToProject ===` 日志？
- [ ] Network 标签中是否有 `/evs/projectCustomers/addCustomers` 请求？

### 后端检查
- [ ] 后端接口是否正常启动？
- [ ] `/evs/projectCustomers/addCustomers` 接口是否存在？
- [ ] 接口是否需要特殊权限？
- [ ] 后端日志是否有错误？

---

## 🔧 临时解决方案

如果问题难以定位，可以使用以下临时方案：

### 方案 1: 后端自动创建客户关联

恢复 `ProjectsServiceImpl.insertProjects()` 中的自动同步逻辑：

```java
@Override
@Transactional(rollbackFor = Exception.class)
public int insertProjects(Projects projects) {
    if (projects.getId() == null || projects.getId().isEmpty()) {
        projects.setId(IdUtils.fastSimpleUUID());
    }
    projects.setCreatedAt(DateUtils.getNowDate());
    projects.setCreatedBy(SecurityUtils.getUsername());
    
    // 插入项目
    int result = projectsMapper.insertProjects(projects);
    
    // 自动创建主客户关联
    if (result > 0 && projects.getCustomerId() != null && !projects.getCustomerId().isEmpty()) {
        syncProjectCustomer(projects.getId(), projects.getCustomerId(), "insert");
    }
    
    return result;
}
```

**缺点**: 只能创建主客户关联，其他客户需要前端单独添加

---

### 方案 2: 使用组合接口

创建一个新的后端接口，一次性完成项目创建和客户关联：

```java
@PostMapping("/createWithCustomers")
public AjaxResult createProjectWithCustomers(@RequestBody CreateProjectRequest request) {
    // 1. 创建项目
    Projects project = request.getProject();
    projectsService.insertProjects(project);
    
    // 2. 添加客户关联
    if (request.getCustomerIds() != null && !request.getCustomerIds().isEmpty()) {
        projectCustomersService.addProjectCustomers(
            project.getId(),
            request.getCustomerIds(),
            request.getPrimaryCustomerId()
        );
    }
    
    return AjaxResult.success(project);
}
```

**优点**: 
- 在一个事务中完成
- 失败时全部回滚
- 逻辑更清晰

---

## 📊 下一步行动

1. **立即执行**: 按照测试步骤操作，查看详细日志
2. **记录结果**: 将控制台的完整日志复制下来
3. **分析原因**: 根据日志确定是哪个环节出了问题
4. **针对性修复**: 根据具体原因选择修复方案

---

**创建时间**: 2026-03-01  
**状态**: ⏳ 等待测试结果
