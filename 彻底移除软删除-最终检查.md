# 彻底移除软删除 - 最终检查

**修复时间**: 2026-03-01  
**状态**: ✅ 所有软删除已改为物理删除

---

## ✅ 已修复的地方

### 1. ProjectCustomersServiceImpl.java ✅

**方法**: `removeProjectCustomer`

**修改前**:
```java
int result = projectCustomersMapper.softDelete(list.get(0).getId());
```

**修改后**:
```java
int result = projectCustomersMapper.deleteProjectCustomersById(list.get(0).getId());
log.info("成功从项目{}移除客户{}（物理删除）", projectId, customerId);
```

---

### 2. ProjectsServiceImpl.java ✅

**方法**: `syncProjectCustomer` - update 操作

**修改前**:
```java
projectCustomersMapper.softDelete(old.getId());
log.info("同步软删除旧的 project_customers 记录...");
```

**修改后**:
```java
projectCustomersMapper.deleteProjectCustomersById(old.getId());
log.info("同步物理删除旧的 project_customers 记录...");
```

---

**方法**: `syncProjectCustomer` - delete 操作

**修改前**:
```java
projectCustomersMapper.softDelete(pc.getId());
log.info("同步软删除项目的所有 project_customers 记录...");
```

**修改后**:
```java
projectCustomersMapper.deleteProjectCustomersById(pc.getId());
log.info("同步物理删除项目的所有 project_customers 记录...");
```

---

### 3. ProjectCustomersMapper.xml ✅

**方法**: `deleteProjectCustomersById` 和 `deleteProjectCustomersByIds`

**修改前**:
```xml
<update id="deleteProjectCustomersById">
    UPDATE project_customers
    SET deleted_at = NOW(), updated_at = NOW()
    WHERE id = #{id}
</update>
```

**修改后**:
```xml
<delete id="deleteProjectCustomersById">
    DELETE FROM project_customers WHERE id = #{id}
</delete>
```

---

### 4. ProjectEdit.vue ✅

**新增**: 导入 `removeCustomerFromProject`

```javascript
import { 
  getProjectCustomersList, 
  addCustomersToProject, 
  removeCustomerFromProject,  // ✅ 新增
  setPrimaryCustomer 
} from '@/api/evs/projectCustomers'
```

**新增**: 移除客户逻辑

```javascript
// 1. 先移除客户（物理删除）
if (toRemove.length > 0) {
  for (const customerId of toRemove) {
    await removeCustomerFromProject({
      projectId: projectId,
      customerId: customerId
    })
  }
}
```

---

## 🔍 检查清单

### 代码检查 ✅

- [x] `ProjectCustomersServiceImpl.removeProjectCustomer` - 使用物理删除
- [x] `ProjectCustomersServiceImpl.addProjectCustomers` - 不再处理软删除记录
- [x] `ProjectsServiceImpl.syncProjectCustomer` (update) - 使用物理删除
- [x] `ProjectsServiceImpl.syncProjectCustomer` (delete) - 使用物理删除
- [x] `ProjectCustomersMapper.xml.deleteProjectCustomersById` - 使用 DELETE
- [x] `ProjectCustomersMapper.xml.deleteProjectCustomersByIds` - 使用 DELETE
- [x] `ProjectEdit.vue` - 导入并使用 `removeCustomerFromProject`

### 编译检查 ✅

```bash
cd sb3/evs-home
mvn clean compile -DskipTests
```

**结果**: ✅ BUILD SUCCESS

---

## 🗑️ 保留但不使用的方法

### ProjectCustomersMapper.softDelete

**位置**: `ProjectCustomersMapper.xml`

```xml
<!-- 软删除 -->
<update id="softDelete">
    UPDATE project_customers
    SET deleted_at = NOW(), updated_at = NOW()
    WHERE id = #{id}
</update>
```

**状态**: 保留但不再使用

**原因**: 
- Mapper 接口中仍有声明
- 为了向后兼容，保留定义
- 但所有调用都已改为 `deleteProjectCustomersById`

---

## 📋 数据库清理

### 必须执行的 SQL

```sql
-- 1. 查看所有软删除的记录
SELECT 
    id, 
    project_id, 
    customer_id, 
    deleted_at,
    created_at
FROM project_customers 
WHERE deleted_at IS NOT NULL
ORDER BY deleted_at DESC;

-- 2. 物理删除所有软删除的记录
DELETE FROM project_customers 
WHERE deleted_at IS NOT NULL;

-- 3. 验证清理结果
SELECT COUNT(*) as soft_deleted_count
FROM project_customers 
WHERE deleted_at IS NOT NULL;
-- 应该返回 0

-- 4. 查看当前所有有效记录
SELECT 
    project_id,
    customer_id,
    is_primary,
    created_at
FROM project_customers 
WHERE deleted_at IS NULL
ORDER BY project_id, is_primary DESC, created_at;
```

---

## 🧪 测试步骤

### 步骤 1: 清理数据库

执行上面的 SQL，确保没有软删除的记录。

---

### 步骤 2: 重启后端服务

```bash
cd sb3/ruoyi-admin
mvn clean package -DskipTests
java -jar target/ruoyi-admin.jar
```

等待服务完全启动。

---

### 步骤 3: 清除浏览器缓存

F12 → 右键刷新按钮 → 清空缓存并硬性重新加载

---

### 步骤 4: 测试删除后再添加

这是最关键的测试！

```
1. 新增项目，选择客户A、B、C
2. 保存
3. 编辑项目，删除客户C
4. 保存
5. 查询数据库：
   SELECT * FROM project_customers WHERE project_id = 'xxx';
   -- 应该只有A、B，没有C的记录（包括软删除的）
   
6. 再次编辑项目，添加客户C
7. 保存
8. 应该成功，不会报唯一约束错误 ✅
9. 查询数据库：
   SELECT * FROM project_customers WHERE project_id = 'xxx';
   -- 应该有A、B、C，都是新记录
```

---

### 步骤 5: 测试修改主客户

```
1. 项目有客户A（主）、B、C
2. 编辑项目，修改主客户为B
3. 保存
4. 查询数据库：
   SELECT * FROM project_customers 
   WHERE project_id = 'xxx' AND is_primary = 1;
   -- 应该只有B是主客户
```

---

### 步骤 6: 测试批量操作

```
1. 项目有客户A（主）、B、C
2. 编辑项目：
   - 删除B
   - 添加D、E
   - 修改主客户为C
3. 保存
4. 查询数据库：
   SELECT * FROM project_customers WHERE project_id = 'xxx';
   -- 应该有A、C（主）、D、E
   -- 没有B的记录（包括软删除的）
```

---

## ⚠️ 如果仍然出现唯一约束错误

### 检查 1: 数据库是否清理干净

```sql
SELECT * FROM project_customers WHERE deleted_at IS NOT NULL;
```

如果有记录，说明数据库没有清理干净。

---

### 检查 2: 后端是否重启

查看后端日志，确认：
- 看到 "Started RuoYiApplication"
- 日志中显示 "物理删除" 而不是 "软删除"

---

### 检查 3: 是否使用了旧的 JAR 包

```bash
# 确保使用最新编译的 JAR
cd sb3/ruoyi-admin
rm -rf target
mvn clean package -DskipTests
java -jar target/ruoyi-admin.jar
```

---

### 检查 4: 查看后端日志

当删除客户时，应该看到：
```
成功从项目xxx移除客户yyy（物理删除）
```

而不是：
```
成功从项目xxx移除客户yyy  // 没有"物理删除"字样
```

---

## 📊 修改总结

### 修改的文件

1. ✅ `ProjectCustomersServiceImpl.java` - 2处修改
2. ✅ `ProjectsServiceImpl.java` - 2处修改
3. ✅ `ProjectCustomersMapper.xml` - 2处修改
4. ✅ `ProjectEdit.vue` - 1处修改（导入）

### 修改的方法

1. ✅ `removeProjectCustomer` - 改用物理删除
2. ✅ `syncProjectCustomer` (update) - 改用物理删除
3. ✅ `syncProjectCustomer` (delete) - 改用物理删除
4. ✅ `deleteProjectCustomersById` - 改用 DELETE
5. ✅ `deleteProjectCustomersByIds` - 改用 DELETE

### 不再使用的方法

- `softDelete` - 保留但不再调用

---

## ✅ 最终确认

- [x] 所有代码中的 `softDelete` 调用都已改为 `deleteProjectCustomersById`
- [x] Mapper XML 中的删除方法都使用 `DELETE` 而不是 `UPDATE`
- [x] 前端已导入 `removeCustomerFromProject`
- [x] 编译通过
- [ ] 数据库已清理（需要手动执行 SQL）
- [ ] 后端已重启（需要手动重启）
- [ ] 功能测试通过（需要手动测试）

---

**修复时间**: 2026-03-01  
**状态**: ✅ 代码已完全修复，等待数据库清理和测试
