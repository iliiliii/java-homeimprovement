# 代码安全分析报告

## 修改概览

### 修改的文件统计
- **后端文件**：8个（包括新增3个）
- **前端文件**：5个（包括新增1个）
- **文档文件**：10个（新增）

### 代码变更统计
```
13 files changed, 739 insertions(+), 215 deletions(-)
```

---

## 安全分析

### 1. 后端权限验证修改

#### 文件：AppProjectScheduleServiceImpl.java & AppDashboardServiceImpl.java

**修改内容：**
```java
private void validateTokenAndAccess(String token, String projectId) {
    // 1. 未登录用户访问演示项目
    if (token == null || token.isEmpty() || "null".equals(token)) {
        boolean isDemo = guestConfigService.isGuestDemoProject(projectId);
        if (!isDemo) {
            throw new ServiceException("未登录用户只能访问演示项目");
        }
        return;
    }
    
    // 2. Token验证（添加异常处理）
    Map<String, Object> claims = null;
    try {
        claims = tokenManager.validateToken(extractToken(token));
    } catch (Exception e) {
        // Token验证失败，检查是否为演示项目
        boolean isDemo = guestConfigService.isGuestDemoProject(projectId);
        if (isDemo) {
            return; // 降级访问
        }
        throw new ServiceException("Token无效或已过期");
    }
    
    // 3. 游客用户权限验证
    if ("guest".equals(userType)) {
        boolean hasAccess = guestConfigService.validateGuestProjectAccess(projectId);
        if (!hasAccess) {
            throw new ServiceException("游客只能访问演示项目");
        }
        return;
    }
    
    // 4. 正常用户权限验证
    // ...
}
```

#### ⚠️ 潜在安全问题

**问题1：降级访问可能被滥用**

**风险等级：** 🟡 中等

**描述：**
- Token验证失败时，如果是演示项目就允许访问
- 攻击者可以使用任意无效token访问演示项目
- 虽然只能访问演示项目，但可能导致日志污染

**影响范围：**
- 仅限演示项目
- 不影响正常用户数据

**建议修复：**
```java
// 添加降级访问计数和限流
private static final int MAX_DEGRADED_ACCESS_PER_IP = 100; // 每IP每小时最多100次降级访问

try {
    claims = tokenManager.validateToken(extractToken(token));
} catch (Exception e) {
    log.warn("[权限验证] Token验证失败: {}, IP: {}", e.getMessage(), ipAddress);
    
    // 检查降级访问频率
    if (isDegradedAccessExceeded(ipAddress)) {
        throw new ServiceException("访问频率过高，请稍后再试");
    }
    
    boolean isDemo = guestConfigService.isGuestDemoProject(projectId);
    if (isDemo) {
        recordDegradedAccess(ipAddress); // 记录降级访问
        return;
    }
    throw new ServiceException("Token无效或已过期");
}
```

---

**问题2：演示项目ID可能被枚举**

**风险等级：** 🟡 中等

**描述：**
- 攻击者可以尝试不同的projectId，找出哪些是演示项目
- 通过响应差异（200 vs 500）可以判断项目是否存在

**影响范围：**
- 可能泄露演示项目ID
- 可能泄露项目是否存在

**建议修复：**
```java
// 统一错误响应，避免信息泄露
if (!isDemo) {
    log.warn("[权限验证] 未登录用户尝试访问非演示项目: {}", projectId);
    // 不要区分"项目不存在"和"无权访问"
    throw new ServiceException("无权访问该项目");
}
```

---

**问题3：缺少访问日志审计**

**风险等级：** 🟢 低

**描述：**
- 降级访问没有详细的审计日志
- 难以追踪异常访问行为

**建议修复：**
```java
// 添加详细的审计日志
if (isDemo) {
    log.info("[安全审计] 降级访问演示项目: projectId={}, token={}, ip={}, userAgent={}", 
        projectId, 
        maskToken(token), 
        ipAddress, 
        request.getHeader("User-Agent")
    );
    return;
}
```

---

### 2. 前端项目ID存储

#### 文件：uni3/src/pages/log/index.vue & design/index.vue

**修改内容：**
```javascript
const initProjectId = async () => {
  const projectId = await getCurrentProjectId(urlProjectId.value, userStore.currentProjectId)
  currentProjectId.value = projectId
  
  // 将项目ID保存到storage
  if (projectId) {
    uni.setStorageSync('currentProjectId', projectId)
  }
}
```

#### ⚠️ 潜在安全问题

**问题4：Storage可能被篡改**

**风险等级：** 🟡 中等

**描述：**
- 用户可以通过开发者工具修改 `uni.storage` 中的 `currentProjectId`
- 修改后可以尝试访问其他项目

**影响范围：**
- 后端有权限验证，无法访问无权限的项目
- 但可能导致错误的API请求

**建议修复：**
```javascript
// 添加项目ID验证
const initProjectId = async () => {
  const projectId = await getCurrentProjectId(urlProjectId.value, userStore.currentProjectId)
  
  // 验证项目ID格式（防止注入）
  if (projectId && !isValidProjectId(projectId)) {
    console.error('[Security] 无效的项目ID格式:', projectId)
    return
  }
  
  currentProjectId.value = projectId
  
  if (projectId) {
    uni.setStorageSync('currentProjectId', projectId)
  }
}

// 项目ID格式验证
const isValidProjectId = (id) => {
  // UUID格式：32个十六进制字符
  return /^[a-f0-9]{32}$/i.test(id)
}
```

---

### 3. API地址配置

#### 文件：uni3/src/config/guest.js

**修改内容：**
```javascript
import { getBaseUrl } from '@/utils/request'

const baseUrl = getBaseUrl()
const apiUrl = `${baseUrl}/app/guest/demo-projects`
```

#### ✅ 安全评估

**优点：**
- 使用统一的API地址管理
- 避免硬编码
- 环境自动切换

**无明显安全问题**

---

### 4. 请求拦截器修改

#### 文件：uni3/src/utils/request.js

**修改内容：**
```javascript
const requestInterceptor = (config) => {
  const token = uni.getStorageSync('token')
  if (token && token !== '' && token !== 'null') {
    config.header = {
      ...config.header,
      'Authorization': `Bearer ${token}`
    }
  } else {
    // 未登录时，不添加Authorization头，但仍然允许请求
    console.log('[Request] 未登录用户，不添加Authorization头')
  }
  
  // 添加项目ID
  const currentProjectId = uni.getStorageSync('currentProjectId')
  if (currentProjectId) {
    config.header['X-Project-Id'] = currentProjectId
  }
  
  return config
}
```

#### ⚠️ 潜在安全问题

**问题5：项目ID未验证就添加到请求头**

**风险等级：** 🟡 中等

**描述：**
- 从storage读取的项目ID直接添加到请求头
- 没有格式验证
- 可能被注入恶意内容

**建议修复：**
```javascript
const requestInterceptor = (config) => {
  // ... token处理 ...
  
  // 添加项目ID（添加验证）
  const currentProjectId = uni.getStorageSync('currentProjectId')
  if (currentProjectId) {
    // 验证项目ID格式
    if (isValidProjectId(currentProjectId)) {
      config.header['X-Project-Id'] = currentProjectId
      console.log('[Request] 添加项目ID到请求头:', currentProjectId)
    } else {
      console.error('[Security] 无效的项目ID格式，已忽略:', currentProjectId)
      // 清除无效的项目ID
      uni.removeStorageSync('currentProjectId')
    }
  }
  
  return config
}

// 项目ID格式验证
const isValidProjectId = (id) => {
  return typeof id === 'string' && /^[a-f0-9]{32}$/i.test(id)
}
```

---

### 5. 游客配置服务

#### 文件：GuestConfigServiceImpl.java

**修改内容：**
```java
@Override
public List<String> getGuestDemoProjectIds() {
    List<SysDictData> dictDataList = DictUtils.getDictCache(GUEST_DEMO_DICT_TYPE);
    
    if (dictDataList == null || dictDataList.isEmpty()) {
        return new ArrayList<>();
    }
    
    List<String> projectIds = dictDataList.stream()
            .filter(dict -> dict.getDictLabel() != null && dict.getDictLabel().startsWith("projects_"))
            .map(SysDictData::getDictValue)
            .filter(StringUtils::isNotEmpty)
            .collect(Collectors.toList());
    
    return projectIds;
}
```

#### ✅ 安全评估

**优点：**
- 使用字典配置，便于管理
- 有空值过滤
- 使用Stream API，代码清晰

**无明显安全问题**

---

## 安全漏洞总结

### 🔴 高危漏洞
**无**

### 🟡 中危漏洞

1. **降级访问可能被滥用**
   - 位置：`validateTokenAndAccess()` 方法
   - 影响：可能导致日志污染、资源浪费
   - 建议：添加限流机制

2. **演示项目ID可能被枚举**
   - 位置：`validateTokenAndAccess()` 方法
   - 影响：信息泄露
   - 建议：统一错误响应

3. **Storage数据可能被篡改**
   - 位置：前端storage操作
   - 影响：错误的API请求
   - 建议：添加格式验证

4. **项目ID未验证就添加到请求头**
   - 位置：`request.js` 请求拦截器
   - 影响：可能的注入攻击
   - 建议：添加格式验证

### 🟢 低危问题

5. **缺少访问日志审计**
   - 位置：权限验证方法
   - 影响：难以追踪异常行为
   - 建议：添加详细审计日志

---

## 功能正确性分析

### ✅ 正确的设计

1. **分层权限验证**
   - 未登录 → 演示项目
   - Token无效 → 演示项目（降级）
   - Token有效 → 正常权限验证

2. **降级策略**
   - Token验证失败时不直接拒绝
   - 检查是否为演示项目
   - 提供容错能力

3. **环境自动切换**
   - 使用 `getBaseUrl()` 统一管理
   - 开发/生产环境自动适配

### ⚠️ 潜在的逻辑问题

**问题6：游客登录后token格式问题未根治**

**描述：**
- 当前方案是在token验证失败时降级访问
- 但没有解决游客登录时token格式不正确的根本问题

**建议：**
检查游客登录逻辑，确保生成正确的JWT token：

```java
// 游客登录时应该生成有效的JWT token
public AppLoginResponse guestLogin(GuestLoginRequest request) {
    // 创建游客用户信息
    Map<String, Object> claims = new HashMap<>();
    claims.put("userId", guestUser.getId());
    claims.put("userType", "guest");
    claims.put("name", "游客用户");
    claims.put("projectIds", demoProjectIds);
    
    // 生成标准JWT token
    String accessToken = tokenManager.createAccessToken(claims);
    String refreshToken = tokenManager.createRefreshToken(claims);
    
    // 返回登录响应
    return new AppLoginResponse(accessToken, refreshToken, ...);
}
```

---

## 性能影响分析

### 1. 额外的数据库查询

**影响：**
- 每次权限验证都会调用 `guestConfigService.isGuestDemoProject()`
- 该方法会查询字典缓存

**优化建议：**
```java
// 在Service中添加本地缓存
private static final Map<String, Boolean> DEMO_PROJECT_CACHE = new ConcurrentHashMap<>();
private static final long CACHE_EXPIRE_TIME = 5 * 60 * 1000; // 5分钟

private boolean isGuestDemoProjectCached(String projectId) {
    // 检查缓存
    Boolean cached = DEMO_PROJECT_CACHE.get(projectId);
    if (cached != null) {
        return cached;
    }
    
    // 查询并缓存
    boolean isDemo = guestConfigService.isGuestDemoProject(projectId);
    DEMO_PROJECT_CACHE.put(projectId, isDemo);
    
    // 定时清理缓存
    scheduleCache Cleanup();
    
    return isDemo;
}
```

### 2. 异常处理开销

**影响：**
- Token验证失败会抛出异常
- 异常处理有性能开销

**评估：**
- 只有token无效时才会触发
- 正常用户不受影响
- 性能影响可接受

---

## 兼容性分析

### ✅ 向后兼容

1. **不影响现有用户**
   - 正常用户的权限验证逻辑不变
   - 只是添加了游客和未登录用户的处理

2. **不影响现有API**
   - API接口没有变化
   - 只是权限验证逻辑更宽松

### ⚠️ 需要注意的点

1. **字典配置依赖**
   - 需要在后台配置 `guest_demo` 字典
   - 如果字典不存在，游客无法访问

2. **演示项目数据**
   - 需要确保演示项目有数据
   - 否则用户看到空页面

---

## 测试建议

### 1. 安全测试

```bash
# 测试1：未登录访问演示项目
curl -X GET "http://api/app/projectSchedules/list" \
  -H "X-Project-Id: demo-project-id"

# 测试2：无效token访问演示项目
curl -X GET "http://api/app/projectSchedules/list" \
  -H "Authorization: Bearer invalid-token" \
  -H "X-Project-Id: demo-project-id"

# 测试3：无效token访问非演示项目
curl -X GET "http://api/app/projectSchedules/list" \
  -H "Authorization: Bearer invalid-token" \
  -H "X-Project-Id: other-project-id"

# 测试4：项目ID注入测试
curl -X GET "http://api/app/projectSchedules/list" \
  -H "X-Project-Id: '; DROP TABLE projects; --"

# 测试5：频率限制测试
for i in {1..200}; do
  curl -X GET "http://api/app/projectSchedules/list" \
    -H "Authorization: Bearer invalid-token" \
    -H "X-Project-Id: demo-project-id"
done
```

### 2. 功能测试

1. 未登录用户访问演示项目
2. 游客登录后访问演示项目
3. 正常用户访问自己的项目
4. 员工用户访问有权限的项目
5. 项目切换功能
6. 页面刷新后项目ID保持

### 3. 性能测试

1. 并发访问测试（100个并发请求）
2. 缓存效果测试
3. 数据库查询次数统计

---

## 修复优先级

### P0 - 立即修复

**无**

### P1 - 高优先级（建议在上线前修复）

1. ✅ **添加项目ID格式验证**
   - 前端：`request.js` 和页面初始化
   - 后端：请求参数验证

2. ✅ **统一错误响应**
   - 避免通过响应差异枚举项目

### P2 - 中优先级（建议在下个版本修复）

3. ✅ **添加限流机制**
   - 防止降级访问被滥用

4. ✅ **添加审计日志**
   - 记录所有降级访问

5. ✅ **解决游客token格式问题**
   - 确保游客登录时生成正确的JWT

### P3 - 低优先级（优化项）

6. ✅ **添加本地缓存**
   - 减少字典查询次数

7. ✅ **前端token格式检查**
   - 避免发送无效token

---

## 总体评估

### 安全性评分：⭐⭐⭐⭐☆ (4/5)

**优点：**
- ✅ 后端有完整的权限验证
- ✅ 降级策略设计合理
- ✅ 不影响正常用户安全

**缺点：**
- ⚠️ 缺少输入验证
- ⚠️ 缺少限流机制
- ⚠️ 缺少详细审计日志

### 功能性评分：⭐⭐⭐⭐⭐ (5/5)

**优点：**
- ✅ 完整实现了需求
- ✅ 未登录和游客都能访问演示项目
- ✅ 有降级容错机制
- ✅ 不影响现有功能

### 可维护性评分：⭐⭐⭐⭐☆ (4/5)

**优点：**
- ✅ 代码结构清晰
- ✅ 有详细的文档
- ✅ 日志输出完整

**缺点：**
- ⚠️ 部分逻辑可以抽取复用

---

## 建议的修复代码

### 1. 添加项目ID验证工具类

```java
// sb3/evs-home/src/main/java/com/ruoyi/app/util/ProjectIdValidator.java
public class ProjectIdValidator {
    
    private static final Pattern PROJECT_ID_PATTERN = Pattern.compile("^[a-f0-9]{32}$", Pattern.CASE_INSENSITIVE);
    
    /**
     * 验证项目ID格式
     */
    public static boolean isValid(String projectId) {
        if (StringUtils.isEmpty(projectId)) {
            return false;
        }
        return PROJECT_ID_PATTERN.matcher(projectId).matches();
    }
    
    /**
     * 验证并抛出异常
     */
    public static void validate(String projectId) {
        if (!isValid(projectId)) {
            throw new ServiceException("无效的项目ID格式");
        }
    }
}
```

### 2. 添加限流器

```java
// sb3/evs-home/src/main/java/com/ruoyi/app/util/RateLimiter.java
@Component
public class RateLimiter {
    
    private final Map<String, AtomicInteger> counters = new ConcurrentHashMap<>();
    private final Map<String, Long> timestamps = new ConcurrentHashMap<>();
    
    /**
     * 检查是否超过限制
     * @param key 限流key（如IP地址）
     * @param maxRequests 最大请求数
     * @param windowSeconds 时间窗口（秒）
     */
    public boolean isExceeded(String key, int maxRequests, int windowSeconds) {
        long now = System.currentTimeMillis();
        Long timestamp = timestamps.get(key);
        
        // 检查时间窗口
        if (timestamp == null || now - timestamp > windowSeconds * 1000) {
            // 重置计数器
            counters.put(key, new AtomicInteger(1));
            timestamps.put(key, now);
            return false;
        }
        
        // 增加计数
        int count = counters.get(key).incrementAndGet();
        return count > maxRequests;
    }
}
```

### 3. 前端项目ID验证

```javascript
// uni3/src/utils/validator.js
/**
 * 验证项目ID格式
 * @param {string} projectId - 项目ID
 * @returns {boolean} 是否有效
 */
export const isValidProjectId = (projectId) => {
  if (!projectId || typeof projectId !== 'string') {
    return false
  }
  // UUID格式：32个十六进制字符
  return /^[a-f0-9]{32}$/i.test(projectId)
}

/**
 * 清理无效的项目ID
 */
export const cleanInvalidProjectId = () => {
  const projectId = uni.getStorageSync('currentProjectId')
  if (projectId && !isValidProjectId(projectId)) {
    console.warn('[Security] 检测到无效的项目ID，已清除:', projectId)
    uni.removeStorageSync('currentProjectId')
  }
}
```

---

## 结论

本次修改总体上是**安全可控**的，主要风险点已识别并提供了修复建议。

**建议：**
1. 在上线前实施 P1 优先级的修复（项目ID验证、统一错误响应）
2. 在下个版本实施 P2 优先级的修复（限流、审计日志）
3. 进行充分的安全测试和功能测试
4. 监控生产环境的降级访问日志

**总体评价：** ✅ 可以上线，但建议先修复 P1 问题

---

**报告版本：** 1.0  
**分析时间：** 2026-02-13  
**分析人员：** Kiro AI Assistant
