# 最终字段修复说明

## 问题根源
通过仔细分析现有的 `ProjectScheduleRecordsMapper.xml`，发现 `project_schedule_records` 表中**没有 `remark` 字段**。

## 实际表结构
根据 `ProjectScheduleRecordsMapper.xml` 中的 `selectProjectScheduleRecordsVo`，实际字段为：
```sql
id, project_id, schedule_id, record_type, images, 
acceptance_title, acceptance_content, acceptance_result, 
acceptance_time, acceptor, created_at, updated_at, 
created_by, updated_by
```

## 已修复的内容

### 1. 移除不存在的字段
- 从所有查询中移除了 `psr.remark` 字段
- 从结果映射中移除了 `remark` 属性

### 2. 正确的字段映射
```sql
-- 实际数据库字段 → VO对象属性
psr.acceptance_title as title           -- 验收标题
psr.acceptance_content as description   -- 验收内容
psr.record_type as type                 -- 记录类型
psr.created_at as create_time          -- 创建时间
psr.created_by as create_by            -- 创建人ID
psr.acceptor as create_by_name         -- 验收人姓名
psr.acceptance_result as inspection_status -- 验收结果
```

### 3. 修复的查询方法
- `selectLatestRecordsByScheduleId` - 查询最新验收记录
- `selectProjectScheduleRecordList` - 查询验收记录列表（分页）
- `selectProjectScheduleRecordById` - 查询验收记录详情

## 测试验证

修复完成后，请：
1. 重新编译项目
2. 重启后端服务
3. 执行 `insert_test_data_correct_tables.sql` 插入测试数据
4. 测试API调用：`/app/projectSchedules/list`

## 预期结果
- API能正常返回项目进度列表
- 每个进度节点包含对应的验收记录
- 前端页面能正确显示施工进度时间轴
- 验收记录显示正确的标题、内容、时间和验收人信息

## 注意事项
- 所有字段映射都基于实际的数据库表结构
- 移除了所有不存在的字段引用
- 使用 `acceptor` 字段作为验收人姓名显示