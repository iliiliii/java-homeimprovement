# 项目进度数据库表创建说明

## 问题描述
API调用失败，错误信息显示：`Table 'new_home.project_schedule' doesn't exist`

## 解决方案
需要在数据库中创建项目进度相关的数据表。

## 执行步骤

### 1. 创建数据库表结构
请在数据库管理工具（如phpMyAdmin、Navicat等）中执行以下SQL脚本：
```
create_project_schedule_tables.sql
```

这个脚本会创建以下三个表：
- `project_schedule` - 项目进度表
- `project_schedule_record` - 项目进度验收记录表  
- `project_schedule_record_file` - 项目进度记录附件表

### 2. 插入测试数据
执行以下SQL脚本插入测试数据：
```
insert_project_schedule_test_data.sql
```

这个脚本会插入：
- 5条项目进度记录（拆除工程、水电改造、泥瓦工程、木工工程、油漆工程）
- 6条验收记录
- 4条附件记录

### 3. 验证数据
执行以下SQL查询验证数据是否插入成功：
```sql
-- 查看项目进度
SELECT * FROM project_schedule WHERE project_id = '5066c822136f45ec896d840cb725e110';

-- 查看验收记录
SELECT * FROM project_schedule_record WHERE project_id = '5066c822136f45ec896d840cb725e110';

-- 查看附件
SELECT * FROM project_schedule_record_file;
```

## 注意事项
1. 测试数据使用的项目ID是 `5066c822136f45ec896d840cb725e110`，这是从前端日志中看到的当前项目ID
2. 如果你的项目使用不同的项目ID，请修改 `insert_project_schedule_test_data.sql` 中的 `project_id` 值
3. 表结构使用了 `utf8mb4` 字符集，确保数据库支持中文字符

## 执行完成后
数据库表创建完成后，重新启动后端服务，API应该能正常工作。