# 项目团队成员卡片实现说明

## 概述
为客户主页创建独立的项目团队成员卡片，让客户可以直观地看到项目关联的团队人员。卡片采用现代化设计风格，支持下拉刷新。

## 设计特点

### 视觉风格
- **卡片设计**：白色背景，圆角设计，轻微阴影和边框
- **头部区域**：图标 + 标题 + 成员数量徽章
- **成员卡片**：渐变背景，顶部装饰条，头像带在线徽章
- **响应式**：点击反馈动画，适配不同屏幕

### 布局结构
```
┌─────────────────────────────┐
│ 👥 项目团队          4人    │ ← 头部
├─────────────────────────────┤
│ ┌──────────┐ ┌──────────┐  │
│ │    👤    │ │    👤    │  │ ← 成员卡片
│ │  张三    │ │  李四    │  │   (Grid 2列)
│ │首席设计师│ │项目经理  │  │
│ └──────────┘ └──────────┘  │
│ ┌──────────┐ ┌──────────┐  │
│ │    👤    │ │    👤    │  │
│ │  王五    │ │  赵六    │  │
│ │工程总监  │ │质检员    │  │
│ └──────────┘ └──────────┘  │
└─────────────────────────────┘
```

## 功能特性

### 1. 下拉刷新支持
- 页面下拉时自动刷新团队成员数据
- 项目切换时自动刷新
- 组件暴露 `refresh()` 方法供外部调用

### 2. 状态管理
- **加载状态**：显示旋转加载动画
- **空状态**：显示友好的空状态提示
- **正常状态**：Grid布局显示成员列表

### 3. 交互反馈
- 成员卡片点击缩放效果
- 顶部装饰条渐显动画
- 头像带绿色在线徽章
- 流畅的过渡动画

## 后端实现

### 1. 数据传输对象（DTO）
**文件**: `sb3/evs-home/src/main/java/com/ruoyi/app/dto/response/ProjectMemberVO.java`
- 包含字段：id、userId、name、post、avatar、role

### 2. Mapper层
**文件**: `sb3/evs-home/src/main/java/com/ruoyi/app/mapper/AppUserMapper.java`
- 方法：`selectProjectMembers(String projectId)`
- 关联查询：project_members、sys_user、sys_user_post、sys_post
- 过滤条件：is_active=1、del_flag='0'、status='0'

### 3. Service层
**文件**: `sb3/evs-home/src/main/java/com/ruoyi/app/service/impl/AppProjectMemberServiceImpl.java`
- 方法：`getProjectMembers(String projectId)`
- 数据映射：name（昵称优先）、post（岗位合并）、avatar

### 4. Controller层
**文件**: `sb3/evs-home/src/main/java/com/ruoyi/app/controller/AppDashboardController.java`
- 接口：`GET /app/dashboard/project/{projectId}/members`
- 无需认证，公开访问

## 前端实现

### 1. 组件文件
**文件**: `uni3/src/components/ProjectTeamCard.vue`

#### Props
- `projectId`: String (required) - 项目ID

#### 暴露方法
- `refresh()`: 刷新团队成员数据

#### 特性
- 自动加载数据
- 监听项目ID变化
- 支持外部刷新调用

### 2. 页面集成

#### CustomerDashboard.vue
```vue
<ProjectTeamCard
  ref="teamCardRef"
  :project-id="currentProject.id"
/>
```

- 添加组件引用 `teamCardRef`
- 监听项目切换，自动刷新
- 暴露 `refreshTeamCard()` 方法

#### dashboard/index.vue
```javascript
// 下拉刷新时调用
if (userType.value === 'customer' && customerDashboardRef.value) {
  customerDashboardRef.value.refreshTeamCard()
}
```

### 3. 样式设计

#### 卡片样式
- 背景：白色
- 圆角：24rpx
- 阴影：0 4rpx 20rpx rgba(0,0,0,0.04)
- 边框：1rpx solid 浅灰色

#### 成员卡片
- 背景：渐变（浅灰到白色）
- 顶部装饰条：红色渐变，点击时显示
- 头像：96rpx，白色边框，阴影
- 在线徽章：绿色渐变，白色边框

#### 动画效果
- 点击缩放：scale(0.96)
- 装饰条渐显：opacity 0 → 1
- 加载动画：旋转 0.8s

## API使用示例

### 请求
```
GET /app/dashboard/project/{projectId}/members
```

### 响应
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": [
    {
      "id": "member-001",
      "userId": "1001",
      "name": "张三",
      "post": "首席设计师",
      "avatar": "/profile/avatar/2024/12/03/xxx.jpg",
      "role": "DESIGNER"
    }
  ]
}
```

## 刷新机制

### 1. 下拉刷新
```
用户下拉页面
  ↓
dashboard/index.vue 触发 onPullDownRefresh
  ↓
调用 loadDashboardData(true) 刷新项目数据
  ↓
调用 customerDashboardRef.refreshTeamCard()
  ↓
调用 teamCardRef.refresh()
  ↓
重新加载团队成员数据
```

### 2. 项目切换刷新
```
用户切换项目
  ↓
CustomerDashboard 监听 currentProject 变化
  ↓
调用 teamCardRef.refresh()
  ↓
重新加载新项目的团队成员数据
```

## 注意事项

1. **下拉刷新**：页面下拉时会同时刷新项目数据和团队成员数据
2. **项目切换**：切换项目时自动刷新团队成员
3. **加载优化**：使用 `{ loading: false }` 避免重复显示loading
4. **空状态处理**：项目ID为空时显示空状态
5. **响应式设计**：适配不同屏幕尺寸
6. **性能优化**：使用 Grid 布局，避免复杂计算

## 数据来源
- 主表：`project_members` （项目成员关联表）
- 关联表：`sys_user`、`sys_user_post`、`sys_post`
- 只显示在职（is_active=1）且有效的成员
- 成员按加入项目时间排序（created_at ASC）
